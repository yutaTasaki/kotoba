# 一万日 v14 仕様書 — 世界：問いから主体を引く「問いモード」

v13（主体の盤面）までの世界タブに足す。既存の言葉・引く・相談・問い・撮る・筋トレ・散歩には触らない。**API は使わない**（コピー → チャット → JSON 貼り戻し）。

## なぜ足すのか

いまの主体は固定ではない（種21件のほかに、出典に行動が書かれていれば新顔も作られる）。**固定なのは入口のほう。** いまは「ニュースが先、主体は後」で、札を読んで初めて主体が見える。

本人の言葉：

> 主体ってのは固定にするのはよくないと思う。何を見るかによって主体は変わるから。もっと柔軟であるべき。
> 例えば、知りたいことを打ったらそれに関するであろう主体が出てきて、行動、思惑、お金の動きなどなど重要なファクターが出てくる。そこから必要な情報やニュースが付随してくるみたいな感じ。

欲しいのは逆向きの入口。**「知りたいことが先、主体はそこから出てくる」。**

そしてこの入口だと、**主体の切り方そのものが問いによって変わる**。

- 「なぜ円安が止まらないのか」→ 財務省・日銀のほかに、投機筋・輸出企業・年金基金
- 「ホルムズ海峡はどうなるか」→ イラン政府・湾岸産油国・海運会社・保険会社

種のリストにも札の主語にも、これは出てこない。**札の主語は「出典に行動が書かれた主体」に厳しく縛ってあるので、そのままでは広げられない。**

問いモードは、**同じ倉庫を別の角度から埋める入口**にする。既存の器（札・主体・概念・地層・時系列・共起）はそのまま使い、足すのは「問い」という記録の種類だけ。

---

## 0. 範囲

| | 入れるもの |
|---|---|
| 1 | 問いの記録（`world/asks.json`。`runs[]` で同じ問いを何度でも） |
| 2 | 問いのプロンプト（既知一覧＋関係しそうな札を差し込む）と JSON の貼り戻し |
| 3 | 主体の二層（問いの主体は `runs` の中。既存の主体は id で参照） |
| 4 | 返ってきた行動を**札として**取り込む（重複判定にそのまま乗る） |
| 5 | 入口＝盤面の一番上の1行。過去の問いはその画面の中だけ |
| 6 | 再訪（近い問いを出して人が選ぶ／前の run との差分） |

**網は保留のまま。**

---

## 1. 保存

```
world/asks.json
{ version: 1, updatedAt,
  asks: [{ id: 'q_YYYYMMDD_連番',
           text: 'なぜ円安が止まらないのか',
           key:  '…',                      // worldNormName(text)。近い問いを探すときの手がかり
           runs: [{ at: '2026-09-10',
                    view: '円安を止めているのは介入ではなく、金利差が縮むのを待っている状態',
                    actors: [{ name: '投機筋',
                               actorId: '',           // 既存の主体なら id。問いだけの主体は空
                               want: '…',             // 必須。すべて推定
                               fear: '', bound: '' }],// 任意
                    eventIds: ['w_...'],              // 生成が指定した、既にある札
                    added:    ['w_...'],              // この回で新しく入った札
                    fork: { text, type } | null,
                    note: '' }],
           createdAt, updatedAt, hidden }] }
```

端末は `worldAsks`（kv）。索引に `asks: { updatedAt, count }`。署名比較で変わったときだけ書く（概念・用語・地層・アクターと同じ）。

**同じ問いの複数回は `runs[]` に積む。** 別レコードにすると「同じ問い×違う時期の自分」が並ばない。言葉のアプリの `answers`／予測の `history` と同じ形。

### 1.1 `view` — この問いへの現時点の見立て

`runs[].view`：**1文・60字以内。全部が推定。**

主体・思惑・札・fork だけだと、**問いそのものへの答えが残らない**。再訪したとき前回と比べる材料が「主体の顔ぶれ」しかないのは弱い。1行あると時系列が読める。

`fork` とセットで「**見立て＋それが崩れる分かれ目**」になり、検証できる立場になる。推定の印は思惑と同じ（琥珀のバッジ）。

---

## 2. 主体の二層

| | 既存の主体（財務省・IMF） | 問いだけの主体（投機筋・輸出企業・保険会社） |
|---|---|---|
| 持ち方 | `runs[].actors[].actorId` に id | `actorId` は空。**名前だけ** `runs` の中に |
| 盤面 | 出る（行動が札にあるので） | **出ない** |
| 時系列 | 出る | 出ない |

これで「盤面が投機筋で汚れる」も「二重管理」も起きない。盤面に出るのは**行動が札として入った主体だけ**という条件が既にあるので、問いだけの主体は自然に出ない。

### 2.1 昇格の線引き

**問いの主体が `actors` ストアに入るのは、その行動が札になったときだけ。**

問いモードで「投機筋」に**出典つきの行動**が付いたら、それは札になり、札になれば `ev.actors` に入り、盤面に出る。出典に行動が書かれた時点で、それは札の主語の条件を満たしている——**境界が動く条件はこれ1つだけ**。

逆に、出典の無い「投機筋は円を売りたがっている」は、いつまでも問いの中の主体のまま。

---

## 3. プロンプトに差し込む札の選び方

100件を `id + fact` で全部入れると 6KB。既知一覧（概念400・用語600・アクター60）と合わせると重すぎる。

**既知の語で絞る。** 概念名・用語名・アクター名（`aka` を含む）のうち**問いの文に含まれるもの**を拾い、それが付いた札を集めて `WORLD_ASK_CARDS_MAX`（20）件まで、新しい順。

```
「なぜ円安が止まらないのか」
  → 含まれる既知の語：円安（用語）／為替介入（概念）…
  → その語が付いた札を新しい順に20件
```

日本語の分かち書きは持っていないので、問いの文をそのまま部分一致にかけても当たらない。**既知の語を辞書代わりに使う**のがこの仕組みの要。既知の語が1つも付いていない札は、問いにも関係しにくい。

**0件だったとき**は、絞り込みの語を入れる欄を出す（`円安` のように1語）。それでも0件なら札は差し込まず、`（関係しそうな札は端末にありません）` と書いて出す。

---

## 4. 取り込み

返ってきた JSON を貼ると、**3つの器に流れる**。

1. **行動 → 札**。`shootSafeUrl`・`eventDate` の妥当性・**重複判定（`worldDupOf`）** は通常の取り込みとまったく同じ。落ちた札のうち「重複」だったものは `runs[].eventIds` に入れる（＝既にある札への参照）。新しく入ったものは `runs[].added`
2. **主体 → `runs[].actors`**。既存の主体に当たれば `actorId` を入れる。当たらなければ名前だけ
3. **問い → `asks`**。`view`・`fork`・`note`

札には **`viaAsk: 'q_...'`** を持たせる。1フィールドで「この問いは何を掘り当てたか」が後から引ける。

### 4.1 存在しない札 id が返ったとき

理由を2つに分けて扱う。

- **端末に読み込んでいない月の札**（`monthOfWorldId()` で月が取れて、索引にその月がある）→ **その月を読み込んでから再判定**する
- **本当に存在しない id**（生成の捏造）→ 数えて `※ 端末に無い札 N件は無視しました` に出す

---

## 5. 入口

**盤面の一番上に1行。**

```
[主体] [出来事] [概念] [予測]

  知りたいことから引く                          ▸

動いている主体                    述＝…／推＝…  ▾
  日本政府   …
```

問いモードは盤面と同じ「主体を見る」入口で、**向きが逆**（盤面＝いま動いている主体から、問い＝知りたいことから）。タブに5つ目を足すと390pxで崩れ、右上は `地層／取り込む` で埋まっている。

押すと問いの画面（入力欄・プロンプトのコピー・貼り戻し・過去の問い）。**過去の問いの一覧はこの画面の中だけ。** 既定に出すと「問いの在庫」ができる。

### 5.1 問いの画面に出すもの

**主体と思惑・見立て・分かれ目だけ。行動（札）は問いの画面に出さない**（`runs[].added` から辿れる）。問いの画面を薄く保つ。

```
なぜ円安が止まらないのか                        3回目

2026-09-10
  [推定] 円安を止めているのは介入ではなく、金利差が縮むのを待っている状態
  分かれ目：年内に日米金利差が3%を切るか

  財務省      介入の原資を減らしてでも、輸入物価の上昇を今止めたい     →詳細
  投機筋      金利差が続くかぎり円を売り続けたい
  輸出企業    円安の利益を確定させたいので、急な反転だけを恐れる
  年金基金    外貨建て資産の比率を落としたくない

  この回で入った札 4件 ・ 既にあった札 2件         → 出来事タブで見る

2026-08-12                                        ▸（畳んである）
2026-07-03                                        ▸
```

- `actorId` があれば `→詳細`（アクター詳細＝時系列へ）
- 最新の run だけ開き、過去は畳む
- **思惑はすべて推定**なので、`want` の行に琥珀の縦線（`--guess`）を通す。1行ずつバッジを付けると琥珀だらけになるので、**ブロック全体に1つ**「この欄はすべて推定」と出す

---

## 6. 再訪

### 6.1 同じ問いかどうかは人が決める

`worldNormName()` で完全一致は拾えるが、「なぜ円安が止まらないか」と「円安が止まらない理由」は別になる。**機械で決めず、投げるときに近い問いを出して選ばせる。**

```
知りたいこと：円安が止まらない理由
  近い問いがあります：「なぜ円安が止まらないのか」（前回 8/12）
  [ この問いの続きにする ]  [ 新しい問いにする ]
```

判定は `textSimilarity`（バイグラム＋Dice、`WORLD_ASK_SIM` = 0.60）。概念の統合候補と同じ仕組み。

### 6.2 前の run との差分

再訪の run を取り込んだら、**前の run との差を出す**。

```
2026-09-10                     前回（8/12）からの変化
  いなくなった主体：日本銀行
  新しく出た主体：輸出企業・年金基金
  見立ての変化：
    前回 [推定] 介入で当面は抑えられる
    今回 [推定] 円安を止めているのは介入ではなく、金利差が縮むのを待っている状態
```

**「1か月前は財務省と日銀の話だったのに、今回は輸出企業と年金基金が出てきた」が見えるのが、この機能の一番の収穫。**

差分は保存しない（`runs` から毎回計算する）。名前の突き合わせは `worldNormName`。

---

## 7. プロンプトの規則（要点）

全文は別掲。設計として効かせるのは4つ。

### 7.1 材料が無ければ無いと書く

**問いモードは札の取り込みより捏造しやすい。** ニュースは「起きたことを拾う」だが、問いは「答えを出せ」と迫るので、生成は必ず何かを答える。

```
その問いに答えるだけの材料が一次ソースに無ければ、無いと書く。
主体を無理に挙げない。
行動は一次ソースの出典が付くものだけ。出典が無い行動は書かない。
思惑はすべて推定。事実として書かない。
```

「3件揃わなくていい」と同じ思想を、問いの側にも通す。

### 7.2 `want` は「誰と対立するか」が分かるように

`want` は放っておくと誰にでも言える一般論になる（「財務省は経済の安定を望む」——正しいが何も増えない）。

```
want は、その主体が他の主体と食い違う点が分かるように書く。
良い例：「介入の原資を減らしてでも、輸入物価の上昇を今止めたい」
悪い例：「経済の安定を望む」「物価の安定を重視する」
```

**思惑が見えるとは、誰と誰の望みがぶつかっているかが見えること。** ここが一般論だと機能ごと死ぬ。

### 7.3 `want` だけ必須、`fear`／`bound` は任意

札の3層（did／said／read）は「事実と推定を分ける」ためだったが、**問いの主体は3つとも推定**。3つ書かせると生成は必ず3つ埋め、薄くなる。書けるものだけ書かせる。

### 7.4 主体は5〜7件

`WORLD_ASK_ACTORS_MAX`（7）。10主体返ってくると読めない。

---

## 8. 定数（追加分）

```js
const WORLD_ASK_ACTORS_MAX = 7;    // 1回の問いで挙げる主体の上限
const WORLD_ASK_CARDS_MAX  = 20;   // プロンプトに差し込む既存の札
const WORLD_ASK_VIEW_MAX   = 60;   // 見立ての字数
const WORLD_ASK_WANT_MAX   = 60;
const WORLD_ASK_SIM        = 0.60; // 「近い問い」のしきい値
const WORLD_ASKS_PATH      = 'world/asks.json';
// WORLD_ASK_PROMPT_HEAD（問いのプロンプトの本文）は定数に
```

---

## 9. 守ること

- **API を使わない。** コピー → チャット → JSON 貼り戻し
- **思惑はすべて推定。** 問いの画面のブロック全体に推定の印
- **札の主語（actors）の縛りを緩めない。** 問いの主体は別の層。昇格は「行動が札になったとき」だけ
- **在庫リストを増やさない。** 過去の問いは問いの画面の中だけ。行動は問いの画面に出さない
- 材料が無ければ無いと書かせる。主体を無理に挙げさせない
- `want` を一般論にさせない
- 朝の流れは変えない
- 既存のデータは消さない

---

## 10. 段階

**一度に入れる。** 問いモードは「投げて、返して、貯まる」で1つの動作で、「主体と行動を返すだけ」で止めると**チャットの出力を眺めるだけ**になり、既存の「Claude に聞く」と変わらない。

**再訪（6）だけは後回しにできる**（1回目は必ず新しい問いなので、2回目を投げるまで使わない）。ただし **`runs[]` の構造は最初から入れる**——あとで移行したくない（予測の `history` と同じ判断）。

---

## 10.5 実装時の差分（v14）

- **1日1 run。同じ日に貼り直すと上書き。** `runs` を `at` で束ねているため（同期のマージも同じキーで束ねるので、そもそも同じ日に2つは残せない）。貼り直しで直せる形なので、これでよい
- **再訪の差分表示（6.2）は入れていない。** `runs[]` の構造は最初から入れたので、あとから足せる。1回目は必ず新しい問いなので、2回目を投げるまで使わない——実物を見てから形を決める
- `actions[].actor` が主体一覧に無い名前でも、札は入る（札の主語として登録される）。ただし `runs[].actors` には出ないので、問いの画面では見えない。プロンプトで「上の主体のどれかの名前」と縛ってあるが、破られても壊れない側に倒した
- 「出来事タブで見る」は `batchFilter` を使い回す。`id` を空にして `partnerIds` に問いの札を入れる形にした（この回の札／つながった相手、の2段組みではなく1段で出す）

## 11. 実装後に見る数字

- **`view` が「〜と考えられる」で終わっていないか。** 見立てが一般論なら 7.2 の縛りを `view` にも広げる
- **主体の何件が `actorId` を持つか。** 全部が既存なら問いモードは盤面の焼き直し。全部が新顔なら既知の主体を見ていない
- **「材料が無い」と返ってきた回があるか。** 1回も無ければ 7.1 が効いていない
- 取り込みの `※ 端末に無い札 N件は無視しました`

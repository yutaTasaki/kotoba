# 一万日 v11.5 仕様書 — 世界：網を実際に育てる

段階2までの世界タブに足す。既存の言葉・引く・相談・問い・撮る・筋トレ・散歩には触らない。

## なぜ足すのか

概念を羅列しても見ない。14件が回数順に並んでいるのは在庫の棚卸しで、開く理由がない。

このアプリの目的に照らすと——

> 日々の隙間時間に情報を集めて、体系的に理解し、自分が応用できるようになること。

「集める」は動いている。**「体系的に理解する」が動いていない。** 概念は保存されているが、繋がっていない。

### 繋がっていない理由は、見せ方ではなく仕組みだった

```js
ev.related.forEach((n) => { const c = touchConcept(n, '', [], false); ... });
                                                          ↑ makeIfMissing = false
```

`related` は**既に存在する概念しかリンクしない**。存在しない名前はリンクもされず、作られもせず、黙って捨てられる。加えてプロンプトが `related：…（既知の一覧から）` と縛っている。

- **1回目の生成**：既知の一覧は空 → リンク **0本**
- **2回目の生成**：既知は1回目の概念だけ → **2回目の概念どうしは絶対に繋がらない**

バグではなく仕様の帰結。**待っていても網は育たない。**

一方で `concept.eventIds` は1回目から溜まっている。**網（related）はまだ無いが、再会（eventIds）はもうある。**

### 位置づけを変える

概念タブは「日々開く場所」ではなく **「たまに整理する場所」**。そう決めるなら羅列のままで正しい。消すべきは一覧ではなく、**一覧を毎日見るつもりだった前提**のほう。

体系ができるのは**新しい札を読む瞬間**なので、繋がりはそこに差し込む。

---

## 0. 範囲

| | 入れるもの |
|---|---|
| 1 | 一緒に出た概念を自動で繋ぐ（共起リンク）＋ 既存データへの遡及 |
| 2 | 再会を見せる（タグに回数、premise カードに1行） |
| 3 | 今日の3件の共通点を1行（**地層**で判定、無ければ出さない） |
| 4 | 概念・用語・出来事の各タブに検索 |

**段階3**：答え合わせ／来なかったもの／枠組み／月に一度 自分の言葉で書く

「月に一度、自分の言葉で書く」は「応用できる」側の唯一の検証だが、**月の振り返りという器がまだ無い**。器のほうが先。

---

## 1. 共起リンク

### 1.1 何をするか

**同じ札に一緒に出た概念どうしを繋ぐ。** `concepts` と premise が同じ出来事に載っている時点で「一緒に出た」という事実がある。生成にも API にも頼らない。

「なぜ繋がっているか」が **「同じ札に出たから」** という検証可能な理由になる。生成が「関係が深いと思う」と言っただけより強い。

1回目の生成から網ができ、既存データにも遡って適用できる。

### 1.2 リンクの形（`related` を置き換える）

いまの `concept.related = [id, ...]` では、1回の共起と5回の共起が同じ太さになる。持ち方を変える。

```
concept.links = [{ id: 'wc_...',
                   by: 'cooc' | 'model',   // 出所。あとで related の要否を判断するため
                   count: 3,                // 何回一緒に出たか
                   eventIds: ['w_...'],     // どの札で一緒に出たか
                   at: '...' }]
concept.rejected = ['wc_...']              // 手で外したもの。二度と繋がない
```

**移行**：既存の `related: [id]` は読み込み時に `links: [{id, by:'model', count:1, eventIds:[]}]` になる。データは消えない。`concept.related` というキーは使わなくなる（`ev.related`（出来事側）はそのまま残る。別物）。

### 1.3 歯止め（3つ）

そのままだと、いずれ全部が全部と繋がって網が意味を失う。

**(1) 回数を数える。** 表示は `count` の多い順。1回の共起と5回の共起を同じに見せない。

**(2) しきい値を定数にする。**

```js
const WORLD_COOC_MIN = 1;   // これ以上の共起を related として見せる
```

いまは 1（データが少ないので 2 にすると何も出ない）。概念が100件を超えたあたりで 2 に上げる。**しきい値は表示にだけ効かせ、`count` は常に数える**（あとから上げても下げても、数え直しが要らない）。

**(3) 手で外せる。** 概念詳細のつながりの行を長押し／×で外す。外したものは `rejected` に入り**二度と繋がない**。kotoba の自動関連づけと同じ形（「自動で結び、後から外せる」は言葉のアプリで既に正しいと分かっている）。

**もう1つ、爆発の歯止めを足す。** `ev.concepts` は最大8件なので、premise と合わせると1つの札から最大 9×8/2 = 36 本のリンクが出る。1回の取り込みで千本単位になりうる。

```js
const WORLD_COOC_MAX_PER_EVENT = 5;   // 1つの札で繋ぐ概念の数（→ 最大10本）
```

premise を先に取り、足りない分を `ev.concepts` の並び順で埋める。

### 1.4 出所を分ける

`by: 'model'`（生成が書いた `related`）と `by: 'cooc'`（共起）を区別して持つ。あとで**「生成の related は結局役に立ったのか」**を見られるようにする。役に立っていなければ、プロンプトから `related` を落とせる。

概念詳細のつながりの行に、小さく出所を出す（`同じ札に3回` / `生成が指定`）。

**正直に書いておく比較の偏り**：`by: 'model'` は構造上ほぼ増えない（既知の概念しか指せないため）。だからこの比較は「model が役に立たない」に倒れやすい。**それでも比較する価値はある**——2回目以降、既知の概念が増えてから model の related が実際に何本作るかは、いまは誰にも分からないため。判断は数か月後でよい。

### 1.5 遡及と作り直し

設定「6. 世界」に **「概念のつながりを作り直す」** を置く。

1. 索引にある全部の月を読み込む（`worldRebuildDue` と同じ手）
2. **`by: 'cooc'` のリンクを一度すべて捨てる**（`by: 'model'` と `rejected` は残す）
3. 全部の出来事から共起を数え直す
4. **`concept.eventIds`（＝再会の回数）も数え直す**
5. **何本できたかを画面に出す**

**4 は実装しながら足した。** リンクが育たないのと同じ理由で、**再会の回数も過少になっている**。`ev.concepts` も `makeIfMissing = false` なので、まだ存在しない概念の名前は取り込み時に捨てられている。あとから同じ概念ができても、前の札に出ていたことは記録されない。作り直しのときは全概念が揃っているので、そこで数え直す。

**`eventIds` は減らさない（足すだけ）。** 読めなかった月があったときに、その月の分を消してしまわないため。

**何度押しても同じ結果になる**（冪等）。押した回数で本数が増えない。

**この数字が答え合わせになる。** 数本しかできなければ、原因は見せ方ではなく、もっと手前（premise が1件1つで `concepts` が薄い、そもそも札の数が足りない）にある。

自動では走らせない（起動時に黙ってデータを書き換えるほうが危ない）。ただし、**共起リンクが0本で出来事が10件以上あるとき**は、概念タブに1行だけ出す：`つながりがまだ作られていません。設定 → 世界 → 概念のつながりを作り直す`。

---

## 2. 再会を見せる

**新しい札を読む瞬間に、それが既に知っていることのどこに乗るかを見せる。** 一覧を見に行く行為にはこの瞬間がない。だから一覧は見ない。

2回目・3回目こそ網ができる瞬間なので、そこを逃さない。

### 2.1 一覧の行 — 行を増やさない

概念タグの中に回数を足す。**文字が2つ増えるだけで、行は増えない。**

```
9/2  中銀が国債買い入れの減額幅を縮めた   [日金利] [イールドカーブ 3]
```

- 初出：今のとおり色付き（`--accent`）、数字なし
- 2回目以降：灰色＋回数
- `WORLD_REUNION_STRONG`（既定3）以上：少しだけ濃く

`premise.kind: 'relation'`（`A × B`）のときは数字を出さない（2つあるので、どちらの回数か分からなくなる）。

### 2.2 展開したとき — premise カードの中に1行

カードは増やさない。既にある premise カードの中に1行足す。

```
イールドカーブ ── 満期の違う国債の利回りを並べた線。
3回目 ・ 前は 9/9「中銀が国債買い入れの減額幅を縮めた」
```

- 「前は」は、この札を除いていちばん新しい、その概念が出た出来事
- **読み込んでいない月の出来事は引けない**ので、その場合は `3回目` だけ出す
- 初出（1回目）のときは**何も出さない**（「1回目」と書いても情報がない）

---

## 3. 今日の3件の共通点

**概念ではなく地層で判定する。** 概念は premise が1件1つ・`concepts` も薄いので、3件が共有することが稀で空振りする。地層は13件しかないので衝突する。

```
今日の3件のうち2件が「日本の長期金利」の上に乗っています
```

- 2件以上が同じ地層を共有したときだけ出す
- 複数該当したら、共有数の多いもの1つだけ
- **共通点が無い日は何も言わない。** 無理に一言ひねり出すと、毎日出る飾りになって読まれなくなる

**置き場所：展開の中だけ。** 世界タブの「今日の3件」の上、朝はステップを開いたあとの中。**畳んだ行には出さない**（朝の流れをこれ以上長くしない）。

---

## 4. 検索

3つのタブそれぞれの上に検索を1つ置く。**タブをまたぐ横断検索は作らない**（探すときは、何を探しているかは分かっている）。

| タブ | 探す対象 |
|---|---|
| 出来事 | `fact` / `why` / `detail` / `source` |
| 概念 | 名前 / 別名 / 注 |
| 用語 | 名前 / 別名 / 注 |

- 大文字小文字・全角半角・中黒・長音を落として比較（`worldNormName` と同じ正規化）
- 入力は保存しない（画面を離れたら消える）
- 空のときは今までどおり全件
- 出来事の検索は**端末に読み込んでいる月だけ**が対象（その旨を1行出す）

一覧は残す。**理由は「探すため」ではなく、統合・名前変更・上位概念・非表示の操作口が一覧しかないから。** 整理の仕組みを入れた以上、整理する場所は要る。

---

## 5. 生成プロンプトの変更

**変更なし。**

`related` は残す（1.4 のとおり、出所を分けて比較するため）。共起はアプリの中だけの計算なので、生成には何も足さない。

将来 `related` を落とすと決めたら、そのとき2行消すだけで済む。

---

## 6. 定数（追加分）

```js
const WORLD_COOC_MIN           = 1;   // これ以上の共起を「つながり」として見せる
const WORLD_COOC_MAX_PER_EVENT = 5;   // 1つの札で繋ぐ概念の数（→ 最大10本）
const WORLD_REUNION_STRONG     = 3;   // この回数から、タグを少し濃くする
const WORLD_COOC_HINT_EVENTS   = 10;  // 出来事がこれ以上あってリンク0なら、作り直しを促す
```

---

## 7. 守ること

- **既存データを消さない。** `related` は `links` に読み替えるだけ。作り直しは `by: 'cooc'` だけを捨て、`by: 'model'` と `rejected` は残す
- 作り直しは**冪等**。何度押しても同じ本数になる
- 共起は**1つの札あたり `WORLD_COOC_MAX_PER_EVENT` 件まで**。爆発させない
- しきい値は**表示にだけ**効かせる。`count` は常に数える
- **一覧の行を増やさない**（再会は概念タグの中の数字だけ）
- **朝の畳んだ行を増やさない**（今日の共通点は展開の中だけ）
- 共通点が無い日は何も言わない
- 生成プロンプトは変えない
- 既存の言葉・引く・相談・問い・撮る・筋トレ・散歩に影響を出さない

---

## 8. 実装後に出す数字

「概念のつながりを作り直す」を1回押して、**既存の出来事から共起リンクが何本できたか**を報告する。

- 数十本できていれば、原因は見せ方だった
- 数本しかできなければ、原因はもっと手前（札の数・premise の粒度・`concepts` の薄さ）にある

**この数字を見てから、次に何を直すかを決める。**

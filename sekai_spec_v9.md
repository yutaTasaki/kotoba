# 一万日 v19 仕様書 — 世界：データを捨てて、お金の動きから作り直す

指示書 sekai_okane_shiji_v3.md と回答書（2回目）で確定したことを、実装できる粒度に落としたもの。
既存の言葉・引く・相談・問い・撮る・筋トレ・散歩・瞑想には触らない。**API は使わない**。

実装の順は **1 → 3 → 4 →［Yuta さんが実行］→ 2 → 5 → 6**。1段階ごとに1コミット。

---

## 0. 押す順番（Yuta さんの操作）

**「世界のデータを捨てる」ボタンは、段階1で入るが、押すのは段階4のあと。**
新しいプロンプトが期待どおり返らなかったとき、古いデータと見比べられるようにするため。

```
① 段階4まで入った版が公開される（公開したと知らせが来る）
② 新しい版に入れ替える（下の「新しい版にする手順」）。
   PC と iPhone の両方で、設定 → 世界 の一番下に
   「版 v19 ・ 世界の世代 0」と出ていることを確かめる      ← 両方が新しい版になった印
③ PC で 世界 → 取り込む →「プロンプトをコピー」
④ Claude のチャットに貼る → 返ってきた JSON を
   **テキストファイルに保存する**（メモ帳に貼って「world_20260911.txt」などで保存）。
   中身を見る（見るところは 4章）。ここではまだ取り込まない
⑤ よさそうなら PC で 設定 → 世界 →「世界のデータを捨てる」
   → バックアップ2つ（保存庫とダウンロード）ができてから消える
   → 「世界の世代 1」になる
⑥ iPhone で世界タブを開く → 同期のあと「世界の世代 1」になり、iPhone 側の古いデータも消える
⑦ PC で 世界 → 取り込む に、④で保存したファイルの中身を貼って取り込む
   （生成し直さなくてよい）
```

②を飛ばしてはいけない。**古い版のアプリは世代番号を知らないので、古い版のまま同期すると、
古い札を保存庫に書き戻す。**（1.4 の「作り前の札は読まない」でも守るが、二重にしておく）

### 新しい版にする手順

このアプリは、公開されたページを**最大10分**端末に覚えておく作りになっている（保存庫の設定）。
それより早く開くと古い版のまま動く。

**iPhone（ホーム画面の一万日）**
1. 公開の知らせから**15分待つ**
2. 画面の下の端から上へゆっくりスワイプして止め、アプリの一覧を出す
3. 一万日の画面を**上へはじいて閉じる**（ホーム画面に戻るだけでは閉じない）
4. ホーム画面の一万日から開き直す
5. 設定 → 世界 の一番下に「版 v19」が出ていればよい。**出ていなければ、10分待って 2〜4 をもう一度**

**PC（ブラウザ）**
1. 公開の知らせから15分待つ
2. 開いている一万日のタブで **Ctrl + Shift + R**（強制的に読み込み直す）
3. 設定 → 世界 の一番下に「版 v19」が出ていればよい

**次の公開から**：新しい版には、設定 → 世界 の一番下に「最新の版を読み込む」ボタンが付く。
押すと、端末に覚えている古いページを使わずに読み込み直す。v19 に入れ替わるまでは古い版なので、このボタンは無い。

---

## 1. 段階1：世界のデータを捨てる（コードだけ入れる。押すのは0章の⑤）

### 1.1 捨てるもの・残すもの

| 器 | 扱い |
|---|---|
| 札（月ファイル `world/YYYY-MM.json`） | **全部空にする**（中身の無い月ファイルを書く） |
| 概念 `world/concepts.json` | 空にする（16件。半分が札限りの語なので残す価値が薄い） |
| 用語 `world/terms.json` | 空にする（ファイルは残す。取り込みで注を書く先として使い続ける） |
| 問い `world/asks.json` | 空にする |
| 予測の期限一覧（index の `due`） | 空にする |
| 主体 `world/actors.json`（24件） | **残す。** 名前・別名・種類・ふだん欲しがるものはそのまま。`eventIds`・`firstEventId`・`lastUsedAt`・`links` だけ空にする |
| 地層 `world/strata.json` | **残す。値は1件も消さない。** 出所の印で機械的に分ける：手入力（`hand`）・確認プロンプトから（`probe`）→ そのまま／札から（`card`）→ 値・出典名・URL は残し、`eventId` だけ空にする |
| ソース `world/sources.json`（22件） | **丸ごと残す**（立場の1行つき） |

言葉・散歩・筋トレ・瞑想・記録（`data.json` ほか世界以外のファイル）には触らない。

### 1.2 バックアップ（消す前に2つ。どちらかが失敗したら消さない）

1. **保存庫**：kotoba-data に `world/_backup_YYYYMMDD.json` を1つ書く。同名があれば `_2`, `_3`…
2. **端末**：同じ中身を `kotoba_world_backup_YYYYMMDD.json` としてダウンロード（言葉のエクスポートと同じ作り）

中身は、index・**全部の月ファイル**・概念・用語・問い・主体・地層・ソース。

止める条件（上から順に見る。**どれか1つでも当たったら、その場で止めて何も消さない**）：

1. 月ファイルが1つでも読めなかった → 「読めなかった月：2026-07、2026-08」と月の名前を出す。
   月ファイルは、端末にあるものではなく**保存庫から読み直す**（端末に無い月もあるため）。
   index に載っている月と、端末に残っている月の両方を対象にする
2. 保存庫に書けなかった（通信なし・トークンなし・エラー）
3. 端末へのダウンロードを始められなかった

### 1.3 世代番号

- `world/index.json` に `epoch`（数、既定0）と `resetAt`（日時）を足す
- 捨てる操作：`epoch` を1つ上げ、`resetAt` に今の日時を入れ、1.1 のとおり空にしたものを保存庫に書く
- 同期（`worldSyncNow`）で相手の index を読んだ直後に比べる：
  **相手の `epoch` が自分より大きい → 自分の世界データを 1.1 と同じ規則で捨ててから、相手の index を採る。**
  そのあと通常の同期に進む（主体・地層・ソースは、捨てた側が新しい日時で書いているので相手が勝つ）
- index を合わせるとき（`mergeWorldIndex`）：`epoch` は大きい方。**`epoch` が違うときは、月と `due` を足し合わせず、大きい側を丸ごと採る**
- 設定 → 世界 の一番下に「世界の世代：N」を1行出す（0章②で両端末の版を確かめるため）

### 1.4 作り前の札は読まない（二重の守り）

`resetAt` があるとき、**`createdAt` が `resetAt` より前の札は、月ファイルを読むときと合わせるときに捨てる。**
古い版の端末が古い札を書き戻しても、画面にも取り込みの重複判定にも出てこない。

### 1.5 画面

設定 → 世界 の一番下（「予測の索引を作り直す」の下）：

```
世界のデータを捨てる
  札・概念・用語・問いを消します。主体（24件）・地層の値・ソースは残します。
  消す前に、保存庫とこの端末にバックアップを作ります。
  もう一方の端末は、次に開いたときに同じように消えます。
  [捨てる]
世界の世代：0
```

**ボタンは3つに分け、上から順にしか押せないようにする。**

```
[1 バックアップを作る]   全部の月を保存庫から読み直し、保存庫に _backup を書く
[2 この端末に保存]       1 で作った中身をダウンロードする
[3 捨てる]               1 と 2 が済むまで押せない。確認を1回出してから実行
世界の世代 0 ・ 版 v19
[最新の版を読み込む]
```

- 1 と 2 を分ける理由：iPhone の Safari は、ボタンを押してから時間のかかる読み込みを挟んだあとのダウンロードを
  止めることがある。2 を「押した瞬間にダウンロードするだけ」にしておけば止められない
- 3 の確認：「札・概念・用語・問いを消します。元に戻すにはバックアップを手で戻す必要があります。捨てますか」
- 3 の結果を1行：「札 N件・概念 N件・用語 N件・問い N件を消しました。世代 1」
- 途中で保存庫への書き込みが失敗したら：「途中で止まりました（何を書けなかったか）。もう一度 3 を押すと続きをやります」
- 押しても何も起きない状態を作らない：トークンが無い・通信が無いときは、1 の下に理由を出して押せなくする
- 「最新の版を読み込む」：端末に覚えている古いページを使わず、新しい住所として読み込み直す（0章）

---

## 2. 段階3：お金の動きに id・kind・after を足す

### 2.1 id

**札の id をそのまま使う**（`w_20260909_3`）。お金の動きは札1枚に1つ。別ファイルに切り出さない。
（2つ目が要る日が来たら `w_…#2` の枝番。今回は作らない）

### 2.2 形

```
flow: {
  from, to, what, amount, value, unit, span, strata,   // 既存
  kind:  'money' | 'goods' | 'people',                  // 新規。無ければ money
  after: 'w_…' | ''                                     // 新規。どの動きの続きか
}
```

- `goods`（物）・`people`（人）には金額が無くてよい。`from`・`to` だけでよい。単位は付けない
- `after` は **札の id の形（`w_YYYYMMDD_N`）のときだけ残す**

### 2.3 取り込みで `after` を確かめる

- 指している札が端末に無ければ、その月を読み込んでから確かめる（id に月が入っているので分かる）
- **お金の動きを持つ既存の札を指していなければ、空にする**（地層の id と同じ扱い。数だけ知らせる）
- **同じ JSON の中の前の札を指すときは `"#2"` と書かせる**（2番目の札）。取り込みで新しい id に置き換える。
  **自分より前の番号だけ許す**（輪を作らせない）
- **`#N` は、落とす前の並び（生成が返したそのままの順、1から数える）で数える。**
  取り込みは重複・URL なし・日付の不正を落とすので、残った並びで数えると番号がずれ、
  エラーも出ずに違う札へ繋がる
- **指していた札が落とされていたら、空にする**（存在しない id を指したときと同じ扱い）
- 取り込みの結果に1行足す：「お金つき N件（金額あり N・物 N・人 N）／続き N件／
  指し先が無くて外した続き N件／落ちた札を指していて外した続き N件」

### 2.4 物と人は、既定の画面に並べない

「お金の流れ」「お金の一覧」「探すのお金」「主体の詳細のお金の段」「時系列の右端の金額」は **`kind` が money のものだけ**。
物と人は、札の中（「動かした側」の下の1行に［物］［人］の印）と、段階6の鎖の中にだけ出る。

### 2.5 確かめ方

手で作った JSON（`after` が既存 id／`#2`／存在しない id／自分より後ろの `#5`／kind が3種）を取り込み、
結果の数字と、札の中の表示を見る。

---

## 3. 段階4：プロンプトを1本にして、お金を先にする

### 3.1 取り込みの画面

「金融・経済（A）」「地政学・政治（B）」の2つのボタンを、**「プロンプトをコピー」1つ**にする。
札の `field` は今後使わない（取り込みでは既定の値を入れ続ける。画面には出していない）。
「既存の札に付け直す」（遡及）の欄は外す（捨てるので対象が無い）。

### 3.2 ソース

- 一次・解釈を分野で分けず、全部を1つの一覧にする（重複は除く）
- **日次のお金の源を2件足す**（段階5の予定だったが、初回の実行で PDF が読めるかを確かめたいので、ここで入れる）：

| 名前 | URL | 何を取るか（プロンプトに書く1行） |
|---|---|---|
| 米財務省 Daily Treasury Statement | https://fiscaldata.treasury.gov/datasets/daily-treasury-statement/ | その日の支払い・受け取りのうち、**金額の大きい順に最大2件**。ただし毎日ある定例のもの（社会保障の給付、国債の償還、税の還付）は除く。from は「米財務省」 |
| 日本銀行 当座預金増減要因と金融調節（毎営業日） | https://www.boj.or.jp/statistics/boj/fm/juq/index.htm | 「財政等要因」（政府と民間のあいだ）と「日銀のオペ」（日銀から金融機関）の金額。大きく動いた日だけ札にする |

  - 決まった id（`ws_money_dts`・`ws_money_boj_juq`）で、**まだ無ければ足す**。何度起動しても1回だけ
  - 役割は一次。立場の1行は空（あとで「まとめて貼る」で入れる）

### 3.3 規則の変更（いまのプロンプトとの差分）

1. **選び方**（新しい節）：「お金が動いた出来事を優先する。同じくらい重要なら、金額が出典に書かれている方を選ぶ」
2. **お金の欄を必須寄りにする**：
   - 出典に金額があれば必ず書く
   - 金額が無くても、誰から誰へ動いたかが出典に書かれていれば `from`・`to` だけ書く
   - 出典に書かれていない移動は書かない。金額を推定しない。換算しない。合計しない
   - 書けなければ `null`（札そのものは返してよい）
   - `kind`：お金なら money、原油・穀物・兵器など物なら goods、移民・避難・人材など人なら people
   - `after`：末尾の「最近のお金の動き」の中に、この動きの**原因や前段**に当たるものがあれば、その id。
     同じ JSON の前の札の続きなら `"#番号"`。無ければ `null`。**無理に繋がない**
3. **平たい言葉**（fact・why・did に足す）：「注が要らないくらい平たい言葉で書く。専門用語を使うなら、その場で言い換える」
4. **用語**：terms は「本文にどうしても残った組織名・国名・制度名だけ」。末尾の既知の用語一覧は外す
   （取り込みは、すでにある用語の注を上書きしない作りなので、同じ注が二重に入ることはない）
5. **概念の例外**：
   - 既知の概念が **10件未満のとき**：premise は新しく作ってよい（kind "new"）。concepts は premise だけでよい
   - **札限りの固有名詞は概念にしない**（悪い例「GDP2次速報」「ホルムズ海峡」／良い例「交易条件」「金融政策の正常化」）
   - 10件以上になったら、いまの規則（既知から必ず探す）に戻る。**この切り替えはプロンプトを作るときに件数で自動で行う**
   - 取り込み側は変えない（いまも概念を新しく作れるのは premise だけで、concepts の知らない名前は無視している）
6. **予測**：lean と because を出力の形からも規則からも外す。fork（分かれ目の文）は残す。1回3件までも残す
7. **件数**：12件を目安（A・B 合わせたぶん。確認できたものだけ）
8. 末尾に足す：

```
■ 最近のお金の動き（after はこの id から選ぶ。ここに無い id を書かない）
w_20260909_3　9/9　財務省→為替市場　円買い介入　3.2兆円
…（直近30日・新しい順に30件。money を先、goods・people を後）
```

  1行平均44字、30件で約1,300字（実測）。捨てた直後は0件なので「（まだありません）」になる。

### 3.4 長さ

実装したら実測して報告する。見込み：いまの約8,700字 − 用語の規則と既知一覧 − lean の規則 ＋ 選び方・お金の規則 ＋ 最近のお金30件
＝ **約1万字**。

---

## 4. Yuta さんの実行で見るところ（0章④）

返ってきた JSON で、次を数える（取り込み後の結果の1行にも同じ数が出る）：

- 札の数（目安12）／そのうちお金つきの数（**半分を超えていれば狙いどおり**）／金額ありの数
- 米財務省 DTS から来た札があるか（2件以内か。定例の給付が混ざっていないか）
- 日銀の当座預金の札があるか（**無ければ PDF が読めていない可能性**。そのとき別の源を探す）
- concepts に固有名詞が入っていないか
- `after` が付いた数（捨てる前は候補が古い札なので、付いていてもよい）
- lean・because が出ていないこと

---

## 5. 段階2：用語集をやめる・予測を畳む（画面から外すだけ。データは消さない）

### 5.1 用語

外す場所：概念の詳細の用語の表示／整理の型の切替の「用語」／「しばらく出ていない用語」／探すの型の切替の「用語」／
問いのプロンプトの既知の用語一覧／90日で沈む規則（`worldTermSunk` を使う場所が無くなる）。

残すもの：札の本文の注（`e.terms` と `terms.json`）／取り込みで注を書く処理／「反対の見方は」に渡す注（あれば渡す。無ければ渡さない）。

### 5.2 予測

外す：札の「予測」ボタンと3択・一言・期限の選択／「答え合わせ N件」の行／答え合わせの画面／
設定の「予測の索引を作り直す」／取り込みの結果の「分かれ目つき N件（うち生成の見立てつき N件）」→「分かれ目つき N件」。

**分かれ目の文は、予測の枠の外に出して常に表示する。** いまは予測の枠の中にあり、枠は予測が無い札では隠れているので、
ボタンを外すと分かれ目の文ごと見えなくなる。

残す：`prediction` のデータ／記録カレンダーの「予測を書いた」「当たり」（中身があるときしか出ないので空欄にならない）。

---

## 6. 段階5：ソースに周期を持たせ、「今日の取り込み」1ボタンにする

### 6.1 持ち方

- **周期**：`sources.json` の各ソースに `cadence`（`day`／`week`／`dekad`／`month`／`quarter`）
  - 初期値：一次＝**日次**、解釈＝**週次**、日次のお金の源2件＝日次
  - 設定 → 世界 → ソースの行に、分野の切替と同じ形で周期の切替を足す
- **最後に取り込んだ日**：`world/index.json` の `pulls: { ソースid: 'YYYY-MM-DD' }`
  - 合わせ方は**ソースごとに新しい日付を採る**（どう合わさっても、最悪「もう一度ボタンが出る」だけで済む）
  - `sources.json` に持たない理由：ソースは新しく更新された側が丸ごと勝つので、取り込みで日付が進むたびに、
    別の端末で直した立場の1行が負けて消えることがある

### 6.2 期限

| 周期 | 期限が来る条件（最後に取り込んだ日から） |
|---|---|
| 日次 | 1日 |
| 週次 | 7日 |
| 旬 | 10日 |
| 月次 | 30日 |
| 四半期 | 90日 |

一度も取り込んでいないソースは、期限が来ているものとして扱う。

### 6.3 いつ記録するか

- コピーしたとき：その中に入れたソースの id を、端末に控えるだけ
- JSON を貼り戻して取り込めたとき：控えたソース全部の日付を今日にする。**確認できた札が0件でも「見た」ことにする**
- コピーしただけで貼らなかった日は、次の日にもう一度ボタンが出る

### 6.4 朝の画面

朝の流れの一番最後（いまの「世界 ・ 動いた主体 N」の行の下）：

- 期限が来ているソースがある日：**「今日の取り込み（N件）」ボタン**。押すと、その N件ぶんのプロンプトがコピーされ、
  世界 → 取り込む の画面が開く（貼る欄が見える状態）
- 無い日：ボタンを出さない。**畳んだ行に薄い字で「今日は取り込むものがありません」**（文字だけ）
- 世界の画面の右上「取り込む」は残す（期限に関係なく全部のソースで作れる、いつもの入口）

プロンプトは1本。ソースの一覧を「今日見るソース」とし、ソースごとに「何を取るか」を1行ずつ書く（3.2 の表の3列目の形）。

### 6.5 削る形（間に合わないとき）

周期を日次だけにする（`cadence` は持つが、切替を出さない）。実際には毎日全部のソースが出る。

---

## 7. 段階6：既定の画面をお金の動き中心にする

### 7.1 鎖

**表示するときに計算する。何も保存しない。**

- 読み込んでいる月の札から、`after` で繋がった塊を作る（前へ辿る＋「続きの一覧」を組んで後ろへ辿る）
- 一度通った札は覚えておいて止める（輪を防ぐ）。辿る深さは最大12
- 塊が2件以上のものが「鎖」。名前は持たない。見出しは自動：「**財務省を中心に 7件**」（from・to に一番多く出た主体＋件数）
- 物・人の動きは、鎖の中の一手としてだけ出す（［物］［人］の印つき）

### 7.2 既定の画面

```
（地層の1行）

お金の鎖                                   最大3本（最後に動いた順）
  財務省を中心に 4件                        最後 9/9
    9/2  イラン → 中国     ［物］原油の輸出
    9/5  日本の輸入元 → 産油国   原油代金   2.1兆円
    9/9  財務省 → 為替市場       円買い介入 3.2兆円

お金の動き（単発）                          3本 ＋「お金の一覧」

今日動いた主体                              5件で「もっと見る」（A-1/A-2 のまま）
背景に沈めた札 N件
知りたいことから引く                        （問いのデータは捨てるが、機能は残す）
探す
```

### 7.3 朝

朝の畳んだ行を開いたときの中身を、**札の一覧から「今日動いた主体」の行に替える**（前回の段階0）。
あわせて、朝の画面で押しても何も起きない2か所を直す：背景の札の開閉（いまは世界の画面を描き直している）、
「もっと見る」（世界の画面へ移る処理が無い）。

**段階6を削るときは、この 7.3 だけを単独で入れる（約10行）。**

---

## 8. 定数

```js
const WORLD_PROMPT_COUNT        = 12;   // 1本で求める札の数（A・B 合わせたぶん）
const WORLD_RECENT_FLOWS_MAX    = 30;   // プロンプトに差し込む最近のお金の動き
const WORLD_RECENT_FLOWS_DAYS   = 30;   // その対象期間
const WORLD_CONCEPT_BOOTSTRAP   = 10;   // 既知の概念がこれ未満なら、新しく作ってよい
const WORLD_CHAIN_DEPTH_MAX     = 12;   // 鎖を辿る深さ
const WORLD_CHAINS_SHOWN        = 3;    // 既定の画面に出す鎖の数
const WORLD_FLOW_KINDS          = ['money', 'goods', 'people'];
const WORLD_CADENCES_SRC        = ['day', 'week', 'dekad', 'month', 'quarter'];
const WORLD_CADENCE_SRC_DAYS    = { day: 1, week: 7, dekad: 10, month: 30, quarter: 90 };
const WORLD_DTS_MAX_PER_DAY     = 2;    // 米財務省 DTS から取る札の上限（プロンプトに書く数字）
```

---

## 9. 守ること

- **API を使わない。** コピー → Claude のチャット → JSON を貼り戻す
- **事実と推定を分ける。** did／said／read（推定は琥珀の印）。said と read が同じなら read は空
- **url が無い値・結果は取り込まない**
- **換算しない、合計しない。** 鎖の見出しにも合計額は出さない（件数だけ）
- **在庫リストを増やさない。** 鎖3本・単発3本で絞る。畳まない
- **押しても何も起きないボタンを作らない。** 取り込むものが無い日はボタンを出さない
- **今回の削除は Yuta さんが決めた一度きりの例外。** バックアップ2つが揃ってから消す。対象は世界タブだけ
- **地層の値は1件も消さない。** 主体は24件全部残す。ソースは丸ごと残す
- 概念は残す（用語だけをやめる）。`terms.json` は消さない
- 分かれ目の文は残す（押させるのをやめるだけ）

---

## 10. 実装後に見る数字

- **札のうちお金つきの割合。** 半分を超えていれば、選び方の規則が効いている
- **`after` が付いた割合。** 捨てた直後は0で、1週間で何本の鎖ができるか。鎖が1本もできなければ、差し込む件数か `after` の規則を見直す
- **外した続き（指し先が無い `after`）の数。** 多ければ生成が id を作っている
- 米財務省 DTS の札が1日2件以内に収まっているか。定例の給付が混ざっていないか
- 日銀の当座預金の札が出ているか（PDF が読めているか）
- 概念が10件に届くまでの日数。届いたあと、固有名詞の概念が混ざっていないか
- プロンプトの字数（実測。見込み約1万字）

---

## 11. 決めていないこと（実装の前に確認したい）

1. **同じ JSON の中の札を `"#2"` で指せるようにする**（2.3）。1回の取り込みの中で「制裁 → 原油 → 輸入額」が
   全部新しい札のとき、これが無いと鎖が作れない。入れてよいか
2. **問いモードの機能は残す**（データだけ捨てる）でよいか。7.2 では残す前提で書いている
3. 段階4で、**日次のお金の源2件を先に入れる**（本来は段階5）。初回の実行で PDF が読めるかを確かめるため。これでよいか

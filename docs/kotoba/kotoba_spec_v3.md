# kotoba v3.0 仕様書 — 散歩アプリとの統合・朝の気分→言葉・相談の記録

## 目的
朝の習慣を一本の流れにする：起床時の体調を押す → 一言書く → 自分のノートから合う言葉を受け取る → 散歩に出る → 散歩後の記録 → X に投稿。
walk10000（散歩記録）の機能を kotoba に取り込み、一つのアプリにする。

## 前提
- ベースは C:\kotoba\index.html（v2.1）。ここに C:\sanpo\ の walk10000 の機能を移植する
- リポジトリと URL は当面 kotoba のまま（yutaTasaki/kotoba、https://yutatasaki.github.io/kotoba/）。アプリ名の変更は別途
- walk10000 のアプリとデータはそのまま残す（移行期間中は両方動く）。散歩データの構造は変えない
- 散歩の記録・投稿画像・振り返り・スタンプカードの表示ロジックは walk10000 のコードを流用し、見た目も基本そのまま持ってくる

## データ
- 言葉のノート：yutaTasaki/kotoba-data の data.json（従来どおり）
- 散歩の記録：yutaTasaki/walk10000-data の data.json（walk10000 と同じ構造で読み書き。項目の追加・削除・改名はしない）
- 朝の記録・相談の記録（新規）：kotoba-data の data.json に `journal` を追加
```json
"journal": [
  {
    "id": "j_20260908_001",
    "date": "2026-09-08",
    "kind": "morning",          // morning（朝）または consult（相談）
    "mood": 3,                  // 朝のみ。1〜5。散歩データの起床時体調と同じ値を書く
    "text": "一言（空でも可）",
    "picks": [{"id": "n_...", "why": "..."}],
    "closing": "...",
    "createdAt": "..."
  }
]
```
- journal もノートと同じくノート単位（エントリ単位）でマージ、削除は tombstone
- GitHub トークンは1本。kotoba-data と walk10000-data の両方に Contents: Read and write を持つものを新たに作り、設定画面で入力する

## 朝の画面（ホーム）
その日の状態によって表示が変わる。

### まだ起床時体調を押していない
1. 上に日数（「N日目 / 10000」）を小さく。walk10000 と同じ計算
2. 「今朝の体調」：顔アイコン5つ（1紫・2赤・3黄・4水色・5緑。walk10000 と同じ基準・同じ色）
3. 押すと散歩データに起床時体調と時刻を保存し、次の段が開く

### 体調を押した後、言葉をまだ受け取っていない
4. 「今朝はどんな感じ？」の一行入力（任意）と「言葉を受け取る」ボタン、「今日はいい」リンク
5. 「言葉を受け取る」：相談と同じ API 呼び出し。入力が空のときは体調の値だけを手がかりにする
   （「今朝の体調は5段階で N。それに合う言葉を選ぶ」）。返すのは最大2枚＋一言
6. 結果を journal に kind: morning で保存し、カードとして表示。カードをタップで詳細へ
7. 「今日はいい」を押したらランダム1枚を表示（journal には残さない）

### 言葉を受け取った後、まだ歩いていない
8. 上に今朝のカードを残したまま、下に「今日歩いた」ボタン（walk10000 と同じ処理：記録日、押した時刻、連続日数）

### 歩いた後
9. 散歩後の体調（顔5つ）、歩数、距離の入力（walk10000 と同じ項目・保存先）
10. 「Xで投稿」（walk10000 の投稿画像生成＋共有シートをそのまま）
11. 投稿後は今日のまとめ（体調2つ、今朝の言葉、日数）を表示して終わり

### 翌日以降
- 日付が変わったら 1 に戻る。走り書きの「昨日の分を記録」ボタン（walk10000 と同じ）は設定画面ではなくホームの下に小さく置く

### 補足
- API キー未設定なら 4〜7 を飛ばしてランダム1枚を表示
- 顔を押し間違えたら、その日のうちなら押し直しで上書きできる（journal の mood も更新）

## 相談画面
- v2.1 のまま。ただし返答を journal に kind: consult で保存する
- 各返答の右上に「消す」。消すと journal から削除（tombstone）
- 画面を開いたとき、今日の相談があれば下に表示される（過去分は「記録」画面で見る）

## 記録画面（新規タブ）
- 上に切替：「朝の言葉」「相談」「散歩」
- 朝の言葉：日付順に、体調の顔・一言・受け取ったカード（縮小表示）。タップで詳細へ
- 相談：日付順に、入力と返答。長いものは折りたたみ
- 散歩：walk10000 の「振り返り」（7日／30日集計、スタンプカード）をそのまま
- どのエントリも「消す」で削除できる

## ノート詳細への追加
- 「届いた回数」：journal の picks にそのノートが出た回数と直近の日付を1行で表示。0 なら非表示

## 下部タブ
「朝」「言葉」「相談」「記録」「設定」の5つ。「言葉」は v2.1 の一覧。追加ボタンは「言葉」画面の右上と「朝」画面の右上に置く

## 設定画面（統合）
1画面を区切り線で区切る。順序：
1. **同期**：GitHub トークン（1本、両リポジトリ）、同期状態、最終同期時刻、「今すぐ同期」
2. **Claude API**：API キー、モデル名、自動関連づけ ON/OFF、朝の言葉 ON/OFF
3. **散歩**：開始日、最長連続の手入力（復元用）、投稿画像のプレビュー（walk10000 の設定項目をそのまま）
4. **言葉**：候補ノートの圧縮しきい値、「全ノートを一括で関連づけ」と進捗、エラーが出たノート
5. **データ**：「言葉のデータをエクスポート」「散歩のデータをエクスポート」「インポート」「リモートから読み込み直す（両方）」
6. **統計**：ノート数、タグ数、リンク数、散歩日数、連続、API 呼び出し回数、最終エラー
- walk10000 の設定にあって上に書いていない項目があれば、3 に入れる

## 移行
- 初回起動時、新しいトークンが入って walk10000-data を読めたら、その日数・連続がそのまま出る。データ移行作業はしない
- 旧 walk10000 は削除しない。1〜2週間併用して問題なければ、ユーザーがホーム画面のアイコンを外す

## 含めないもの
- X 投稿の言葉版（散歩画像に言葉を載せる）、一括入力、網の可視化、アプリ名変更

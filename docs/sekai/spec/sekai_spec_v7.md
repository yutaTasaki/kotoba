# 一万日 v16 仕様書 — 世界：地層を専用プロンプトで埋める（B）

指示書の A・B・C のうち **B**。順番は **B → C → A** で確定。
既存の言葉・引く・相談・問い・撮る・筋トレ・散歩には触らない。**API は使わない**。

---

## なぜ B が先か

A（導線の組み直し）は一番大きく、B と C の成果物を置き場所ごと並べ替える作業になる。
A を先にすると、B と C のたびに1画面の構成を触ることになる。**並べ替えは最後に1回**。

そして A の1画面には**地層の1行**が乗る。**赤だらけの地層行を並べても導線にならない**ので、
先に赤を消せる手段を持っておく。

---

## 0. 問題

v15 で「札に残高が書かれていれば地層に入る」ようになった。だが**週のニュースの札に出てこない地層**が残る。

| 札から埋まりうる | 札にまず出てこない |
|---|---|
| 外貨準備、日銀の資産、軍事費、中銀の金保有、債務/GDP | 長期金利（日米）、生産年齢人口、中国の人口、実質実効為替レート、エネルギー自給率、世界貿易/GDP |

13件のうち、札から埋まるのはせいぜい5件。残り8件は「値は未記入」のまま。

**地層専用のプロンプト**を作る。月に一度、または赤が残っているときに回す。

---

## 1. 「古い」の判定 — 一律の日数をやめる

長期金利は日次、外貨準備は月次、SIPRI の軍事費と国連の人口は年次。
**90日を一律にすると軍事費が1年中赤くなる**。赤が常時点いていると、赤は情報でなくなる。

地層に `cadence` を手で振る（v16 の地域と同じやり方）。

```js
const WORLD_CADENCE_DAYS = { month: 45, quarter: 120, year: 400 };
```

| cadence | 赤 | 地層 |
|---|---|---|
| `month` | 45日 | 日本の長期金利、米国の長期金利、日銀の資産規模、日本の外貨準備残高、円の実質実効為替レート |
| `quarter` | 120日 | 日本の政府債務/GDP、米国の政府債務/GDP、世界貿易/GDP |
| `year` | 400日 | 日本の生産年齢人口、中国の人口、日本のエネルギー自給率、世界の軍事費、各国中銀の金保有 |

```js
// 値が無いものは常に古い（未記入は最も古い状態）
function worldStratumStale(s) {
  const lv = worldStratumLevel(s);
  if (!lv) return true;
  const d = worldDaysSince(lv.at);
  return d !== null && d > (WORLD_CADENCE_DAYS[s.cadence] || WORLD_CADENCE_DAYS.quarter);
}
```

いま `paintStratumLine` に直書きされている `days > 120` を、この1本に置き換える。
地層の一覧の赤（`.slVal.empty`）も、未記入だけでなく**古いもの**に広げる。

---

## 2. 専用プロンプト

### 2.1 どこから

**地層の一覧（`worldStratumAll`）の見出しに置く。**
`[現在値を調べる]` を押すとコピー、下の欄に JSON を貼る。取り込みと同じ形。

地層の一覧は世界タブの右上「地層」→「地層の一覧」で開く既存の画面。
A のタブ再編でこの画面は動かないので、いま置いても作り直しにならない。

見出しに `古い N件 / 13件` を出す。**N が 0 なら押す必要がない**ことが押す前に分かる。

### 2.2 質問の精度

本人の言葉：

> Claude に質問するプロンプトをコピーする形式だから、質問は精度の高いものにしてほしい。

**値・基準日・出典URL の3点が揃わなければ返せない形**にする。

- 13件を**1本**で回す（分野で分けない）。月1回の作業を3回に割ると回らなくなる。
  返答が途中で切れても、**配列に入っているものだけ入る**ので、足りなければもう一度回せば済む
- 13件の `name`・`line`・`unit`・`where.source`・`where.url` を差し込む。
  **確認先が既にあるので、生成に探させるのは値だけ**
- **推測・概算を書かせない。** 見つからなければその地層は返さない（0件でよい）
- **url が無い値は取り込まない**（3.3）

### 2.3 プロンプト全文

```
（SHOOT_PROMPT_HEAD）
あなたは、決まった13個の「量」の現在値を調べて返す係です。
解説を書きません。増やしません。減らしません。下に並ぶ13個だけを見ます。

■ 必ず守ること
- web 検索を必ず使う。検索せずに書かない。
- それぞれの「確認先」に挙げた一次ソースで確認できた値だけを返す。
  確認先で見つからなければ、そのソースが引用している一次の統計まで辿ってよい。
  ニュース記事・まとめサイト・百科事典を出典にしない。
- 値・基準日・出典URL の3つが揃わないものは返さない。
  2つしか揃わないくらいなら、その量は返さないほうがよい。
- 推測・概算・「およそ」「約」を書かない。四捨五入された公表値はそのままでよい。
- URL は実在するページのものだけ。開けるか確かめる。
  確かめられなければ、その量を返さない。
- 13個すべてを埋めなくてよい。7個しか確認できなければ7個を返す。
- JSON のみ。ほかの文章を返さない。

■ それぞれに書くこと
- stratumId：下の一覧にある id だけ。一覧に無いものを作らない。
- value：出典の書き方のまま。単位は下の「単位」に合わせる。
  換算しない。「1.85」と書いてあれば「1.85」。
- at：その値の基準日（YYYY-MM-DD）。
  「2026年8月末時点」なら 2026-08-31。「2026年第2四半期」なら 2026-06-30。
  基準日が書かれていなければ、その量を返さない（公表日で代用しない）。
- source：媒体名と統計名。（例：財務省 外貨準備等の状況 2026-08-31）
- url：その値が載っているページ。トップページではなく、数字のあるページ。

■ 出力形式
{"levels":[
  {"stratumId":"st_jp_fx_reserve","value":"12075","at":"2026-08-31",
   "source":"財務省 外貨準備等の状況 2026年8月末","url":"https://..."},
  {"stratumId":"st_jp_rate_long","value":"1.85","at":"2026-09-09",
   "source":"財務省 国債金利情報","url":"https://..."}
]}

確認できなかった量は、行ごと書かない（null や空文字で埋めない）。

────────────────────────────────────────
■ 調べる量（13件）
st_jp_rate_long      日本の長期金利       単位 %
  1990年代から下がり続け、2022年以降上がり始めた
  確認先：財務省 国債金利情報  https://www.mof.go.jp/jgbs/reference/interest_rate/
  （前回の値：1.85%  2026-06-30 ・ 72日前）
  ...（13件）
```

**「前回の値」を差し込む**のは、桁を1つ間違えた返答をこちらで気づけるようにするため
（前回 1.85% に対して 185 が返ってきたら分かる）。**生成に前回値を使わせるためではない**ので、
「前回の値は参考。確認できなければ返さない。前回値をそのまま書き写さない」を規則に入れる。

### 2.4 プロンプトの大きさ

13件 ×（id・名前・単位・line・確認先2行・前回値）で約1400字。規則と出力形式で約900字。
**合計およそ2300字**。既存の生成プロンプト（約6000字）より小さい。

---

## 3. 取り込み

### 3.1 `via:'probe'` を分ける

v15 の `levels` の仕組みに乗せるが、**`via` は分ける**。出典の出所が違うため。

| via | url の出どころ | 検証 |
|---|---|---|
| `card` | 札の url | **取り込み時点で検証済み** |
| `probe` | 生成が新しく書く | **未検証** |
| `hand` | 手入力 | — |

混ぜると「どの url が検証済みか」が消える。分けておけば、あとで
「札から N件／調べて M件」も出せる。

### 3.2 上書きの規則（v15 のまま）

- **同じ日付が既にあれば落とす**（`card` と `probe` の両方）
- **上書きしてよいのは `hand` だけ**（打ち間違いを直せないと困る）

`probe` を上書き可にしない理由：月1回回すたびに同じ基準日の値が来る。
上書き可にすると、**間違った値で正しい値を上塗りする経路**ができる。落とすほうが安全で、
落ちたことは取り込み結果に出る。

### 3.3 落とすもの

| 条件 | 扱い |
|---|---|
| `url` が無い・http(s) でない | **落とす**（probe だけの縛り。card は札の url を継ぐので常にある） |
| `at` が日付でない | 落とす（**札と違い、eventDate で代用しない**。基準日の無い残高は意味が違う） |
| 一覧に無い `stratumId` | 落とす |
| 同じ日付が既にある | 落とす |
| `value` が空 | 落とす |

取り込み結果：

```
地層の値 7件を入れました。
※ 出典URLが無い 1件、基準日が無い 1件、同じ日付で重なった 2件を落としました。
古いまま残っている地層： 米国の政府債務/GDP ・ 世界の軍事費
```

**最後の1行が要**。7件入っても、赤が残っていることが同じ画面で分かる。

### 3.4 桁の見張り

`probe` の値が**前回値の100倍以上または1/100以下**なら、入れたうえで**警告を出す**。

```
※ 日本の長期金利 が前回の 100倍以上です（1.85 → 185）。地層を開いて確かめてください。
```

落とさないのは、本当に桁が変わることがあるため（単位が変わった、統計が改定された）。
**判断は人がする。機械は気づかせるだけ。**

---

## 4. 定数

```js
const WORLD_CADENCE_DAYS   = { month: 45, quarter: 120, year: 400 };
const WORLD_CADENCES       = ['month', 'quarter', 'year'];
const WORLD_PROBE_JUMP     = 100;   // 前回値の何倍で桁の警告を出すか
```

---

## 5. 守ること

- **url が無い値を取り込まない。** 捏造の入り口を作らない
- **基準日が無い値を取り込まない。** 札と違い eventDate で代用しない
- **推測・概算を書かせない。** 見つからなければ返さない
- **同じ日付は落とす。** 上書きしてよいのは手入力だけ
- **一覧に無い地層を作らせない**（13件は閉じたリスト）
- 換算しない（`value` は出典の書き方のまま、単位は地層の `unit`）
- 赤の基準は地層ごと（一律にすると赤が情報でなくなる）
- 既存のデータは消さない。朝の流れは変えない

---

## 6. 実装後に見る数字

- **13件のうち何件が赤でなくなったか。** これが B の成否
- 落とした内訳（url 無し／基準日無し／重複）。url 無しが多いなら、プロンプトの縛りが効いていない
- 桁の警告が出た回数。0でないなら、`unit` の指定が弱い

---

# 付録：このあとの C と A（確定した判断の控え）

実装はしない。忘れないための控え。

## C（予測を AI にも書かせ、答え合わせを自動にする）

- `lean`（up/down/flat）・`because`（60字・推定の印）は **`prediction` 側**に置く。
  `fork` は問い、`lean` は答えで別のもの
- **`result` を2つに割る**：`leanResult`（生成の当たり外れ）と `result`（自分の）。
  同じ分かれ目で両方の的中率を比べるため
- `worldPredictionWritten()` を「生成が書いた」「自分が書いた」の2つの述語に割る
- **fork のある札だけに lean を書かせる。加えて `WORLD_FORK_PER_BATCH = 3`**
  ——1回の取り込みで fork を書くのは、生成が重要だと並べた**上位3件まで**。
  実データで fork は約半分の札に付いており、絞らないと90日後に300件が待つ。
  **分かれ目が立つ札は、その週で数件しかないのが正常**
- 期限は90日で固定（生成に選ばせない。fork の規則が「おおむね3ヶ月で決着」なので）
- 答え合わせのプロンプトは**12件/回・期限の古い順**。残りは「あと N件」
- **url が無い結果は「保留」**にする。判定を捏造させない
- 保留は `holdUntil = judgedAt + 14日` で戻し、**2回 hold で打ち切り**
- 段階3（来なかったもの・枠組み・月に一度自分の言葉）は**後**

## A（世界タブを1本の導線に組み直す）

```
毎朝「取り込む」→ 今日動いた主体 → 主体を開く
                              → お金 → 行動 → 述べた理由 → 読んだ思惑
```

- **`WORLD_PER_DAY = 3` を外す。** その日に来たものはその日に見える。
  古い未読の警告だけ残す。取り込みが週1にずれた週に N が大きくなるのは許容
- **`readDate` は残す**（朝の畳んだ行・記録の月まとめ・`worldReadOn` が依存）。
  変えるのは単位だけ——今日の札を**主体でまとめて**出す
- 「主語がある札＝盤面」「主語が無い札＝地層に吸収」。ただし
  **「読み物にするか」と「levels を吸うか」は別の判断**（介入の公表は行動の札で、両方に出る）
- **「背景に沈めた札 N件」の1行**を置く。主語も数字も無い札が画面から消えるのを防ぐ。
  0件の日は出さない
- タブを消し、**一時的な札の一覧画面（`view:'cards'`）**を1つ作る。
  入口は「入れた札を見る」「つながる札」「問いの札」「探す」「お金」「地層」「主体」——
  **入口が必ず何かの結果なので、在庫にならない**
- **畳むのではなく件数を絞る**（地層一覧の失敗を繰り返さない）
- 主体の詳細は**お金を一番上**に。その下に行動・述べた・推定
- 予測タブを消し「答え合わせ N件」だけ（期限が来た日のみ）。予測の一覧は答え合わせ画面の中
- 月1以下のボタン（半年の動きを読む・名前を直す・非表示・親でまとめる）は設定か長押しへ

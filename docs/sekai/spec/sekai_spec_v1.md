# 一万日 v10 仕様書 — 世界（金融・地政学を体系として持つ）

既存の index.html に追加する。記録タブの子画面として「世界」を置き、朝タブの流れの最後に1ステップを足す。
kotoba の言葉（notes）・引く・相談・問い・網には一切混ぜない。

## 目的

金融・地政学・政治・国際情勢を、断片ではなく体系として理解できるようになる。

1. 毎日ごく少数（3件）の、これから効き続ける動きを受け取る
2. それを理解するのに要る前提を、**すでに知っていることは省いて差分だけ**受け取る
3. 予測を残し、後で答え合わせする

ニュースアプリの劣化版にしない。**量ではなく消化。** 3件揃わない週があってよい。埋められるより良い。

---

## 1. 置き場所と入口

### 1.1 記録タブの子画面

`screen-world` を足し、`showScreen()` の対応表に `world: 'navRecords'` を追加する（training / meditation / shoot と同じ扱い）。

記録タブの右上ボタンが4つになるので、**topbar は「← 戻る／記録」だけにして、入口のボタン行は画面本体の先頭に移す**（確定）。

```
← 戻る        記録

[ 撮る ] [ 瞑想 ] [ 筋トレ ] [ 世界 ]
─────────────────────────────────
振り返る  1年前  1か月前  1週間前  どこかの日
```

既存3画面（撮る・瞑想・筋トレ）の入口位置も変わる。390px の topbar に4つ以上は構造的に入らないので、増えるたびに崩れる形を先に直しておく。

### 1.2 朝タブの1ステップ

`HOME_STEP_IDS` に `worldCollapsedRow` / `worldStage` を足す。既存ステップと同じく**既定は畳んだ1行**。

- 畳んだ行：`世界 ・ 今日の3件`（未読キューが空なら `世界 ・ 新しい分がありません`）
- 期限が来た予測がある日は：`世界 ・ 今日の3件 ・ 答え合わせ2件`（段階2）
- **開かなければ何も起きない。** 開いた瞬間に初めてその日の3件が確定する（後述 3.3）
- **位置：流れの一番最後（X投稿の後ろ）。** 「今日歩いた → 体調・歩数 → 投稿」は1つの連続した作業なので途中に別の話を挟まない。世界を読むのは別の行為で、手が空いたところに置くほうが読むのが選択になる。世界を飛ばしても投稿は止まらない
- 投稿ステージ（`walkShareStage`）は畳めないので、世界の行はその下に出る。散歩がまだの日も、朝の流れの最後に必ず1行出す（世界だけ読む日があってよい）

---

## 2. データ

### 2.1 ファイル

| 置き場 | 端末（kv） | リポジトリ（kotoba-data） |
|---|---|---|
| 出来事 | `world:YYYY-MM`（eventDate の月ごと） | `world/YYYY-MM.json` |
| 索引 | `worldIndex` | `world/index.json` |
| 概念ノート | `worldConcepts` | `world/concepts.json` |
| ソース設定 | `worldSources` | `world/sources.json` |
| 未同期の月 | `worldDirty` | — |
| 最後に見た索引 | `worldStamps` | — |

journal には一切入れない（月ファイル・`noteStats` 索引・記録カレンダーの日詳細すべてに影響が出るため）。
書き方は瞑想（v8.0）の年ファイル＋索引に寄せる。`mergeMedSets` と同じ id ベースのマージ＋墓標。

**端末に全件を持たない。** 出来事は次の月を常時ロードし、それ以外は必要になったとき（一覧・答え合わせ・概念詳細）に読む。

1. 今月・先月
2. **索引の `months[m].unread > 0` の月**

2 が要る理由：月ファイルは `eventDate` 基準なので、8/30 の出来事は 8月のファイルに入る。「今月＋先月」だけだと、それを読まないまま10月になった時点でキューから消える（ファイルには残っているのに画面から見えない＝静かな消失）。未読数は索引に持っているので追加のファイル読みは要らず、未読は14日で「古い」印が付くので、実際に増えるのは多くて1〜2ファイル。

概念ノートとソース設定は全件を常に持つ（プロンプトに既知一覧を差し込むため）。

### 2.2 出来事 1 件

```
{ id: 'w_YYYYMMDD_連番',      // YYYYMMDD は eventDate。ここから月ファイルが引ける
  eventDate: 'YYYY-MM-DD',    // 実際に動いた日。AI が出典ページから書く
  readDate: '',               // 出した日。空 = 未読キュー
  field: 'finance' | 'geo',   // どちらのプロンプトから来たか

  fact: '',                   // 何が起きたか。1文・40字以内
  why: '',                    // なぜ今動いたか。1〜2文
  premise: {                  // 理解に要る概念 1つ
    kind: 'new' | 'relation',
    name: '',                 // kind:'new' のとき。概念名
    aka: [],                  // 別名・日本語訳・英語名（表記ゆれ統合の本命）
    a: '', b: '',             // kind:'relation' のとき。既知の概念2つ
    line: ''                  // 概念（または関係）の説明 1文
  },

  detail: '',                 // 数字・背景。1〜3文。推定値には「推定」と明記
  source: '',                 // 媒体名・発表日
  url: '',                    // 空の出来事は取り込まない
  concepts: [],               // この出来事が触れている概念名（既知・新規とも）
  related: [],                // premise の概念とつながりの深い概念名

  prediction: {               // 段階1で最終形。段階2は書き込む画面を足すだけ
    text: '',                 // 今の文
    days: 90,                 // 30 | 90 | 180
    writtenDate: '',          // 最初に書いた日。期限はここから引く
    dueDate: '',              // writtenDate + days
    setAt: '',                // 今の文が置かれた時刻
    history: [{ text, at }],  // 前の版。撮るの answers と同じ扱い
    result: '',               // '' | 'hit' | 'miss' | 'hold'
    judgedAt: '',
    judgeNote: ''
  },

  batchId: '',                // どの取り込みで来たか
  order: 0,                   // その batch 内での並び（AI が重要だと思った順）
  createdAt, updatedAt }      // createdAt = 取り込んだ時刻（古びの判定に使う）
```

### 2.3 月ファイルと索引

```
world/YYYY-MM.json
{ version: 1, month: 'YYYY-MM', updatedAt, events: [...], deletedIds: [{id, deletedAt}] }

world/index.json
{ version: 1, updatedAt,
  months: { 'YYYY-MM': { updatedAt, count, unread } },
  due: { 'YYYY-MM-DD': ['w_20260902_1', ...] },   // 未判定の予測だけ。判定したら消す
  concepts: { updatedAt, count },                  // 変更検知用
  sources:  { updatedAt, count } }
```

`due` は全月を走査せずに期限の来た予測を拾うためだけにある。id から月が引けるので、そこから該当月だけを読む。
索引が壊れたときのために、設定に **「予測の索引を作り直す」**（全月を読んで `due` を組み直す）を置く。

### 2.4 概念ノート 1 件

```
{ id: 'wc_YYYYMMDD_連番',
  name: '',
  aka: [],                    // 別名・訳語・英語名。統合で増える
  line: '',                   // 1文の説明（最初に出たときのもの）
  field: 'finance' | 'geo' | '',
  eventIds: [],               // この概念が出た出来事
  related: [],                // つながる概念の id
  firstEventId: '',
  createdAt, updatedAt,
  lastUsedAt: '' }            // プロンプトに差し込む400件を切るときの順序
```

`concepts.json` は `{ version: 1, updatedAt, concepts: [...], dismissedPairs: [...] }`。

`premise.kind: 'relation'` のときは**新しい概念を作らない**。既存の概念 A と B の `related` を互いに足し、`line`（関係の説明）は出来事側にだけ持つ。両概念の詳細画面には「この2つの関係に触れた出来事」として出る。

### 2.5 ソース設定

```
world/sources.json
{ version: 1, updatedAt,
  sources: [{ id, name, url,
              field: 'finance' | 'geo' | 'both',
              role:  'primary' | 'analysis',
              note: '' }] }
```

設定画面に「7. 世界」を足す。1行＝名前・URL・分野の切替・役割の切替・削除。下に追加フォーム。
分野タグはプロンプトA／Bの振り分けに使う（`both` は両方に入る）。

### `role` — 事実の出所と、解釈の補助を分ける

- **primary**（中銀・政府・国際機関・統計）＝ 出来事・日付・数字の出所。**`fact` / `eventDate` / `detail` の数字は primary でしか確認できない**
- **analysis**（シンクタンク）＝ `why`（なぜ今動いたか）を書くための補助。**analysis だけを根拠に `fact` を立ててはいけない**

これが無いと、シンクタンクの論考が「出来事」の顔をして体系に入る。捏造防止と同じ理由で、プロンプトの中でも2つの一覧を分けて差し込み、それぞれの使い道を明記する。

### 初期リスト（22件）

URL はドメイン（トップページ）で登録する。深い階層は変わると死ぬので持たない。**apex では HTTPS が応答しないものがあり、`www.` を付けてある**（実測で確認済み）。

**分野A（金融・経済）**

| 名前 | URL | role |
|---|---|---|
| 日本銀行 | https://www.boj.or.jp/ | primary |
| FRB（米連邦準備制度） | https://www.federalreserve.gov/ | primary |
| ECB（欧州中央銀行） | https://www.ecb.europa.eu/ | primary |
| BIS（国際決済銀行） | https://www.bis.org/ | primary |
| IMF | https://www.imf.org/ | primary |
| 財務省 | https://www.mof.go.jp/ | primary |
| 内閣府 経済社会総合研究所 | https://www.esri.cao.go.jp/ | primary |
| 総務省統計局 | https://www.stat.go.jp/ | primary |
| IEA（国際エネルギー機関） | https://www.iea.org/ | primary |
| EIA（米エネルギー情報局） | https://www.eia.gov/ | primary |
| Bruegel | https://www.bruegel.org/ | analysis |
| PIIE（ピーターソン国際経済研究所） | https://www.piie.com/ | analysis |

**分野B（地政学・政治・国際情勢）**

| 名前 | URL | role |
|---|---|---|
| 外務省 | https://www.mofa.go.jp/ | primary |
| NATO | https://www.nato.int/ | primary |
| 国連 | https://www.un.org/ | primary |
| WTO | https://www.wto.org/ | primary |
| SIPRI（ストックホルム国際平和研究所） | https://www.sipri.org/ | primary |
| CSIS | https://www.csis.org/ | analysis |
| Carnegie Endowment | https://carnegieendowment.org/ | analysis |
| Chatham House | https://www.chathamhouse.org/ | analysis |
| IISS | https://www.iiss.org/ | analysis |
| アジア経済研究所（IDE-JETRO） | https://www.ide.go.jp/ | analysis |

初期リストは、ソース設定が一度も保存されていないときにだけ入れる（`world/sources.json` が無いときの初期値）。一度でも編集したら二度と入れ直さない。

**注意**：`www.mofa.go.jp` と `www.imf.org` は自動化クライアントに 403 を返す（WAF によるボット遮断。ドメイン自体は生きている）。実ブラウザでは開ける。この2つだけ何も出てこない週が続くようなら疑う。

### 2.6 同期

`syncNow()` の最後に `worldSyncNow(token)` を足す。

- 出来事の月ファイル：`worldDirty` に入っている月だけ push
- 概念・ソース：**署名比較で、変わったときだけ書く**（瞑想の `medSignature` と同じ）。毎回の同期で GitHub 呼び出しが2つ増えるのを避ける
- 索引：月ファイルか概念かソースを書いたときだけ

エラーは `WORLD_LAST_SYNC_ERROR_KEY` に入れ、既存の syncBar に出す。

---

## 3. 取り込み

### 3.1 画面

世界タブの右上「取り込む」→ 取り込み画面。

```
プロンプトをコピー
  [ 金融・経済（A） ]   [ 地政学・政治（B） ]

チャットが返した JSON をここに貼る
  [                                    ]
  [ 取り込む ]

  → 12件を入れました。
    重複で3件、URL が無くて2件を落としました。
    新しい概念 5件。近い名前が 2件あります → [ 見る ]
```

同じ画面に A と B を続けて貼れる（追記なので何回貼ってもよい）。

### 3.2 取り込みの規則

**追記のみ。置き換えはしない。** 読んだ出来事も予測も、取り込みで消えることは構造的に起こらない。

1. `shootParseJson()` をそのまま使う（```json のフェンス剥がし、最初の `{` から最後の `}` の切り出し）
2. 1件ずつ検査する。落とす条件：
   - `fact` が空
   - `url` が http/https でない（`shootSafeUrl()` を流用）
   - `eventDate` が `YYYY-MM-DD` として妥当でない
   - **`eventDate` が今日より後**（未来日付を通すと、その出来事は永久にキューの先頭に居座るか、存在しない月ファイルを作る）
   - 既存と重複（次項）
3. 字数は取り込み時に切る：fact 40 / why 200 / line 120 / detail 600 / source 120
4. `eventDate` の月をすべて先に読み込んでから（`ensureWorldMonths`）id を振る。同じ日に複数入っても連番が衝突しない
5. **落とした件数を理由ごとに必ず画面に出す**（`URL なし 2件 ／ 日付が不正 1件 ／ 重複 3件 を落としました`）
6. `eventDate` が90日より古いものは**弾かない**（過去の出来事に新しい動きが出ることは普通にある）。件数だけ知らせて、判断は人がする

**重複判定**（API 不使用）：同じ `eventDate` ±3日 の既存出来事のうち、
`textSimilarity(fact, 既存fact) >= WORLD_DUP_SIM(0.70)` または `url` が完全一致 なら重複。
`textSimilarity` は既存の文字バイグラム＋Dice をそのまま使う。

窓を ±3日 にしてあるのは、同じ発表を片方が発表日・片方が発生日で書くと1〜2日ずれるため。
**その結果、判定は1つの月ファイルでは閉じない。** 8/30 の出来事は 9/1・9/2 の既存分とも突き合わせる必要がある。
なので走査範囲は **その出来事の月＋、月初/月末から3日以内なら隣接月**。`ensureWorldMonths` に渡す月に隣接月を足すだけで、走査量はほぼ変わらない。

### 3.3 「今日の3件」の確定

- 未読キュー = `readDate === ''` の出来事。並び順は `createdAt`（取り込み順）→ 同じ batch 内は `order`（AI が重要だと思った順）
- 朝のステップを開いた瞬間、または世界タブで「今日の3件」を開いた瞬間に、先頭から3件に `readDate = 今日` が入る
- 同じ日に開き直しても同じ3件（`readDate === 今日` のものを先に拾う）
- **分野が偏らないようにする**：3件目までに finance と geo が1件以上ずつ入るよう、不足分野の先頭を1件だけ繰り上げる。片方の分野しか無ければそのまま
- 開かなければ何も消費されない。読まない日があってよい

### 3.4 古び

未読のまま `createdAt` から `WORLD_STALE_DAYS`（既定14日）を過ぎたものは「古い」。

- 世界タブの上部に `古い未読が 8 件あります [ まとめて捨てる ]` を出す
- **自動では消さない。** 捨てるのは墓標を立てて削除（`deletedIds`）
- 未読が `WORLD_QUEUE_WARN`（30件）を超えたら、取り込み画面に「読む速さより入れる速さが上回っています」と出す

---

## 4. 画面

### 4.1 今日の3件（朝タブのステップ／世界タブの先頭）

**一覧は fact の1行だけ。** 3件で3行。

```
9/2  中銀が国債買い入れの減額幅を縮めた            [ イールドカーブ ]
9/1  紅海航路の保険料が3か月ぶりに上がった          [ シーレーン ]
8/29 ある国が穀物輸出の枠を来年まで延ばした         [ 食料安全保障 × 外貨 ]
```

- 左は `eventDate`（`M/D`）。今日の話とは限らないことを最初から見せる
- 右端の概念タグは1つだけ。**初出は色つき（--accent）、既知は灰色（--text-faint）**
- `premise.kind: 'relation'` のときは `A × B` と出す（両方とも既知なので灰色）
- タップで**その行だけ**開く（他は閉じる）

開いた形：

```
9/2  中銀が国債買い入れの減額幅を縮めた
     ─────────────────────────────
     長期金利の上がり方が財政の利払いに直接効く域に
     入り、財政側から減額の速度に注文が付いた。

     イールドカーブ ── 満期の違う国債の利回りを並べた線。
     短期より長期が低くなると景気後退の予兆とされる。

     [ 詳しく ]   [ 予測を書く ]   [ Claude に聞く ]
```

`詳しく` でさらに `detail` ／ `source` ／ `url`（リンク）が出る。**既定では閉じている。**

### 4.2 予測を書く

```
3ヶ月後どうなっていると思うか（書かなくてよい）
[                                        ]
期限   [ 1ヶ月 ] [ 3ヶ月 ] [ 6ヶ月 ]      ← 既定 3ヶ月
```

- 最初に書いた日が `writtenDate`、`dueDate = writtenDate + days`。索引の `due` に載る
- 期限は後から変えられる（`dueDate` を引き直し、索引も直す）。**起点は最初に書いた日のままで動かない**
- 書き直すと `history` に前の版が積まれる。**今の文が置かれてから1分以内の書き直しは行を作らない**（`setAt` で見る。撮るの `answers` と同じ扱い）
- 空のまま閉じてよい。何も起きない

### 4.3 世界タブ

```
← 戻る     世界     [ 取り込む ]

古い未読が 8 件あります              [ まとめて捨てる ]

今日の3件
  （4.1 の3行）

[ 出来事 ]  [ 概念 24 ]  [ 予測 6 ]
```

- **出来事**：月ごとに新しい順。1行（`M/D` ＋ fact ＋ 概念タグ）。タップで 4.1 と同じ展開。端末に読み込んでいる月だけが出る（その旨を末尾に1行出す）
- **概念**：出た回数の多い順。1行（名前 ＋ 出た回数）
- **予測**：`待ち` / `済み` の2つ。待ちは期限の近い順。済みは当たり・外れ・保留の比率を上に1行（段階2）
- 今日の分をまだ出していないときは `今日の3件をひらく` を出す。**押すまで1件も消費しない**（朝のステップを開くのと同じ扱い）

### 4.4 概念の詳細

```
イールドカーブ
別名：利回り曲線 / yield curve

満期の違う国債の利回りを並べた線。短期より長期が低くなる
と景気後退の予兆とされる。

つながる概念
  財政ファイナンス ・ 中銀の独立性 ・ 実質金利

この概念が出た出来事（6件）
  9/2  中銀が国債買い入れの減額幅を縮めた
  8/14 ...

[ Claude に聞く ]   [ 名前を直す・まとめる ]
```

### 4.5 表記ゆれの統合（段階1に入れる）

**取り込みのたびに走る。** 新しい概念名を、既存の概念名＋`aka` すべてと突き合わせる。

1. **正規化して完全一致 → 自動で同一にする。** 前後空白・中黒（`・`）・長音（`ー`）・全角半角・英字の大小を落として比較
2. **正規化後の `textSimilarity` >= `WORLD_CONCEPT_SIM`（0.60）→ 候補**
3. **片方がもう片方を含む → 候補**（「イールドカーブ」と「イールドカーブの逆転」）

候補は取り込み結果から `[ 見る ]` で開き、1行1組で出す。

```
イールド・カーブ        ↔  イールドカーブ（既存 6件）
     [ 同じ概念にする ]   [ 別のもの ]
```

`同じ概念にする` を押すと、新しい方の名前が既存の `aka` に入り、出来事の `concepts` の綴りも既存名に寄せる。`別のもの` を押した組は覚えておき、二度と聞かない（`dismissedDuplicatePairs` と同じ形で `concepts.json` の `dismissedPairs` に持つ）。

**この仕組みの限界をはっきりさせておく。**
バイグラムは文字の重なりを見るので、「イールドカーブ」と「利回り曲線」のように**文字が1つも重ならない言い換えは拾えない**。だから本命は次の2つで、バイグラムは中黒・長音・送り仮名の掃除係にすぎない。

- **プロンプトで `aka` を必ず書かせる**（別名・日本語訳・英語名）。`aka` 同士も突き合わせるので、片方の生成が「利回り曲線」と書いても、もう片方が `aka` に「利回り曲線」を持っていれば当たる
- **概念一覧から手で統合できる**（4.4 の `名前を直す・まとめる`）。既存のタグの名前変更と同じ形で、**既にある名前に変えたら統合になる**（`renameTag` がそう動いているのと同じ）

### 4.6 記録カレンダーの日詳細

`renderDayDetail()` に1枚足す（撮る・瞑想と同じ `recordEntry` の形）。

```
世界 ・ 3件
  中銀が国債買い入れの減額幅を縮めた
  紅海航路の保険料が3か月ぶりに上がった      予測を書いた
  ある国が穀物輸出の枠を来年まで延ばした
```

- その日 `readDate` が入った出来事を並べる
- その日 予測を書いたものには印
- その日 答え合わせをしたものには結果（当／外／保留）を出す（段階2）
- 行をタップすると世界タブのその出来事の詳細へ

月のまとめにも1行足す：`世界の出来事 ・ 読んだ N 件 / 予測 M 件`

### 4.7 「Claude に持っていく」

出来事：

```
【出来事】2026-09-02
中銀が国債買い入れの減額幅を縮めた

【なぜ今動いたか】
長期金利の上がり方が財政の利払いに直接効く域に入り、…

【前提】イールドカーブ
満期の違う国債の利回りを並べた線。…

【詳細】…
【出典】日銀「金融市場調節方針」2026-09-02
https://...

【自分の予測（2026-09-02 記入・期限 2026-12-01）】
減額はもう一段緩む。財政側の要求は止まらない。

この件について、私が見落としている力学を教えてください。
私は既に上の前提は理解しています。
```

概念：

```
【概念】イールドカーブ（利回り曲線 / yield curve）
満期の違う国債の利回りを並べた線。…

【つながる概念】財政ファイナンス ・ 中銀の独立性 ・ 実質金利

【この概念が出た出来事】
- 9/2 中銀が国債買い入れの減額幅を縮めた
- 8/14 …
（最大5件）

この概念について、上の理解の次に来るものを教えてください。
```

`copyToClipboard()` を使う。失敗したら撮ると同じく、下の欄に本文を出して手でコピーさせる。

---

## 5. プロンプト

`SHOOT_PROMPT_HEAD` と同じ前置きを付ける。共通部分は1つの関数にまとめ、分野ごとに差し替える。

### 5.1 共通の骨

```
（このまま Claude のチャットに貼ってください。返事は JSON だけにしてください。）

あなたは、ある人の世界の見方を少しずつ更新するための材料係です。
解説記事は書きません。ニュースの要約もしません。
「これから効き続ける動き」だけを、決められた形で返します。

■ 必ず守ること
- web 検索を必ず使う。検索せずに書かない。
- 下の「ソース」に挙げたドメインで確認できた出来事だけを返す。
- 日付・数字は、出典ページに書かれているものだけを書く。
  推定値・概算を書くときは detail に「推定」と明記する。
- URL は実在するページのものだけ。開けるか確かめる。
  確かめられなければ、その出来事を返さない。
- 件数が足りなくてよい。埋めるくらいなら少ない方がよい。
  10件求めて4件しか確認できなければ4件を返す。
- 出来事以外の文章を返さない。JSON のみ。

■ 期間
直近7日間に動いたもの。ただし「その日のニュース」ではなく、
これから数ヶ月〜数年にわたって効き続ける動きを選ぶ。
発表そのものより、発表が示している力学を選ぶ。

■ ソース：一次（fact・eventDate・detail の数字はここでしか確認できない）
- 日本銀行 https://www.boj.or.jp/
- （設定画面の sources のうち role: 'primary' で分野が一致するものを差し込む）

■ ソース：解釈（why を書くための補助）
- Bruegel https://www.bruegel.org/
- （設定画面の sources のうち role: 'analysis' で分野が一致するものを差し込む）

■ ソースの使い分け（厳守）
- fact・eventDate・detail の数字は、一次のソースで確認できたものだけを書く。
- 解釈のソースだけを根拠に出来事を立ててはいけない。
  シンクタンクの論考は「出来事」ではない。
- 解釈のソースは why を書くときにだけ使う。
  その場合も url と source には、一次のソース（出来事そのものの出所）を書く。

■ すでに知っている概念（説明しなくてよい）
イールドカーブ / シーレーン / 財政ファイナンス / ...
（最大400件。名前だけ。一覧に無くても、経済・政治の基礎的な用語は説明不要）

■ それぞれの出来事に書くこと
- fact：何が起きたか。1文・40字以内。数字か固有名詞を必ず含む。
  聞く人に向けた文にする。専門用語を並べない。
  良い例：「中銀が国債買い入れの減額幅を縮めた」
  悪い例：「日銀が国債買入れオペにおける減額ペースの調整方針を公表」
- why：なぜ今動いたか。1〜2文。
  **誰が何を欲しがり・何を恐れ・何に縛られているか**を書く。
  出来事の言い換えを書かない。評価・感想・助言を書かない。
- premise：この件を理解するのに最低限必要な概念を **1つだけ**。
  - 上の「すでに知っている概念」で足りるなら、新しい概念を作らない。
    そのときは kind を "relation" にして、既知の概念2つ（a と b）の
    **関係**を line に1文で書く。
  - 新しい概念が要るときは kind を "new" にして、name と line を書く。
    aka に別名・日本語訳・英語名を必ず入れる（表記ゆれを防ぐため）。
  - line は1文。その概念の定義であって、この出来事の説明ではない。
- detail：数字・当事者・背景。1〜3文。ここには専門用語を書いてよい。
- source：媒体名と発表日。
- url：出典ページ。
- eventDate：出典ページに書かれた発表日または発生日（YYYY-MM-DD）。
- concepts：この出来事が触れている概念名（既知・新規とも）。
- related：premise の概念と、つながりの深い概念名（既知の一覧から。最大3つ）。

■ 並べ方
重要だと思う順に並べる。上から読まれる。

■ 出力形式
{"events":[{
  "fact":"...",
  "why":"...",
  "premise":{"kind":"new","name":"...","aka":["...","..."],"line":"..."},
  "detail":"...",
  "source":"...",
  "url":"https://...",
  "eventDate":"YYYY-MM-DD",
  "concepts":["..."],
  "related":["..."]
}]}

kind が "relation" のときの premise：
  {"kind":"relation","a":"既知の概念1","b":"既知の概念2","line":"..."}
```

### 5.2 分野ごとの差し替え

**A：金融・経済**
```
■ 分野
金融・経済。中央銀行、財政、統計、債券・為替・商品の市場構造、規制。
株価の上下そのものは書かない。値動きの背後にある制度・制約を書く。
10件を目安に返す（確認できたものだけ）。
```

**B：地政学・政治・国際情勢**
```
■ 分野
地政学・政治・国際情勢。領域と航路、資源とエネルギー、同盟と制裁、
選挙と政変、軍事配置。
どちらが正しいかを書かない。誰が何に縛られているかを書く。
10件を目安に返す（確認できたものだけ）。
```

### 5.3 既知概念の差し込み

- **名前だけ。** 説明は入れない
- 上限 `WORLD_KNOWN_MAX`（400件）。超えたら `lastUsedAt` の新しい順に切る
- `aka` は差し込まない（長くなるだけで、AI 側の判定には名前で足りる）
- 分野で絞る：プロンプトAには `field` が `finance` と `''` のもの、Bには `geo` と `''` のもの
- 末尾に「一覧に無くても、経済・政治の基礎的な用語は説明不要」を必ず付ける

---

## 6. 定数（コード先頭にまとめる）

```js
const WORLD_PER_DAY        = 3;              // 1日の件数
const WORLD_PREDICT_CHOICES= [30, 90, 180];  // 予測期限の選択肢（日）
const WORLD_PREDICT_DAYS   = 90;             // 既定
const WORLD_STALE_DAYS     = 14;             // 未読が「古い」になるまで
const WORLD_KNOWN_MAX      = 400;            // プロンプトに差し込む既知概念の上限
const WORLD_QUEUE_WARN     = 30;             // 未読が溜まりすぎの警告
const WORLD_FACT_MAX       = 40;             // fact の字数
const WORLD_DUP_SIM        = 0.70;           // 出来事の重複しきい値
const WORLD_CONCEPT_SIM    = 0.60;           // 概念名の近さしきい値
const WORLD_PROMPT_COUNT   = 10;             // 1本あたりに求める件数
const WORLD_DUP_WINDOW_DAYS = 3;             // 重複を見る eventDate の窓（前後）
const WORLD_CONCEPT_MIN_LEN = 3;             // 「片方が片方を含む」を使う最短の長さ
const WORLD_OLD_EVENT_DAYS  = 90;            // これより古い eventDate は数だけ知らせる
const WORLD_DAY_LOOKBACK_MONTHS = 4;         // 記録の日詳細で読む月の数
const WORLD_FIELDS         = ['finance', 'geo'];
const WORLD_FIELD_LABEL    = { finance: '金融', geo: '地政学' };
```

---

## 7. 段階の切り分け

### 段階1（API 不使用）

- ソース設定画面（設定「7. 世界」／名前・URL・分野タグ）
- プロンプトのコピー（A／B）
- JSON 取り込み（追記のみ・重複弾き・URL 無しは落とす・落とした理由の表示）
- 概念ノートの保存、一覧、詳細
- **表記ゆれの統合**（自動の正規化一致＋候補提示＋手動の名前変更・統合）
- 朝タブの1ステップ（3件・1行表示・分野の偏り補正）
- 出来事の詳細展開（why / premise → 詳しく → detail / source / url）
- 予測の記入（期限3択・書き直し履歴）※**データ構造は最終形**
- 「Claude に持っていく」コピー（出来事・概念の両方）
- 世界タブ（今日の3件／出来事／概念／予測の待ち一覧）
- 記録カレンダーの日詳細に1枚、月のまとめに1行
- 古い未読のまとめ捨て
- 同期（月ファイル・索引・概念・ソース／署名比較）
- 設定に「予測の索引を作り直す」

### 段階2

- 答え合わせ（朝の1行・世界タブの判定画面・当たり／外れ／保留の比率）
- 記録カレンダーに判定結果を出す
- 概念の網の一般化（`findNote` の差し替え）— **概念が100件溜まってから、やるかどうかを決める**
- 撮るとの連携（カードに `worldId`、「世界から」ピッカー）

---

## 8. 守ること

- **取り込みは追記のみ。** 置き換えを一切しない。読んだ出来事と予測が消える経路を作らない
- 予測のデータ構造は段階1で最終形。段階2で移行しない
- 自動保存・墓標・localStorage 失敗のガードは既存と同じ水準（`persistMonth` の失敗は syncBar に出す）
- kotoba の言葉（notes）・引く・相談・問い・網・朝の言葉に一切混ぜない。`data.notes` を読まない、書かない
- 既存タブの表示は変えない。記録タブの入口の並べ方だけ、1.1 の (b) を採るなら変わる（実装前に確認）
- 定数はコード先頭にまとめ、数字を後から変えられるようにする
- API は使わない。既知判定・重複判定・表記ゆれ判定はすべて端末側の文字比較で行う

---

## 9. 決着済みの論点

- 記録タブの入口 → topbar から画面本体の先頭へ移す（1.1）
- ソースの初期リスト → 22件、`role` 付き（2.5）。URL は実測で確認済み
- 朝のステップの位置 → 流れの一番最後（X投稿の後ろ）（1.2）
- 月ファイルは `eventDate` 基準。未読が残る過去月も起動時に読む（2.1）
- `eventDate` が不正・未来なら取り込まない（3.2）
- 重複判定は月をまたぐ（±3日の窓が月境を越えるとき隣接月も見る）（3.2）

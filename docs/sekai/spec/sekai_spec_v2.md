# 一万日 v11 仕様書 — 世界：差分だけを見ない

段階1.5 までの世界タブに足す。既存の言葉・引く・相談・問い・撮る・筋トレ・散歩には触らない。

## なぜ足すのか

分野A・Bを実データで2回回した結果、**このアプリは「差分を集める装置」になっている**ことが分かった。出来事は「変化したもの」しか映さない。だが世界を理解するうえで効いてくるのは、次のようなものだ。

- **動かないもの**：金利、人口、債務残高は数十年単位でしか動かないので、毎日見ていると存在しないように見える。反転して初めて「あれは条件だったのか」と気づく
- **溜まるもの**：報道はフロー（起きたこと）を扱うが、効くのはストック（積み上がった債務、失われた信頼、制度の摩耗）。何も起きなかった日にも溜まっている
- **起きなかったもの**：回避された危機は記事にならない。起きた事例だけで世界像を作ると確率の感覚が壊れる
- **発信者の立場**：誰が何を持って喋っているかが情報の形を決める。一次ソースだから中立ではない
- **枠組みそのもの**：「西側対中露」のような区切りは情報ではなく容器。容器は検証されないまま使われ続ける

共通しているのは、**情報を集める行為が差分に注意を向けさせる構造を持っている**こと。プロンプトの調整では直らない。データの種類を足す。

---

## 0. 範囲

**段階2（この仕様書）— すぐ効くもの**

| | 入れるもの |
|---|---|
| A | 地層（動かないもの・溜まるものを毎日見せる） |
| C | 発信者の立場（ソースに1行） |
| E | `reason`（なぜこの札を選んだか） |
| F | 非表示（出来事・概念・用語） |
| G | 整理（用語が沈む／概念の上位概念） |

**段階3 — 期限が来てから**

答え合わせ（元の段階2）／B 来なかったもの／D 枠組み（作るかどうかを含めて）

B は予測の決着から作られるが、答え合わせ自体が未実装で、最初の予測の期限は12月。いま作っても3ヶ月間0件の画面ができるだけで、実物を見ずに形を決めることになる。D も、月の振り返りを1回も回していない段階では使い道が確かめられない。

---

## 1. A：地層

### 1.1 何であるか

**数十年単位でしか動かない量**を10〜20個だけ持つ。増やさない。**生成には作らせない**（増えたら意味がなくなる）。

出来事は必ずどれかの地層に乗る。乗らないものもある（`strata` は空でよい）。

### 1.2 保存

概念・用語とは**別の形**にする。あちらは増えるものなので統合や `dismissedPairs` を持つが、地層は閉じたリストで、統合も要らない。同じ形にすると使わない機構がついてくる。

```
world/strata.json
{ version: 1, updatedAt,
  strata: [{ id: 'st_jp_rate_long',   // 手で付ける短い英字 id。生成はこれを返す
             name: '日本の長期金利',
             line: '1990年から30年下がり続け、2022年から上げ',
             unit: '%',
             dir: 'up'|'down'|'flat',
             dirSince: '2022',
             where: { source: '財務省 国債金利情報', url: '' },  // 更新のたびに開く場所
             levels: [{ value: '1.85', at: '2026-07-01', note: '' }],  // 新しい順・全部残す
             active: 1,
             order: 0,
             createdAt, updatedAt }] }
```

端末は `worldStrata`（kv）に全件。索引に `strata: { updatedAt, count }` を1行足す（概念・用語と同じ署名比較）。

**`levels` は上書きせず全部残す。** 地層の値そのものが「溜まるもの」なので、上書きしてしまうと**この機能が防ごうとしている当のもの（ストックが見えない）を、この機能自身がやる**ことになる。四半期に一度なら10年で40件、全部足しても数KB。

**`where` は更新を止めないための項目。** 四半期に一度手で入れるとき、毎回「どこの数字だったか」を探すのが一番の摩擦になる。地層一覧から直接開ける。

### 1.3 初期リスト（13件・`levels` は空で登録）

数字は入れない。`where` の確認先を開いて手で入れる。**ここで推測の数字を入れると、この機能の信頼が最初から壊れる。**

| id | 名前 | line | unit | dir | dirSince | where |
|---|---|---|---|---|---|---|
| st_jp_rate_long | 日本の長期金利 | 1990年から30年下がり続け、2022年から上げ | % | up | 2022 | 財務省 国債金利情報 |
| st_us_rate_long | 米国の長期金利 | 1981年から40年下がり続け、2020年に底、2022年から上げ | % | up | 2022 | FRB H.15 |
| st_jp_debt_gdp | 日本の政府債務/GDP | 1990年代から上がり続け、下がった局面がほぼない | % | up | 1990s | IMF / 財務省 |
| st_us_debt_gdp | 米国の政府債務/GDP | 2008年以降上がり続け、2020年に急増 | % | up | 2008 | IMF / FRB Z.1 |
| st_jp_pop_working | 日本の生産年齢人口 | 1995年をピークに減り続けている | 万人 | down | 1995 | 総務省統計局 人口推計 |
| st_cn_pop | 中国の人口 | 2022年から減少に転じた | 億人 | down | 2022 | IMF / 国連 |
| st_boj_assets | 日銀の資産規模 | 2013年から急拡大、2024年から国債買入れ減額へ | 兆円 | down | 2024 | 日本銀行 営業毎旬報告 |
| st_jp_fx_reserve | 日本の外貨準備残高 | 2011年以降1.2〜1.4兆ドルで横ばい、2026年に介入で減少 | 億ドル | down | 2026 | 財務省 外貨準備等の状況 |
| st_yen_reer | 円の実質実効為替レート | 1995年をピークに下がり続け、2022年以降は1970年代の水準 | 指数 | down | 1995 | BIS 実効為替レート |
| st_jp_energy_self | 日本のエネルギー自給率 | 1960年代から下がり、2011年以降1割前後 | % | flat | 2011 | 資源エネルギー庁 / IEA |
| st_world_trade_gdp | 世界貿易/GDP | 1990年代から2008年まで上昇、以降横ばい | % | flat | 2008 | WTO / IMF |
| st_world_military | 世界の軍事費 | 2010年代から増加、2022年以降加速 | 億ドル | up | 2022 | SIPRI |
| st_cb_gold | 各国中銀の金保有 | 2010年以降純購入が続き、2022年から増加 | トン | up | 2022 | IMF / BIS |

`where.url` は空で登録する（実装後に手で入れる）。ソース設定と同じく、**一度も保存されていないときだけ**入れる。

### 1.4 出来事への紐づけ

```
ev.strata = ['st_jp_rate_long']   // 0〜2件
```

生成には**一覧の `id` から選ばせる**。閉じたリストなので、概念・用語と違って表記ゆれの問題が起きない。

取り込み時に**一覧に無い id は落とす**（`ev.strata = ids.filter(存在する)`）。落とした件数は結果表示に出す。

### 1.5 見せ方 — 2つに分ける

指示書には2つの要求が入っていて、そのままでは両立しない。

- 「出来事は必ずどれかの地層に乗る」「fact の下に1行出る」→ 出来事に紐づく
- 「何も起きなかった日にも出続ける」→ 出来事に紐づかない

出来事に紐づけると、取り込みが切れた日には1行も出ない。3件それぞれに出すと、1行に絞った一覧に3行増える。**分ける。**

**(1) 出来事の行 → タグだけ**

premise のタグの隣に、地層の短い名前をもう1つ。**行は増えない。**

```
9/2  中銀が国債買い入れの減額幅を縮めた      [金利] [イールドカーブ]
```

地層タグは `--text-faint`（premise のタグより弱く）。押すとその地層の詳細へ。

**(2) 今日の3件の上 → 地層の1行を毎日1つ、日替わりで巡回**

```
長期金利 ── 1990年から30年下がり続け、2022年から上げ
1.85%（2026.07.01 ・ 71日前）
```

- 巡回は日付から決める：`有効な地層[ 今日の通日 % 件数 ]`。保存は要らず、同じ日に何度開いても同じものが出る
- 13件あれば13日で一周する
- **`levels` が空なら「値は未記入」**と出す。押すと地層の詳細（値を入れる場所）へ飛ぶ
- **「前回更新から N日」を必ず出す。** 手で四半期更新する設計なので、古いまま放置されていることが見えないと数字が嘘になる

**なぜ巡回にするか。** 変わらないものを毎日同じ形で見せると、2週間で見えなくなる（それが差分に注意が向く仕組みそのもの）。巡回させれば全部を定期的に通過しつつ、毎日「今日はどれか」という小さな差が残る。壁紙にならない最小の工夫。

**(3) 朝の畳んだ行の下**

`WORLD_STRATUM_ON_COLLAPSED`（既定 `true`）が立っているとき、朝の畳んだ行の**下に薄い1行**で地層を出す。

```
世界 ・ 今日の3件
  長期金利 ── 40年下げて、2022年から上げ ／ 1.85%（71日前）
```

A の目的は「毎日見せる」ことなので、開かないと見えない場所に置くと機能しない。ただし朝の流れが1行増える。**気に入らなければ定数を `false` にすれば、展開の中だけになる。**

### 1.6 地層の一覧と詳細

世界タブのタブ行に4つ目は作らない（`[出来事] [概念] [予測]` のまま）。**地層は「今日の1行」を押すと開く**、独立した画面にする（概念詳細と同じ階層）。一覧はその画面の中から辿る。

地層の詳細：

```
日本の長期金利
1990年から30年下がり続け、2022年から上げ
上向き（2022年から）・単位 %

いまの水準
  1.85 %   2026.07.01（71日前）
  [ 値を入れる ]

  確認先：財務省 国債金利情報  [ 開く ]

これまで
  1.85 %   2026.07.01
  1.72 %   2026.04.01
  ...

この地層に乗った出来事（6件）
  9/2  中銀が国債買い入れの減額幅を縮めた
  ...

[ 一覧 ]
```

**値の更新は地層一覧の中でやる**（四半期に一度、確認先を見ながら入れるので、一覧を見ている場所で入れるのが自然）。設定は登録・並べ替え・有効/無効だけ。

---

## 2. C：発信者の立場

ソースに1行足す。`role` とは別項目。

```
source.stance = '国債の金利を低く保ちたい'   // 60字まで
```

- 分野では変わらない（財務省は分野A・Bの両方に出るが、欲しいものは同じ）
- **表示**：出来事の「詳しく」の中、出典のすぐ下
- **コピー**：「反対の見方は」にだけ入れる。反論は前提を突くので効く。他の3つには入れない（「やさしく説明」に入れるとノイズ）
- 文面は22件ぶんを後から手で入れる（設定の1行入力）

一次ソースだから中立ではない、を毎回目に入れるための項目。

---

## 3. E：`reason`

```
ev.reason = '減額の速度が財政側の要求で決まる形が続く'   // 60字まで
```

- `why` は「なぜ動いたか」、`reason` は「なぜこの札を選んだか」
- 生成に20字程度で書かせる
- **表示は「詳しく」の中だけ。** 段階2では読むだけにする（「この選び方は違う」の印を足すのは、実際に反対したくなる札が出てから）

---

## 4. F：非表示

**本文は残して見えなくする。** 月ファイルからは消さない。

### 4.1 出来事

`ev.hidden = 1`、`ev.hiddenAt`。

既存の `worldEvents` は**14か所から読まれている**。フラグだけ足して絞り忘れると、**非表示の札が朝の3件に出る**（一番まずい壊れ方）。

**`worldLiveEvents()` を1つ作り、読み出しを全部そこに通す。** 生の `worldEvents` を使ってよいのは3か所だけ：

| 生を使う | 理由 |
|---|---|
| `worldMonthDoc()`（保存） | 本文を残すため |
| `worldDupOf()`（重複判定） | **消したのに次の取り込みで戻ってくるのを塞ぐ。** ここを絞ると非表示の意味が消える |
| `mergeWorldSets()`（マージ） | 端末間で消えないため |

索引の `unread` からも外す（非表示の未読はキューに残さない）。

### 4.2 概念・用語

`hidden: 1` のフラグ。`worldConcepts()` は4か所、`worldTerms()` は5か所からしか読まれていないので、絞り忘れる面が小さい。

- 一覧に出さない
- **生成の既知一覧から外す**
- 本文中のチップにもしない（`worldTermEntriesOf` で落とす）
- 出来事の `concepts` / `terms` の文字列はそのまま残す（復活したらまた効く）

### 4.3 戻す

設定「6. 世界」に**「非表示のもの」**を置く。出来事・概念・用語を種類ごとに並べ、押すと戻る。件数を見出しに出す。

---

## 5. G：整理

### 5.1 用語が沈む

**`seenAt` は使わない。** 注を開くのは**その語を知らなかったとき**なので、`seenAt` で沈めると IMF や FRB のように完全に身についた語から先に沈み、一度つまずいた語が上に残る。意図と逆になる。

**`lastUsedAt`（最後にその語が札に出た日）を使う。**

```
沈む条件：lastUsedAt が WORLD_TERM_SINK_DAYS（既定90）より古い
```

- 用語一覧の**下段**に「しばらく出ていない用語（N）」として畳んで置く。**消さない**
- 沈んでいても、その語を含む古い札を開けば注は普通に出る
- **生成の既知一覧から外す**

3つ効く。(1) プロンプトが縮む方向に働く、(2) 90日どの札にも出なかった語はこちらも忘れている可能性が高く、もう一度注が付くのは正しい、(3) 名前で引き当てるので重複は作られない（`worldFindIn` が既存に当て、`line` が空なら埋める、という今の動きに乗る）。

### 5.2 概念の上位概念

```
concept.parent = 'wc_...'   // 概念 id を1つ。無ければキー自体を持たない
```

**手で付ける**（概念詳細に「上位概念を選ぶ」）。**生成には選ばせない**（地層と同じで当てずっぽうが入る）。

穴は3つ。全部塞ぐ。

| 穴 | 対処 |
|---|---|
| 統合したとき | 消える側を親にしていた子を、残る側に付け替える。消える側が親を持っていたら残る側に引き継ぐ（残る側に親が無いときだけ） |
| 循環 | A→B→A を作らせない。設定時に祖先を辿り、自分が出たら弾いて理由を出す |
| 深さ | **2段まで**（親と子だけ）。孫を許すと一覧が木になり、畳む UI が要る。親を持つ概念は親になれない |

並び替えは競合しない。**「親でまとめる」を4つ目の切替**にして、既存の回数順・五十音順・新しい順はフラットのまま残す。

`related`（横のつながり）と `parent`（縦の束）は別の軸なので、段階3以降で網をやることになっても衝突しない。

---

## 6. 生成プロンプトの新版

変更は4つ。

1. `strata` が出力に増える（末尾の一覧から `id` を選ぶ）
2. `reason` が出力に増える
3. **既知一覧を末尾へ移す**（規則 → 出力形式 → 既知一覧）。3本になると、長い羅列が指示を押しのけるため
4. 既知一覧から、非表示のものと沈んだ用語を外す

全文は別掲（この仕様書と一緒に見せる）。

---

## 7. 定数（追加分）

```js
const WORLD_STRATA_PER_EVENT     = 2;     // 1件が乗れる地層の数
const WORLD_STRATUM_ON_COLLAPSED = true;  // 朝の畳んだ行の下にも地層を出すか
const WORLD_TERM_SINK_DAYS       = 90;    // これだけ札に出ない用語は一覧の下に沈む
const WORLD_CONCEPT_PARENT_DEPTH = 2;     // 上位概念の深さ（親と子だけ）
const WORLD_REASON_MAX           = 60;    // なぜこの札を選んだか
const WORLD_STANCE_MAX           = 60;    // 発信者の立場
const WORLD_STRATUM_DIRS         = ['up', 'down', 'flat'];
const WORLD_STRATUM_DIR_LABEL    = { up: '上向き', down: '下向き', flat: '横ばい' };
// 初期の13件は WORLD_DEFAULT_STRATA（1.3 の表）
```

---

## 8. 守ること

- **地層は生成に新規作成させない。** 一覧に無い id は取り込み時に落とす
- **地層の `levels` は上書きしない。** 全部残す
- **初期リストの `levels` は空。** 推測の数字を入れない
- 非表示は本文を残す。**重複判定だけは非表示のものも見る**（消したものが戻ってくるのを塞ぐ）
- 用語が沈む信号は `lastUsedAt`。`seenAt` ではない
- 上位概念は手で付ける。2段まで。循環を作らせない
- 出来事の読み出しは `worldLiveEvents()` に一本化する。生の配列は保存・重複判定・マージだけ
- 既存の言葉・引く・相談・問い・撮る・筋トレ・散歩に影響を出さない
- 定数と文面はまとめて置く

---

## 9. 段階3へ送ったもの

- **答え合わせ**（元の段階2）：期限の来た予測に当たり／外れ／保留を付ける
- **B 来なかったもの**：予測タブの3つ目。「起きなかった」で決着したものを、数ではなく札として読み返す。「自分は起きると思っていた」に印。混ぜて印だけ分ける（分けると両方とも件数が少なすぎる）
- **D 枠組み**：作るかどうかを含めて、月の振り返りを1回やってから決める。「使わないリストは、それ自体が管理しているつもりを生む」と「見えないものは疑えない」のどちらが効くかは、回してみないと分からない

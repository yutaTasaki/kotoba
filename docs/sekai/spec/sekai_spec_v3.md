# 一万日 v12 仕様書 — 世界：動かす側（アクター）の層

段階2.5 までの世界タブに足す。既存の言葉・引く・相談・問い・撮る・筋トレ・散歩には触らない。

## なぜ足すのか

このアプリの本当の目的（本人の言葉）：

> すべての物事は人間の思惑で動いている。
> その思惑は、お金の動きや行動に現れる。それがわかるような仕組みを作りたい。

「情報を集めて体系的に理解する」は手段で、理解したいのは**誰が何を欲しがって動かしているか**。

いまの札には `why` があり、問いは正しい。だが答えが**札ごとの文章に溶けていて、札を閉じると消える**。財務省が7月に何を欲しがり9月に何を欲しがったかを並べて見る手段がない。概念の網は「何の話か」で繋ぐ。見たいのは「**誰が動かしているか**」で繋ぐ軸。いまの設計には**主語が無い**。

### 一番の危険：思惑の捏造

思惑を書かせると生成は必ず捏造する。出典には「財務省が介入した」しか書いていないのに「円安による輸入物価上昇を恐れて」と足す。もっともらしくて検証できない。

だから **行動／述べた理由／読んだ思惑** の3層を分けて持ち、**「述べた理由と同じなら、読んだ思惑は空」** とする。これで生成は述べた理由をなぞって推定に化かす道を失い、read を書くなら述べたことと違うことを書くしかなくなる。書けなければ空でいい。**空が多いのは正常。**

---

## 0. 範囲

| | 入れるもの |
|---|---|
| 1 | アクターのストア（種22件・開いたリスト・統合の仕組みは概念と共通） |
| 2 | 札ごとの記録 `ev.actors[]`（did／said／read の3層）と `ev.flow` |
| 3 | 取り込み：正式名へ寄せる・規則の機械的な担保・推定の割合を出す |
| 4 | 表示：札の展開に「動かした側」／用語と地層を1行に／推定の印 |
| 5 | アクターの詳細（時系列・金額が縦に並ぶ）と一覧の切替 |
| 6 | 既存の札への遡及（別文面のプロンプト＋足すだけの取り込み） |

**B（網の可視化）はこの後。** ノードが概念かアクターかは、アクターの形を実データで見てから決める（1枚・中心を切り替える形が有力）。

---

## 1. アクターのストア

### 1.1 種を撒いて開く

地層（閉じた13件）でも概念（開いた）でもない、第3の形。**主要な主体は最初から aka 付きで登録し、新顔は概念と同じく生成が作れる。** 生成が「日本銀行」と書いても「BOJ」と書いても同じ主体に落ちる。表記ゆれは `WORLD_STORES` の仕組み（正規化・候補・手動統合）をそのまま使う。

```
world/actors.json
{ version: 1, updatedAt,
  actors: [{ id: 'ac_mof_jp',                // 種は固定 id。新顔は ac_YYYYMMDD_連番
             name: '財務省',
             aka: ['MOF', '日本の財務省'],
             kind: 'state'|'cb'|'intl'|'industry'|'person'|'',
             stance: '',                      // ふだん欲しがるもの。手書き・任意。同名ソースの stance を初期値に
             eventIds: [], createdAt, updatedAt, lastUsedAt, hidden }],
  dismissedPairs: [] }
```

端末は `worldActors`（kv）に全件。索引に `actors: { updatedAt, count }`。署名比較で変わったときだけ書く（概念と同じ）。

`WORLD_STORES.actor = { key:'actor', label:'アクター', idPrefix:'ac_', minLen: WORLD_ACTOR_MIN_LEN(3), rewrite: ev.actors[].name と ev.flow.from/to を寄せる }`。`worldStoreOf()` は id の接頭辞 `ac_` でアクターを返す。

### 1.2 種（22件・`stance` は空。同名ソースがあればその `stance` を初期値にコピー）

| id | name | aka | kind |
|---|---|---|---|
| ac_mof_jp | 財務省 | MOF, 日本の財務省 | state |
| ac_boj | 日本銀行 | 日銀, BOJ | cb |
| ac_gov_jp | 日本政府 | 政府, 内閣, 首相官邸 | state |
| ac_fed | FRB | 米連邦準備制度, Fed, FOMC | cb |
| ac_treasury_us | 米財務省 | Treasury | state |
| ac_admin_us | 米政権 | ホワイトハウス, 米大統領 | state |
| ac_congress_us | 米議会 | 上院, 下院 | state |
| ac_ecb | ECB | 欧州中央銀行 | cb |
| ac_eu | 欧州委員会 | EU, 欧州連合 | state |
| ac_pboc | 中国人民銀行 | PBOC | cb |
| ac_gov_cn | 中国政府 | 北京, 国務院 | state |
| ac_imf | IMF | 国際通貨基金 | intl |
| ac_bis | BIS | 国際決済銀行 | intl |
| ac_opec | OPEC＋ | OPEC, 産油国 | industry |
| ac_wto | WTO | 世界貿易機関 | intl |
| ac_nato | NATO | 北大西洋条約機構 | intl |
| ac_un | 国連 | UN, 安保理 | intl |
| ac_gov_ir | イラン政府 | イラン, テヘラン | state |
| ac_gov_ru | ロシア政府 | ロシア, クレムリン, モスクワ | state |
| ac_gov_ua | ウクライナ政府 | ウクライナ, キーウ | state |
| ac_gov_kp | 北朝鮮 | 朝鮮民主主義人民共和国, 平壌 | state |

**「市場参加者」は入れない。** 市場は思惑を持つ主体ではなく、思惑が現れる場所。`flow.to` に自由文で「為替市場」と書ける形があれば足りる。

一度も保存されていないときだけ入れる（地層・ソースと同じ）。

### 1.3 用語との関係

IMF は用語（注）でもアクター（主語）でもある。**重ねたまま役割で分ける。** 用語ストアに注、アクターストアに立場と履歴。**注は複製しない**——アクターの詳細を開いたとき、同名の用語があればその注を借りて出す。

### 1.4 個人の扱い

**原則は組織をアクターにする。** 審議委員の発言は「日本銀行」の行動として読む。**個人がアクターになるのは、組織と立場が違うとき**（審議委員が執行部と違う提案をした、閣僚が政権方針と違うことを言った）。そのときだけ `kind: 'person'` で作る。生成プロンプトにこの規則を書く。

---

## 2. 札ごとの記録

### 2.1 3層

```
ev.actors = [{ name: '財務省',                   // 取り込み時に正式名へ寄せる（概念と同じ）
               did:  '円買い介入 15兆円',         // 行動。出典に書かれたことだけ。数字はここにだけ
               said: '投機的な動きに対応',        // 述べた理由。出典が自ら述べたものだけ。無ければ空
               read: '円安による輸入物価上昇を止めたい' }]   // 読んだ思惑。生成の推定。said と同じなら空
```

- 1札に **`WORLD_ACTORS_PER_EVENT`（2）まで**。多い札は主語が散っている
- **`did` が空の主体は取り込み時に落とす**（行動が出典に書かれていない主体をアクターにしない。why に出てくるからといって「産油国が…」と書かれると、その札で産油国は何もしていないのにアクターになる）
- **`read` を正規化して `said` と同じなら、取り込み時に `read` を空にする**（プロンプトの規則を機械でも担保する）
- 字数：did 40／said 60／read 60

`why` は残す。why は札1つの読み（誰が誰に、を1文で）、actors はそれを主語ごとに分解したもの。表示で重ならないよう Q4 で畳む。

### 2.2 お金の動き

```
ev.flow = { from: '財務省', to: '為替市場', what: '円買い介入',
            amount: '15兆円',         // 出典の書き方のまま。表示はこれ
            value: 15000000000000,   // 端末側で解析できたときだけ。できなければ null
            unit: 'JPY' }             // 'JPY'|'USD'|'EUR'|'CNY'|'%'|''
            | null
```

- **換算しない。** 円をドルに直した瞬間に生成が為替レートを選ぶ＝推定が事実の顔をして混ざる。比較は同じ単位どうしだけ
- `from`／`to`：アクター名ならストアの正式名へ寄せる。当たらなければ自由文のまま（「為替市場」はアクターではない）
- `value`／`unit` は取り込み時の小さな解析だけ：`(数)(兆|億|万)?(円|ドル|ユーロ|元|%)`。「15兆円」→ 15e12 JPY、「1.4億ドル」→ 1.4e8 USD。それ以外は `value: null, unit: ''`
- 無い札は `null`。fork と同じく無理に書かせない

---

## 3. 取り込み

1. `ev.actors` を1件ずつ検査：`name` 空・`did` 空は落とす。`read` が `said` と同じなら `read` を空に
2. `name` をストアで引き当て（name＋aka）。**無ければ作る**（`kind` は生成の値、`stance` は同名ソースがあればコピー）。正式名へ寄せる。`eventIds`・`lastUsedAt` を触る
3. `flow.from`／`to` も引き当て、当たれば正式名に。`amount` を解析
4. 統合候補（概念・用語と同じ画面に `[アクター]` の別で並ぶ）
5. **結果に出す**：
   - `動かした側つき N件`
   - `推定を書いた札 N件／全 M件` ← **高すぎたら生成が推定を作りすぎている**。空が多いのは正常
   - `お金の動きつき N件`
6. **取り込みの最後の自動数え直し（v11.7）にアクターも含める**：端末にある札を読み直し、`eventIds` を足す（足すだけ・冪等）

---

## 4. 表示

### 4.1 札の展開 — 段数は 7 のまま

```
なぜ今動いたか（why の1文）
動かした側
  財務省    円買い介入 15兆円                    ← name + did の1行。押すと下に開く
            述べた  投機的な動きに対応
            [推定]  円安による輸入物価上昇を止めたい
  日本銀行  声明で介入を「政府の判断」と表現
premise カード（再会の行つき）
用語 ・ 地層（1行にまとめる）                    ← いまは2段。これで増えた1段を相殺
つながる札
[★][詳しく][予測][Claude に聞く][非表示]
詳しく：detail／選んだ理由／お金の動き／出典／立場／URL
```

- 「動かした側」は **name + did の1行が既定**。said／read は押したときだけ開く（用語の注と同じ）
- `said` が空なら「述べた」行を出さない。`read` が空なら推定の行を出さない。両方空なら押しても開かない
- **推定の印は色と形で強く分ける**：`[推定]` を琥珀色（`--guess`、新しい色）の塗りつぶしバッジ、行の左に破線。薄いラベルだと読み慣れたときに事実と区別がつかなくなる
- アクター名を押すと、そのアクターの詳細（4.3）へ
- **用語チップと地層ピルを1行に**（どちらも短いチップ）
- `flow` は独立した段を作らない。「詳しく」の中に `お金の動き：財務省 → 為替市場　円買い介入 15兆円` の1行
- **出典の立場（v11 C）は、その出典が札のアクターに含まれているときは出さない**（動かした側に同じことが書いてある）。含まれていないときだけ従来どおり

### 4.2 コピー（Claude に聞く）

4つの依頼文すべてに、`【動かした側】` として name／did／said／read を渡す（read には「推定」と明記して渡す）。「反対の見方は」では **read を疑う**ことを依頼文に足す：`私が推定として書いた思惑が外れているとしたら、どこか`。

### 4.3 アクターの詳細 — 時系列

```
財務省（MOF ／ 日本の財務省）
注：国の予算と国債の発行を決める役所           ← 同名の用語があれば借りる
ふだん欲しがるもの：国債の金利を低く保ちたい     ← stance（手書き・任意。ここで直せる）

この半年
  9/2   円買い介入 15兆円           ← date + did（+ flow の amount がこの主体のものなら太字）
        述べた  投機的な動きに対応
        [推定]  円安による輸入物価上昇を止めたい
        └ 札を開く
  7/14  円買い介入 5兆円
  6/30  為替介入の実績を公表
  ...

[名前を直す・まとめる] [非表示] [この半年の動きを読む]
```

- `eventIds` から月を割り出し（id に eventDate が入っている）、**その月だけ読み込む**。端末に無い月は「読み込み中」を出して待つ。記録タブが月を引くのと同じ
- **金額が縦に並ぶ**のがこの画面で一番見たい絵。`flow.from` か `to` がこの主体で `amount` があれば右端に出す。**同じ単位だけが並ぶ**（換算しないので、混ざっていれば混ざって見える。足し算もしない——合計もまた判断なので）
- 「この半年の動きを読む」は、この時系列をそのまま貼るコピー（`WORLD_ACTOR_ASK_PROMPT`）。依頼文：述べた理由と推定を並べて、**言っていることと動かした金額のズレ**を指摘してもらう。300字

### 4.4 一覧

概念タブの切替を `[概念] [用語] [アクター]` の3つに。並び替えは回数順／五十音順／新しい順（親でまとめる・沈むは無し）。検索あり。統合・名前変更・非表示の操作口はここ。**主な入口は札の「動かした側」の名前**で、一覧は整理の場所。

---

## 5. 遡及（既存の札）

**概念とは逆の判断で、作る。** 主語の軸は履歴が無いと開く意味がない——「財務省がこの半年何を欲しがってきたか」は、いまの17件が無いと最初の数か月が空白になる。

### 5.1 形

取り込み画面に3つ目の段 **「アクターを付け直す（既存の札）」**。

1. **プロンプトをコピー**：`ev.actors` が空の札（端末にある分・`WORLD_RETRO_MAX`＝40件まで）の id・fact・why・detail・source を差し込む。文面は `WORLD_ACTOR_RETRO_PROMPT`（通常の生成とは別。出典を開き直さない前提なので縛りが違う）
2. **JSON を貼って取り込む**：札 id ごとの `actors` と `flow`。**`ev.actors` が空の札にだけ足す**。置き換えない。id が端末に無い札は無視
3. 結果は通常の取り込みと同じ形で出す（推定の割合を含む）

### 5.2 縛り（通常より厳しい）

捏造の危険は遡及のほうが高い（出典を開き直さず、why の文章から読むため）。

- `did` は **fact・detail に書かれた行動だけ**。why から行動を作らない
- `said` は **why か detail に「述べた」「説明した」「表明した」と分かる形で書かれているときだけ**。無ければ空
- `read` は必ず推定。said と同じなら空
- `flow` は **detail に金額があるときだけ**

---

## 6. 生成プロンプトの差分

変更は4つ。全文は別掲。

1. `why` の直後に **`actors` の規則**（3層・2件まで・行動が無い主体は書かない・個人の規則・said と同じなら read は空）
2. `strata` の後に **`flow` の規則**（任意・換算しない・出典の書き方のまま）
3. 出力形式に `actors`・`flow`
4. 末尾に **`■ すでに知っているアクター`**（name（aka）。`WORLD_KNOWN_ACTORS_MAX`＝60、`lastUsedAt` 順。無ければ新しく書いてよい）

長さ：約 13.2KB → **約 15KB**。用語一覧のほうがまだ大きい。

---

## 7. 定数（追加分）

```js
const WORLD_ACTORS_PER_EVENT = 2;     // 1札の主語の数
const WORLD_KNOWN_ACTORS_MAX = 60;    // プロンプトに差し込む既知アクターの上限
const WORLD_ACTOR_MIN_LEN    = 3;     // 「片方が片方を含む」を使う最短の長さ
const WORLD_ACTOR_DID_MAX    = 40;
const WORLD_ACTOR_SAID_MAX   = 60;
const WORLD_ACTOR_READ_MAX   = 60;
const WORLD_ACTOR_KINDS      = ['state', 'cb', 'intl', 'industry', 'person'];
const WORLD_ACTOR_KIND_LABEL = { state: '政府', cb: '中銀', intl: '国際機関', industry: '業界', person: '人物', '': 'その他' };
const WORLD_FLOW_UNITS       = ['JPY', 'USD', 'EUR', 'CNY', '%'];
const WORLD_RETRO_MAX        = 40;    // 遡及プロンプトに載せる札の数
// WORLD_DEFAULT_ACTORS（1.2 の22件）／WORLD_ACTOR_RETRO_PROMPT／WORLD_ACTOR_ASK_PROMPT は定数に
```

---

## 8. 守ること

- **思惑を fact と混ぜない。** did にだけ数字、read は必ず推定の印、said と同じ read は空
- **行動が出典に書かれていない主体をアクターにしない**（取り込みでも `did` 空を落とす）
- **換算しない。** amount は出典の文字列のまま。同じ単位だけ並べる。合計もしない
- 個人は組織と立場が違うときだけ
- 用語とアクターを混ぜない（注は用語、主語はアクター。注は借りるだけで複製しない）
- 朝の流れを長くしない（アクターは世界タブの中だけ）
- 展開の段数を増やさない（用語と地層を1行にまとめて相殺）
- 遡及は `ev.actors` が空の札にだけ足す。置き換えない
- 既存の札・概念・用語・地層は消さない

---

## 8.5 実装時の差分（v12）

- 種は **21件**。回答の「計22件」は「市場参加者」を含めた18件に4件を足した数で、市場参加者を外したぶん1件少ない
- 「公表そのものを行動としない」を通常・遡及の両方に入れた。「反対の見方は」の1行は**「札に推定として付いている思惑が外れているとしたら、どこか」**（推定を書いたのは生成であって本人ではない）
- 取り込みの call site で `actors`・`flow` を `normalizeWorldEvent` に渡し忘れていて、テストで「動かした側つき 0件」が出た。直した。プロンプトが完璧でも、ここが抜けていれば札に主語が付かない——実データで回す前に見つかったのは、テストが「取り込み結果の文言」まで見ていたため
- アクターの一覧は `[概念][用語][アクター]` の切替。詳細は概念と同じ画面で、アクターのときだけ「つながる概念」「上位概念」を隠し、「ふだん欲しがるもの」の入力と時系列を出す

## 8.6 時系列の思惑は押す前から出す（v12.1）

実機で「時系列に思惑が出ていない」と報告があり、調べると **said/read も金額もすでに描画されていた**（押せば開いた）。壊れてはいなかった。

だが**目的は今の形では果たせない**。狙いは「日本政府の3件なら、述べたのは1件だけというのが**縦に見える**」こと。3行を1つずつ押して回るのは「縦に見える」ではない。

そこで時系列だけ、**said/read を押す前から出す**形に変えた。

- 日付＋did の行の下に、`述べた` と `[推定]` を直接置く
- **何も無い行は何も出さない。その空白そのものが「述べていない」の信号**になる
- 押して開くのは札の文脈（fact と「札を開く」）だけ。行には `▸/▾` を付けて押せることを示す

**札の展開（動かした側）は押す形のまま**にする。あちらは既に7段ある密な文脈で、2主体×2行＝4行増えると読めなくなる。時系列は「パターンを読むための専用画面」なので、扱いが違ってよい。

`▸` を足したのは、地層の入口で学んだのと同じ理由——**地味さと「押せる」手がかりは同じ要素に両立しない**。押せると分からない行は、無いのと同じ。

## 9. 実装後に見る数字

- 取り込み結果の **「推定を書いた札 N件／全 M件」**。半分を大きく超えたら、生成が推定を作りすぎている（said をなぞっていないか、実物の JSON で見る）
- **「動かした側つき N件」が全件に近いか**。低ければ、生成が行動の主体を見つけられていないか、「行動が無い主体は書かない」で落としすぎている
- 遡及のあと、**財務省・日本銀行の時系列が何件並ぶか**。ここが3件以上あれば、この層は最初から読める

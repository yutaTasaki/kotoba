# 一万日 v10.5 仕様書 — 世界：読めるようにする

段階1で作った世界タブに足す。既存の言葉・引く・相談・問い・撮る・筋トレ・散歩には触らない。

## なぜ足すのか

段階1を1週回した結果、一覧・展開・詳細の3段は狙いどおりに働いた。問題は**本文の中身の側**にあった。

「IMFがエルサルバドルへ約1.4億ドル追加融資で実務合意」という札は、IMF が何でエルサルバドルがどこかを知らないと1文字も入らない。premise は1件に1つなので、この札の premise は「コンディショナリティ」になり、IMF も国名も素通りする。

「Claude に聞く」で埋められるはずが、**毎回質問文を考えるのが面倒で押さない**。ここを工数ゼロにしないと「読めない札が並ぶ画面」で終わる。

予測も同じ理由で1件も書かれなかった。**白紙の自由記述は書く気が起きない。**

段階1.5 はこの3つを潰す。

---

## 0. 範囲

| | 入れるもの |
|---|---|
| 1 | 用語の注（terms）— 保存・本文中のチップ・チップ行・`seenAt`・統合・一覧の切替 |
| 2 | 定型プロンプト（出来事に4つ、概念に2つ） |
| 3 | fork と3択の予測（空振り可） |
| 4 | ★（出来事に付ける） |
| 5 | 概念一覧の並び替えの切替 |

**段階1.6 は痩せさせだけ**（唯一データを壊せる操作で、`WORLD_SLIM_MONTHS`＝12ヶ月経つまで対象が1件も出ない）。

### 既に実装済みで、今回やるのは切替だけ

概念一覧の**既定は既に「出た回数の多い順」**（`renderWorldConceptsTab`：`worldConceptCount(b) - worldConceptCount(a)`、同数は五十音）。全部「1件」に見えるのは件数がまだ少ないため。今回足すのは五十音順・新しい順への切替だけ。

---

## 1. 用語（terms）

### 1.1 概念と用語は別のもの

- **概念**＝積み上がって網になるもの（ホルムズ海峡、外貨準備、コンディショナリティ）
- **用語**＝読むための注。組織名・国名・略語・制度名（IMF、EIA、CPMI、エルサルバドル、EFF）

混ぜると概念一覧が固有名詞で埋まり、体系として機能しなくなる。**ファイルを分けて、構造で担保する**（1ファイル＋種別だと、どこか1か所の絞り忘れで概念一覧に IMF が並ぶ。世界を journal に入れなかったのと同じ判断）。

### 1.2 保存

```
world/terms.json
{ version: 1, updatedAt,
  terms: [{ id: 'wt_YYYYMMDD_連番',
            name, aka: [],           // 別名・訳語・英語名
            line: '',                // 20〜35字の注
            kind: 'org'|'place'|'scheme'|'person'|'',
            eventIds: [], firstEventId,
            createdAt, updatedAt,
            lastUsedAt: '',          // プロンプトに差し込む600件を切る順序
            seenAt: '' }],           // 一度でも注を開いた時刻（1.5節）
  dismissedPairs: [{a, b, at}] }
```

端末は `worldTerms`（kv）に全件を持つ。概念と同じく**署名が変わったときだけ**書く。
`world/index.json` に1行足す：`terms: { updatedAt, count }`。

**`line` は 20〜35字、かつ1つの節で言い切る**（v10.5.1）。

最初は 20〜40字にしたが、実機で回すと注が全部 40 字側に張り付き、1文の中に節が2つ入った（「〜が集まり、〜を作る」）。チップを押すたびにこの密度が出ると、結局「読みに行く」ことになる。注は読ませるものではなく、一瞬で片づけて本文に戻るためのもの。

このとき、それまでの良い例「加盟国が資金を出し合い、外貨が足りない国に融資する国際機関」自身が二段構えだったので差し替えた（規則と例が食い違うと、モデルは例のほうに従う）。

### 1.3 出来事側

```
ev.terms = ['IMF', 'エルサルバドル']   // 名前だけ。line はストアから引く
```

**`line` を出来事に複写しない。** 複写すると、統合や書き直しのあと古い注が出来事の側に残る。ストアは全件を端末に持つので常に引ける。

### 1.3.1 取り込み時に正式名へ寄せる

生成が「IMF」と書き、ストアの正式名が「国際通貨基金」（`aka` に IMF）だった場合、**取り込みの時点で `ev.terms` を「国際通貨基金」に書き換えて保存する**。見つからなければ新しい用語として登録する。

こうしておくと、あとで統合や名前変更をしたときに既存の出来事が自動で追従し、同じ用語がバラバラの綴りで表示されることもなくなる。

**概念側も同じ形に揃える**（`ev.concepts` / `ev.related` / `ev.premise.name` / `ev.premise.a` / `ev.premise.b`）。

現状（段階1）の概念は次のように動いている。

- 引き当ては `worldFindConcept(name)` が `name` と `aka` の両方を見るので、**注や網が壊れることはない**
- ただし出来事には生成が書いた綴りがそのまま残るので、**同じ概念が出来事ごとに違う綴りで表示される**
- 統合が出来事の綴りを寄せるのは「消えた側の**主名**と正規化して一致するもの」だけで、消えた側の `aka` の綴りで書かれていた出来事は寄らない

段階1.5 で、両方とも取り込み時に正式名へ寄せる。**既に取り込んである分は書き換えない**（`aka` 経由で解決するので壊れておらず、起動時に黙ってデータを書き換えるほうが危ない）。

### 1.4 本文中の検出

**fact には注を入れない。** fact は一覧の1行そのもので、タップ＝展開。ここにチップを置くと、指が語に当たったときだけ展開しない、という当たり外れの操作になる。

注が開くのは**展開したあとの `why` と `detail` の中だけ**。

検出は**表示時に名前で照合**する（生成に文字位置を持たせる案は、字数の切り詰めで位置がずれるうえモデルが文字数を数えられないので採らない）。事故を防ぐ縛りを5つ入れる。

1. 照合するのは**その出来事の `terms` に載っている語だけ**（ストア全体と照合しない）。名前と `aka` の両方を見る
2. **最長一致・重なりなし**（「エルサルバドル」が「サルバドル」に負けない）
3. **英数字の略語は前後が英数字でないときだけ**当てる（`EFF` が `EFFECT` に当たらない）
4. **日本語で2文字以下の語は本文中では当てない**（「円」が「円安」に、「米」が「米国債」に当たる事故）。チップ行からは開ける
5. **1つの語につき最初の1回だけ**（`why` と `detail` を通して1回。同じ語に5回下線が引かれると読めない）

`innerHTML` は使わない。テキストノードと `<button class="termChip">` を組み立てる。

### 1.5 チップ行と `seenAt`

premise カードのすぐ下に**「用語」の行**を置き、その出来事の terms を全部チップで並べる（fact にしか出てこない語、2文字以下で本文に当たらなかった語も、ここから開ける）。

チップを押すと、その場に注が開く（premise カードと同じ見た目）。

**`seenAt`**：一度開いた注は**下線を薄くする**。「既に知っていることは省く」がこの機能の背骨なので、画面の上でも同じ原則を働かせる。まだ開いていない語だけが目立つ。

`seenAt` を立てるときは端末に保存して dirty にするが、**自分から同期を呼ばない**（`scheduleSync()` しない）。注を開くたびに同期が走るのは割に合わないので、次の自然な同期に相乗りする。

### 1.6 概念が優先

同じ名前が両方に来ることがある。**概念が本体で、用語は注**という順序で解く。

- 用語として来た名前が**既に概念にある**ときは、用語を作らない。本文のチップはその概念の `line` を出し、押すと**概念の詳細へ飛ぶ**（注のポップアップではなく）。網が育つ方向
- 用語だった名前が**あとから `premise.kind: 'new'` として来た**ときは、概念を作り、**その用語をストアから消して概念に吸わせる**（`aka` を移す。統合の仕組みをそのまま使う）。同じ名前が両方に居座らない

### 1.7 一覧

**概念タブの中に `[概念] [用語]` の小さな切替を置く。** タブ行を4つ（出来事／概念／用語／予測）にはしない。「用語は読むための道具であって、体系ではない」という位置づけが画面に出るほうを採る。

用語の一覧は `kind` で畳める（組織／地名／制度／その他）。

用語の詳細は概念の詳細と同じ形（名前・別名・注・この用語が出た出来事・`[やさしく説明]`・`[名前を直す・まとめる]`）。ただし**「つながる概念」は持たない**（用語は網に参加しない）。

### 1.8 統合

**ストアを引数で受け取る形に共通化する。** いま `worldFindConcept` / `worldMergeCandidates` / `worldMergeConcepts` / `worldRenameConcept` は `wd.concepts.concepts` を直接見ている。これを

```js
const WORLD_STORES = {
  concept: { list, idPrefix: 'wc_', minLen: WORLD_CONCEPT_MIN_LEN, save, rewrite: 出来事の概念名を寄せる },
  term:    { list, idPrefix: 'wt_', minLen: WORLD_TERM_MIN_LEN,    save, rewrite: 出来事の ev.terms を寄せる },
};
```

にして、`worldFindIn(store, name)` / `worldMergeCandidatesIn(store, ids)` / `worldMergeIn(store, keep, drop)` / `worldRenameIn(store, id, name)` にする。丸ごとコピーして2本持つと、正規化のしきい値と「別のもの」の記憶を2か所で直す羽目になる。

分かれるのは**出来事側の綴りを寄せる1か所だけ**：

- 概念 → `e.concepts` / `e.related` / `e.premise.name` / `e.premise.a` / `e.premise.b`
- 用語 → `e.terms`

**`WORLD_TERM_MIN_LEN = 4`**（概念は3）。`IMF` は3文字なので、正規化後の「片方が片方を含む」規則にちょうど掛かり、`IMFC`（国際通貨金融委員会。実在する別組織）が統合候補に出る。押したら別組織が1つになる事故なので、略語の多い用語側は1文字上げる。

---

## 2. 定型プロンプト

### 2.1 出来事側

展開部のボタンは今 `[詳しく] [予測を書く] [Claude に聞く]` の3つ。ここに4つ足すと7ボタンになり、v9 の「情報が多すぎて読みに行ってしまう」の再演になる。

**`[Claude に聞く]` を押すと、その下に4つのチップが開く。**

```
[★] [詳しく] [予測] [Claude に聞く]
      ↓ 押すと
   [やさしく説明] [なぜ今なのか] [日本にどう効くか] [反対の見方は]
```

面倒の正体は「質問文を考えること」であって「1タップ増えること」ではないので、開いて選ぶ2タップでも工数はゼロのまま。既定の画面は静かに保てる。

押すと、その出来事の素材を差し込んだ**完成形がコピー**される（撮るのプロンプトコピーと同じ仕組み）。

### 2.2 素材の差し込み

4つとも共通で差し込むもの：`fact` / `eventDate` / `why` / `premise` / `detail` / `source` / `url` / 自分の予測（あれば `fork` と `pick` も）。

**terms の扱いだけ分かれる。**

| ボタン | terms を渡すか | 理由 |
|---|---|---|
| やさしく説明 | **渡さない** | 押すのは注を読んでもまだ分からなかったとき。既知として渡すと、分からなかったものを飛ばして説明される |
| なぜ今なのか | 渡さない | |
| 日本にどう効くか | 渡さない | |
| 反対の見方は | **渡す** | 反論は前提を突く。こちらが何を知っているかを渡さないと既知の説明から始まる |

### 2.3 4つとも入れる共通の縛り

生成プロンプトと同じ規律をここでも効かせる。一次ソースで固めた札から根拠のない話に流れ込むのが一番もったいない。

```
- web 検索を使ってよい。ただし出典のある事実と、推測・解釈をはっきり分けて書くこと。
  推測を書くときは「ここからは推測」と明示する。
- 見出し・箇条書き・太字を使わない。普通の文で書く。
- 前置きを書かない。最後に要約を繰り返さない。
- 最初から全部書かない。もっと知りたいところは私が続けて聞く。
```

### 2.3.1 長さ（v10.5.1）

実機で「やさしく説明」を回したら、非常に長い文章が返ってきて読む気が失せた。
この機能の目的は**隙間時間に情報を集めて体系的に理解すること**で、長い解説は隙間時間には読めないし、読んでも積み上がらない。

長さは `max` として定数側に持ち、組み立てのときに `- N字以内で書く。` の1行として差し込む（数字を1か所で直せるように）。

| ボタン | 長さ | 構成 |
|---|---|---|
| やさしく説明 | 300字 | ①一言で何が起きたか ②出てくる相手と、それぞれが何を欲しがっているか ③これが続くと何が変わるか |
| なぜ今なのか | 300字 | 何が引き金になったか。誰の都合で今このタイミングなのか |
| 日本にどう効くか | 300字 | どの経路で伝わるか。生活に届くまでに何段階あるか |
| 反対の見方は | 400字 | この見立てが前提にしていること。それが崩れる筋道 |
| 概念：やさしく説明 | 300字 | 一言で言い切ってから、なぜ問題になるか。身近な例を1つ |
| 概念：この概念を厚くする | 400字 | 誤解1つ・例外1つ・セットで覚える概念2つまで |

「反対の見方は」だけ長いのは、前提を示してから崩す必要があるため。

**「なぜ今なのか」から「次にどこを見ていれば分かるのか」を外した。** その役割は `fork` がすでに持っている（分かれ目を1つ立てて、それに対して3択を押す）。300字で4つは入らないので、重ねて聞かないほうを取る。足りないと感じたら、他を削るのではなく 400 字に上げる。

**「やさしく説明」の一言注は字数と衝突する。** 注だけで300字を使い切りかねないので、`添える一言は5〜10字程度にとどめ、注も含めて全体を字数に収めてください。` を入れてある。注の多い出来事では本文が短くなる形で自動的に釣り合う。

### 2.4 文面（`WORLD_ASK_PROMPTS`）

定数1か所にまとめる。私が直せるように、依頼文だけを持ち、素材の組み立ては共通の関数がやる。

```js
const WORLD_ASK_COMMON =
  '- web 検索を使ってよい。ただし出典のある事実と、推測・解釈をはっきり分けて書くこと。\n' +
  '  推測を書くときは「ここからは推測」と明示する。\n' +
  '- 見出し・箇条書き・太字を使わない。普通の文で書く。\n' +
  '- 前置きを書かない。最後に要約を繰り返さない。\n' +
  '- 最初から全部書かない。もっと知りたいところは私が続けて聞く。\n';

const WORLD_ASK_PROMPTS = [
  { key: 'plain', label: 'やさしく説明', terms: false, max: 300, body:
    'この出来事を、前提知識のない人に向けて説明してください。\n' +
    'まず一言で何が起きたか。次に、出てくる相手と、それぞれが何を欲しがっているか。\n' +
    '最後に、これが続くと何が変わるか。この順番で書いてください。\n' +
    '出てくる組織・国・制度・略語・人物は、名前を出した最初のところで一言ずつ添えてください。\n' +
    '添える一言は5〜10字程度にとどめ、注も含めて全体を字数に収めてください。\n' +
    '私はこの分野に詳しくありません。上の「前提」も、分かった気になっているだけかもしれません。\n' },

  // 「次にどこを見ていれば分かるか」は fork がその役割を持つので聞かない
  { key: 'why', label: 'なぜ今なのか', terms: false, max: 300, body:
    'なぜこれが今このタイミングで動いたのかを教えてください。\n' +
    '何が引き金になったか。そして、誰の都合で今なのか。\n' +
    '出来事の言い換えではなく、その裏で働いている力を知りたい。\n' },

  { key: 'japan', label: '日本にどう効くか', terms: false, max: 300, body:
    'これが日本にどう効くかを教えてください。\n' +
    'どの経路で伝わるか。私の生活に届くまでに何段階あるか。\n' +
    '効かないなら「効かない」と書いてください。無理につながりを作らないでください。\n' },

  // 前提を示してから崩す必要があるぶん、ここだけ長い
  { key: 'against', label: '反対の見方は', terms: true, max: 400, body:
    '上の見立てを疑う立場からの反論を書いてください。\n' +
    'まず、この見立てが前提にしていることを示してください。次に、それが崩れる筋道を書いてください。\n' +
    '私の予測が書いてあるときは、それが外れるとしたらどの前提が崩れるときかを書いてください。\n' +
    '私が既に知っていること（上に並べた用語）の説明はしないでください。\n' },
];

// 素材のあとに差し込む依頼文。長さの数字はここ1か所から出る。
function worldAskTail(ask) {
  return '\n' + ask.body + '- ' + ask.max + '字以内で書く。\n' + WORLD_ASK_COMMON;
}
```

### 2.5 概念・用語の詳細

概念詳細は全画面で余白があるので、**3つ直接並べる**（開く階層を作らない）。

`[やさしく説明] [この概念を厚くする] [名前を直す・まとめる]`

用語詳細は `[やさしく説明] [名前を直す・まとめる]` の2つ。

```js
const WORLD_CONCEPT_ASK_PROMPTS = [
  { key: 'plain', label: 'やさしく説明', max: 300, body:
    'この概念を、前提知識のない人に向けて説明してください。\n' +
    'まず一言で言い切ってから、なぜそれが問題になるのかを書いてください。\n' +
    '身近な例を1つだけ添えてください。たとえ話は1つまでにしてください。\n' },

  { key: 'deepen', label: 'この概念を厚くする', max: 400, body:
    'この概念について、上の理解の次に来るものを教えてください。\n' +
    'よくある誤解を1つ、例外や効かない場面を1つ、\n' +
    'そしてこの概念とセットで覚えるべき概念を2つまで挙げてください。\n' +
    '入門の説明の繰り返しは要りません。\n' },
];
```

---

## 3. fork と3択の予測

### 3.1 置き場所

**`fork` は生成が書くもの、`prediction` は自分が書くもの**なので、入れ物を分ける。同じ入れ物にすると、予測を消したとき fork まで消える。

```
ev.fork = { text: '…', type: 'level' | 'event' } | null

ev.prediction = {
  pick: '' | 'up' | 'down' | 'flat',   // 追加。3択
  text: '',                             // 自由記述（そのまま残す。任意）
  days, writtenDate, dueDate, setAt, history, result, judgedAt, judgeNote
}
```

### 3.2 ラベル

値は `up / down / flat` の3つで共通。**表示だけ `fork.type` で切り替える。**

| type | fork の例 | ボタン |
|---|---|---|
| `level` | ブレント原油が80ドル/バレルを割るか | 上 ／ 下 ／ 横ばい |
| `event` | IMF理事会が年内に承認するか | 起きる ／ 起きない ／ どちらとも |

### 3.3 空振りを許す

**`fork` は null でよい。** 3ヶ月で決着する分かれ目が思いつかない出来事は必ずある。無理に書かせると「原油価格は変動するか」のような無内容な fork が並ぶ（問いのときと同じ）。

`fork` が無い出来事は、**自由記述だけの今の形**に落ちる。

### 3.3.1 何を問うか（v10.5.1）

実機で回したら、fork が3件とも「来月発表される統計の数字が、この線を超えるか」という同じ形になった。これは**統計の数字を当てるゲーム**で、当たっても外れても世界の見方は変わらない。

向きを変える：**その出来事が言っていること自体が本当かを問う。**

| | 例 |
|---|---|
| 避ける形 | 10月末の米ディーゼル小売価格が1ガロン5.55ドルを超えるか |
| 目指す形 | 米政府の見通しどおり、10月にディーゼル在庫が1億バレルを割るか |

後者なら、外れたときに「政府の見立てが外れた」という意味が残る。当たり外れが理解につながる。

出典が予想を述べていない札（単なる決定・発表）もあるので、**「出典が予想を述べていないときは、その出来事が狙った効果が出るかを問う。それも立たなければ null」** という逃げ道を足してある。これが無いと、予想を含まない札で fork が立たず全部 null になる。

### 3.4 挙動が変わるところ

いまは `text` を書いた時点で `dueDate` が入る。今後は **`pick` を押しただけでも入る**（3択だけ押して一言を書かないのが既定の使い方になるため）。

```
書かれている = pick !== '' || text !== ''
→ writtenDate（初回のみ）と dueDate = writtenDate + days が入り、索引の due に載る
→ 両方空に戻したら dueDate を外し、索引からも落とす
```

`pick` の変更は `history` に行を作らない（`text` の書き直しだけが履歴に残る）。ただし `history` の行に、そのとき何を押していたかを添える：`{ text, at, pick }`。

### 3.5 機械で担保できないこと

「検証可能な fork だけを書かせる」はプロンプトで縛れるが、**取り込み時に機械で判定できない**。数字の有無は見られるが、`event` 型は数字を持たないので弾けない。

答え合わせのとき fork をそのまま出すので、曖昧なものは「保留」で流す運用になる。取り込む JSON は貼る前に目を通すので、そこで気づける。

### 3.6 一覧での見え方

予測が書かれた出来事の右端の印を、いまの「予測」から**押した向き**に変える（`↑` `↓` `→`）。`fork` があって未記入のものは何も出さない（催促しない）。

---

## 4. ★

**出来事そのものに持たせる。** 言葉側の journal picks とは完全に別。

```
ev.hit = 1        // 立っているときだけ存在する
ev.hitAt = ''
```

- **重みには一切使わない。** 世界の出来事は引く対象ではないので、`HIT_TUNING` には触れない
- 既存の `hitBtn`（☆/★）の見た目をそのまま使う
- 置き場所は展開部のボタン列の**左端**：`[★] [詳しく] [予測] [Claude に聞く]`
- 段階1.6 の痩せさせで、そのまま除外条件になる
- 記録カレンダーの日詳細に「★」の印、**月のまとめに「★ N件」**を足す

---

## 5. 概念一覧の並び替え

既定は今のまま（**出た回数の多い順**、同数は五十音）。切替を足す。

`[回数順] [五十音順] [新しい順]`

用語一覧にも同じ切替を出す。

---

## 6. 生成プロンプトの新しい版

`worldPromptText(field)` を差し替える。**変更点は3つ**：既知一覧が2本になる／`terms` が出力に増える／`fork` が出力に増える。

```
（このまま Claude のチャットに貼ってください。返事は JSON だけにしてください。）

あなたは、ある人の世界の見方を少しずつ更新するための材料係です。
解説記事は書きません。ニュースの要約もしません。
「これから効き続ける動き」だけを、決められた形で返します。

■ 必ず守ること
- web 検索を必ず使う。検索せずに書かない。
- 下の「ソース」に挙げたドメインで確認できた出来事だけを返す。
- 日付・数字は、出典ページに書かれているものだけを書く。
  推定値・概算を書くときは detail に「推定」と明記する。
- URL は実在するページのものだけ。開けるか確かめる。
  確かめられなければ、その出来事を返さない。
- 件数が足りなくてよい。埋めるくらいなら少ない方がよい。
  10件求めて4件しか確認できなければ4件を返す。
- 出来事以外の文章を返さない。JSON のみ。

■ 分野
金融・経済。中央銀行、財政、統計、債券・為替・商品の市場構造、規制。
株価の上下そのものは書かない。値動きの背後にある制度・制約を書く。
10件を目安に返す（確認できたものだけ）。

■ 期間
直近7日間に動いたもの。ただし「その日のニュース」ではなく、
これから数ヶ月〜数年にわたって効き続ける動きを選ぶ。
発表そのものより、発表が示している力学を選ぶ。

■ ソース：一次（fact・eventDate・detail の数字はここでしか確認できない）
- 日本銀行 https://www.boj.or.jp/
（設定の sources のうち role: primary で分野が一致するもの）

■ ソース：解釈（why を書くための補助）
- Bruegel https://www.bruegel.org/
（設定の sources のうち role: analysis で分野が一致するもの）

■ ソースの使い分け（厳守）
- fact・eventDate・detail の数字は、一次のソースで確認できたものだけを書く。
- 解釈のソースだけを根拠に出来事を立ててはいけない。
  シンクタンクの論考は「出来事」ではない。
- 解釈のソースは why を書くときにだけ使う。
  その場合も url と source には、一次のソース（出来事そのものの出所）を書く。

■ すでに知っている概念（説明しなくてよい）
イールドカーブ / シーレーン / 財政ファイナンス / ...
（最大400件。名前だけ）

■ すでに知っている用語（説明しなくてよい）
IMF / EIA / エルサルバドル / ...
（最大600件。名前だけ。一覧に無くても、経済・政治の基礎的な用語は説明不要）

■ それぞれの出来事に書くこと
- fact：何が起きたか。1文・40字以内。数字か固有名詞を必ず含む。
  聞く人に向けた文にする。専門用語を並べない。
  良い例：「中銀が国債買い入れの減額幅を縮めた」
  悪い例：「日銀が国債買入れオペにおける減額ペースの調整方針を公表」
- why：なぜ今動いたか。1〜2文。
  誰が何を欲しがり・何を恐れ・何に縛られているかを書く。
  出来事の言い換えを書かない。評価・感想・助言を書かない。

- premise：この件を理解するのに最低限必要な「概念」を 1つだけ。
  概念とは、積み上がって世界の見方になるもの（外貨準備、コンディショナリティ、
  シーレーン）。組織名・国名・略語は概念ではない。それは terms に書く。
  - 上の「すでに知っている概念」で足りるなら、新しい概念を作らない。
    そのときは kind を "relation" にして、既知の概念2つ（a と b）の
    関係を line に1文で書く。
  - 新しい概念が要るときは kind を "new" にして、name と line を書く。
    aka に別名・日本語訳・英語名を必ず入れる（表記ゆれを防ぐため）。
  - line は1文。その概念の定義であって、この出来事の説明ではない。

- terms：fact・why・detail の中に出てくる組織名・国名・略語・制度名を、
  出てきたぶんだけ全部挙げる。概念は書かない（それは premise）。
  - line は 20〜35字。1つの節で言い切る。
    注は読ませるものではない。一瞬で片づけて本文に戻るためのもの。
    「〜が集まり、〜を作る」のように節を二段重ねにしない。
    かといって「国際的な金融機関」のような、読んでも何も増えない書き方もしない。
    良い例：「外貨が足りない国に融資する、加盟国出資の国際機関」
    良い例：「世界の決済システムの共通ルールを作る、中央銀行の集まり」
    悪い例：「BISの下で各国の中央銀行が集まり、決済システムの国際基準を作る委員会」
    悪い例：「国際通貨基金のこと」「中米の国」
  - line はその語の定義であって、この出来事の説明ではない。
  - kind は org（組織）／place（国・地域・海峡など）／scheme（制度・枠組み）
    ／person（人物）のどれか。
  - person の注は、**肩書きと立場だけ**を書く。
    評価・来歴・人物像を書かない（「〜で知られる」「〜と対立している」は不可）。
    人は入れ替わるので、**いつ時点の肩書きかを必ず含める**。
    良い例：「2026年時点の米財務長官。国債の発行計画を決める側」
    悪い例：「著名な投資家出身で、市場との対話を重視することで知られる」
  - aka に別名・日本語訳・英語名を入れる（IMF なら 国際通貨基金、
    International Monetary Fund）。
  - **すでに知っている用語も、本文に出てきたら name だけは必ず書く。
    その場合 line・kind・aka は空でよい。**（読むときの目印に使うため）
  - 本文に出てこない語は書かない。

- fork：**おおむね3ヶ月で決着する**「分かれ目」を1つ。検証可能なものだけ。
  - **その出来事が言っていること自体が本当かを問う形にする。**
    出典が予想・見通し・想定を述べているときは、その予想が当たるかどうかを
    fork にする。外れたときに「その見立てが外れた」という意味が残るため。
  - **統計の次の1回の数値を当てる形にしない。** 数字を当てても、
    当たっても外れても世界の見方は変わらない。
    良い例：「米政府の見通しどおり、10月にディーゼル在庫が1億バレルを割るか」
    悪い例：「10月末の米ディーゼル小売価格が1ガロン5.55ドルを超えるか」
  - 出典が予想を述べていないときは、その出来事が狙った効果が出るかを問う。
    それも立たなければ null にする。
  - type が "level" のとき：示された水準に届くか（text の例
    「政府の見通しどおり在庫が1億バレルを割るか」）
  - type が "event" のとき：起きるか起きないかがはっきりする事実（text の例
    「IMF理事会が年内に承認するか」）
  - **3ヶ月では決着しないものを書かない。** 1年かけて決まることを書かれると、
    どう転んでも「どちらとも」にしかならない。
  - **思いつかなければ null にする。** 無理に作らない。
    「原油価格は変動するか」のような、どちらにも転ばない書き方は禁止。
  - 誰がどう見ても同じ判定になる書き方にする。
    「市場が落ち着くか」「関係が改善するか」は書かない。

- detail：数字・当事者・背景。1〜3文。ここには専門用語を書いてよい。
- source：媒体名と発表日。
- url：出典ページ。
- eventDate：出典ページに書かれた発表日または発生日（YYYY-MM-DD）。未来の日付にしない。
- concepts：この出来事が触れている概念名（既知・新規とも）。
- related：premise の概念と、つながりの深い概念名（既知の一覧から。最大3つ）。

■ 並べ方
重要だと思う順に並べる。上から読まれる。

■ 出力形式
{"events":[{
  "fact":"...",
  "why":"...",
  "premise":{"kind":"new","name":"...","aka":["...","..."],"line":"..."},
  "terms":[{"name":"IMF","line":"...","kind":"org","aka":["国際通貨基金"]},
           {"name":"エルサルバドル","line":"...","kind":"place","aka":[]}],
  "fork":{"text":"...","type":"level"},
  "detail":"...",
  "source":"...",
  "url":"https://...",
  "eventDate":"YYYY-MM-DD",
  "concepts":["..."],
  "related":["..."]
}]}

kind が "relation" のときの premise：
  {"kind":"relation","a":"既知の概念1","b":"既知の概念2","line":"..."}
すでに知っている用語：
  {"name":"IMF"}
分かれ目が思いつかないとき：
  "fork": null
```

### 6.1 なぜ premise の説明を書き足したか

段階1の版は「必要な概念を1つ」としか言っていない。terms を足すと、**組織名を premise に書いてしまう**経路ができる（「IMF」を premise の概念として立てる）。概念と用語の線引きを生成の側にも明示しないと、概念一覧が固有名詞で埋まる。

---

## 7. 取り込みの変更

追記のみ・重複弾き・落とす条件は**そのまま**。足すのは3つ。

1. `terms` を読んで用語ストアに起こす／既存につなぐ（概念と同じ手順、1.6 の優先順位つき）
2. `ev.terms` と概念名を**正式名へ寄せて保存**する（1.3.1）
3. `fork` を読む。`text` が空、または `type` が `level`/`event` でなければ `null` にする
4. 結果表示に「新しい用語 N件」と、用語側の統合候補を足す

**統合候補の画面は概念と用語で共通**（1つのリストに `[概念] [用語]` の別を添える）。

---

## 8. 定数（追加分）

```js
const WORLD_TERM_MIN_LEN     = 4;    // 用語の「片方が片方を含む」を使う最短の長さ（概念は3）
const WORLD_KNOWN_TERMS_MAX  = 600;  // プロンプトに差し込む既知用語の上限
const WORLD_TERM_LINE_MIN    = 20;   // 注の長さの目安（プロンプトに書く数字）
const WORLD_TERM_LINE_MAX    = 35;
const WORLD_TERM_BODY_MIN_LEN = 3;   // 本文中で当てにいく日本語の最短の長さ
const WORLD_TERM_KINDS       = ['org', 'place', 'scheme', 'person'];
const WORLD_TERM_KIND_LABEL  = { org: '組織', place: '地名', scheme: '制度', person: '人物', '': 'その他' };
const WORLD_FORK_DAYS        = 90;   // fork が決着する時間軸（プロンプトに書く数字）
const WORLD_FORK_TYPES       = ['level', 'event'];
const WORLD_PICK_LABEL = {
  level: { up: '上', down: '下', flat: '横ばい' },
  event: { up: '起きる', down: '起きない', flat: 'どちらとも' },
};
const WORLD_CONCEPT_SORTS = ['count', 'kana', 'new'];
// 文面は WORLD_ASK_COMMON / WORLD_ASK_PROMPTS / WORLD_CONCEPT_ASK_PROMPTS（2章）
```

---

## 9. 守ること

- 用語と概念を**ファイルの時点で分ける**。表示のフィルタに頼らない
- `e.terms` は名前だけ。`line` を出来事に複写しない
- fact に注を入れない（一覧の1行＝展開の引き金を壊さない）
- 世界の ★ は重みに使わない。`HIT_TUNING` に触れない
- 取り込みは追記のみ。段階1から変えない
- 既存の言葉・引く・相談・問い・撮る・筋トレ・散歩に影響を出さない
- 文面（プロンプト4種＋概念2種＋生成2本）と数字は定数にまとめる
- API は使わない。既知判定・重複判定・表記ゆれ判定はすべて端末側の文字比較

---

## 10. 段階1.6（痩せさせ・別途）

- `WORLD_SLIM_MONTHS`（既定12）より古い月を全部読み込む
- 対象＝**予測なし（`pick` も `text` も空）・★なし・`slim` なし**
- **件数だけでなく、対象の `fact` を1行ずつ全部出してから**押させる
- 押したら `why = ''` / `detail = ''` / `slim = 1`、`updatedAt` を更新して通常の同期に乗せる
- **戻せない。** 復元は kotoba-data の git 履歴（押した日のコミットに全文がある）
- 概念・用語は絶対に痩せさせない
- `slim` の出来事には「この出来事は要約だけ残しています」と1行出す

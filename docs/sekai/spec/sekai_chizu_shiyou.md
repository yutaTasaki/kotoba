# 一万日 v20 仕様書 — 世界：お金の流れを地図で見る

回答書 `sekai_chizu_kaitousho.md` で確定した方針の仕様書です。**まだ実装していません。** 見てもらってから実装に入ります。

地図の部品・世界地図のデータ・換算レートの API は、2026-09-11 に実際に取って確かめました。Yuta さんの実データの数字は、回答書1章の内訳（お金つき13件：国をまたぐ4・国内6・場所が定まらない3）を使っています。

---

## 0. 全体の形

```
世界の画面の一番上：小さな地図（直近7日の線）……押すと大きな地図の画面へ（M5）
大きな地図の画面：直近90日の線。国の点を押すと、その国の網へ（M1・M4）
  線の太さ＝ドル換算（推定）の5段（M3）。物・人・換算できないものは細い点線
  地図の下に3つの枠：市場／国際機関／地域（場所の無い相手の置き場）
国の網：その国の主体を円に並べ、お金の動きを矢印で結ぶ（M4）
初期投入：地域ごとに4回のプロンプト。今の取り込みの形のまま（M2）
```

**段階と削る順**：M1 → M2 → M3 → M4 → M5。間に合わないときは M5 → M4 → M3 の順に削ります。**M1 と M2 は削りません**（国をまたぐ線は今4本しかなく、うち3本がウクライナ支援に集中しているため。回答書1章）。

**変わらない原則**：元の金額（`amount`）は書き換えない。ドル換算は推定として別の欄に持ち、線の太さにだけ使い、合計には使わない。データは追記のみ。url の無い値は取り込まない。中継サーバーを作らない。自動実行しない。

---

## 1. M1：主体に国を付け、大きな地図を描く（v20.0。実機で見た直し4点は v20.1）

### 1.1 データ：主体に `country`

主体（`actors.json` の各主体）に1つ欄を足します。

```
country: 'JPN'    // 3文字の国コード（ISO 3166-1）
                  // または 'MKT'（市場）・'INT'（国際機関）・'REG'（地域・集まり）
                  // 空は「まだ分からない」
countrySetAt: ''  // 一度でも決めた印（周期の cadenceSetAt と同じ考え方。自分で直したものを既定で上書きしない）
```

### 1.2 国の付け方（上から順に当てる）

| 順 | 相手 | 付け方 |
|---|---|---|
| 1 | 種の主体21件 | 決まった表で付ける（下の表）。起動時、`countrySetAt` が空のものだけ |
| 2 | これから入る主体 | 取り込みのプロンプトの actors に `country` を足して、生成に書かせる（分からなければ空） |
| 3 | 国が空の既存の主体 | **別名の表（付録A）に載っている呼び名と、名前が丸ごと一致したときだけ**、その国。頭の一致では当てない（「中東の産油国」が「中」で中国に当たる、を防ぐ）。外れたら空のまま（指示書 sekai_chizu_jisso_shiji.md 2-B） |
| 4 | お金の出す側・受ける側が、主体ではなく国名そのもの（「米国」「カナダ」） | 同じく、別名の表と丸ごと一致したときだけ |
| 5 | どれにも当たらない | 「場所不明」。地図に出さず、地図の下に「場所不明 N件」と出して、押すと一覧。**間違った国に当てるより、空のままの方がよい** |

順3・順4は、表示のたびに当てるだけで、主体には書き込みません（書き込むのは、種の主体の表と、手で直したとき、取り込みで生成が国を書いたときだけ）。

**M1 の実装前に決めたこと（回答書 sekai_chizu_m1_kaitou.md）**

- **後ろの決まった言葉を外してから丸ごと一致**：名前の後ろが「人民銀行・準備銀行・中央銀行・政府・政権・財務省・議会・国防省」のとき、それを外した残りが別名の表と丸ごと一致したときだけ、その国（「イスラエル政府」→ イスラエル、「中国人民銀行」→ 中国）。頭の一致は今までどおり使わない（v20.2.1 の「頭の国名」は、決まった言葉の前が丸ごと一致したときだけに限る）。
- **一般名の相手は、もう片方の国に置く（国内）**：丸ごと一致は「金融機関・銀行・株主・企業・家計・投資家」、含むは「保有者・支払い先・受給者」。付録B の「保有者＝市場の枠」より先に当てる。**基準は「その相手は、もう片方と同じ国にいると言い切れるか」**。もう片方が枠（市場・国際機関・地域）や場所不明なら、言い切れないので国内に寄せず、場所不明にする。
- **「買い手」「購入者」は一般名にしない。市場の枠に置く**（「ロシア産原油の買い手 → ロシア政府」を国内にすると、国外から入ったお金が地図から消えるため）。
- **国の点を押したら（M4 まで）**：その国が絡む直近90日の札を一覧で出す。M4 で国の網に置き換える。
- **線と国の点が重なる所（v20.3.2、指示書 sekai_sen_kasanari_shiji.md）**：押した所から22px以内の国の点と線（枠への線を含む）をまとめて拾う。1つならすぐ開き、2つ以上なら地図のすぐ下に「この辺りの線・国（◯◯の近く）」の一覧を出す。国が先、線は近い順で「相手・額・日付」の1行。8本を超えたら8本と「ほかN本を一覧で見る」（近くの線の札をまとめて開く）。見出しの国は、押した所が国の形の中ならその国、海の上なら近い国の点、どちらも無ければ付けない。線の並べ方（見た目）は変えない。
- **国が空の既存の主体にも、取り込みで生成が書いた国を入れる**：空のときだけ。自分で直した国（と種の表の国）は上書きしない。
- 当て方の順：主体の国 → 名前（と別名）が別名の表と丸ごと一致 → 後ろの言葉を外して丸ごと一致 → 一般名 → 枠の丸ごと一致 → 枠の「含む」 → 頭の国名（v20.2.1）→ 場所不明。

**v20.2.1 で足したこと（指示書 sekai_bashofumei_naoshi_shiji.md）**

- **頭の国名**：名前の中の「の・向け・産・系・国内」の前が、別名の表の国と丸ごと一致したら、その国（「米国の製油所」→ 米国、「ウクライナ向け防衛調達」→ ウクライナ）。長い方から試す。枠の言葉より後に当てるので、「ロシアの同盟国」「中東の産油国」は地域の枠のまま。**その端の場所を決めるだけで、向きは from／to の位置のまま**（国名から向きを推し量らない）。
- **プロンプトの from／to**：「主体名か、国名で始まる相手（米国の製油所）」と書かせる。国の定まらない相手（世界の海運会社・防衛装備の供給元）には国名を付け足させない。これらは場所不明のまま一覧で見る（両側の国を書かせる遡及はしない）。毎朝と初期投入の両方。
- **既知アクターの並べ方**：「名前（別名）」をやめて「名前［別名：…］」にし、「先頭の名前だけを書く」と添える（生成が「欧州委員会（EU／欧州連合）」を丸ごと名前にして、重複の主体が11件できたため）。
- **取り込み**：名前の後ろの括弧（（…）・［…］）を外した名前でも既存の主体を探す。
- **重複の主体**：名前の後ろに括弧があり、外した名前の主体が別にあるものは、手の統合と同じ仕組みで元の主体へまとめる（消える側の名前は別名に残り、札のつながりは足し合わせる）。起動時と、同期で主体の表を合わせた後に走らせる（主体の表は消した印を持たないため、別の端末の分が戻ってくる）。

**種の主体21件の割り当て**

| 国 | 主体 |
|---|---|
| JPN | 財務省・日本銀行・日本政府 |
| USA | FRB・米財務省・米政権・米議会 |
| CHN | 中国人民銀行・中国政府 |
| IRN・RUS・UKR・PRK | イラン政府・ロシア政府・ウクライナ政府・北朝鮮 |
| REG（地域：EU） | 欧州委員会・ECB |
| INT（国際機関） | IMF・BIS・WTO・NATO・国連・OPEC＋ |

**手で直す**：主体の詳細の画面に「国：日本（直す）」の1行を足します。押すと国名を打つ欄が開き、辞書で引いて決めます（`countrySetAt` を入れる）。

### 1.3 国の表（アプリの中に持つ）

- **番号と3文字コードの対応表**（250行）：世界地図のデータは国を数字の番号（日本は392）で持っているので、3文字コード（JPN）との対応が要ります。公開データ（i18n-iso-countries の codes.json）から作ります。
- **日本語の国名**：同じ公開データの日本語名を使います。ただし**正式名で入っている**ので（アメリカ合衆国・中華人民共和国・ロシア連邦・イラン・イスラム共和国）、**報道の呼び方の別名を手で足します**（米国・米・アメリカ／中国／ロシア／イラン／英国・イギリス／韓国／北朝鮮／台湾／ドイツ・独／フランス・仏 など40ほど）。
- 合わせて約18KB。`assets/world_countries.json` に置きます。
- 揉めている境界・国名（台湾・コソボ・西サハラなど）は、**世界地図のデータの形のまま使い、名前は中立に書きます**（回答書 10章-8）。

### 1.4 場所の無い相手の置き場

地図の下に、3つの枠を固定で置きます（本部の場所には置かない）。

| 枠 | 入るもの |
|---|---|
| 市場（MKT） | 為替市場・金市場・世界の石油市場・「ロシア産原油の買い手」など（「国債の保有者」は一般名として国内に寄せる） |
| 国際機関（INT） | IMF・世界銀行・BIS・WTO・NATO・国連・OPEC＋ |
| 地域（REG） | EU（欧州委員会・ECB）・「欧州の同盟国とカナダ」・「中東の産油国」などの国の集まり |

市場・国際機関・地域の判定は、1.2 の表と、付録B の名前の決め方で決めます。当たらなければ「場所不明」です（例：「TSMC → 先端工程と海外工場」の受ける側。指示書 1-1 のとおり、M1 では from／to に国を書かせない。v20.2.1 から、相手は国名で始めるよう指示する。国の定まらない相手は場所不明のまま）。

- **EU は「地域」の枠**（指示書 1-2。ブリュッセルの点に置くとベルギーの動きと混ざるため）。
- **「欧州の同盟国とカナダ」などの国の集まりも「地域」の枠**（指示書 1-3）。

### 1.5 地図の部品（リポジトリに置く）

| ファイル | 大きさ | 転送 |
|---|---|---|
| `assets/d3-geo.min.js`（d3-geo 3） | 36KB | 13KB |
| `assets/d3-array.min.js`（d3-array 3。d3-geo が中で使う） | 17KB | 6KB |
| `assets/topojson-client.min.js`（topojson-client 3） | 7KB | 2.6KB |
| `assets/countries-110m.json`（world-atlas 2 の粗い版。177か国） | 108KB | 39KB |
| `assets/world_countries.json`（1.3 の国の表） | 約18KB | 約7KB |

- **地図の画面を初めて開いたときに読みます**（筋肉の画面で three.js を読むのと同じ形）。アプリと同じ場所から来るので、アプリが開けるなら地図も開けます。
- 版は固定し、ファイルの頭に「どこの何版か」を1行書きます。

### 1.6 国の点（押す場所）の置き方

実際に全177か国で計算しました。
- 国全体の重心を使うと、13か国の点が国の外に落ちます（日本・フランス・ノルウェー・インドネシアなど）。
- **いちばん大きな島（多角形）の重心**を使うと、9か国は直ります。
- **それでも4か国は外に落ちます：イスラエル・ベトナム・クロアチア・ハイチ**（細長い・曲がった形）。これらは手で位置（経度・緯度）を持ちます。**イスラエルは実データの動き（米政権 → イスラエル政府）の行き先**なので、必ず直します。
- **粗い版の地図に無い小さな場所は、点だけを足します**（手で位置を持つ表）：シンガポール・香港・マカオ・ケイマン諸島・バミューダ・バーレーン・マルタ・ジャージー・ガーンジー・モーリシャス・リヒテンシュタイン・モナコ。

### 1.7 大きな地図の画面

- 世界の画面に、新しい画面（`view: 'map'`）を1つ足します。M5 までは、世界の画面の「お金の流れ」の見出しの横の「地図」から入ります。
- **図法**：Equal Earth（面積が正しく、形の歪みが小さい）。幅360なら高さは **178px** です（実測）。
- **地域に寄せるボタン**：全体・米州・欧州・中東・アジア。押すとその地域に合わせて拡大します（拡大は1倍に戻る）。
- **拡大・縮小（v20.1 で足した。指示書 sekai_chizu_naoshi_shiji.md 3。「作らない」の判断は取り消し）**：二本指でつまむと拡大・縮小（1〜8倍）。拡大しているときは、一本指でなぞると地図が動く。拡大していないときの一本指は、今までどおりページのスクロール。PC はホイール。**拡大しても線の太さと点の大きさは変えない**（国の形だけを変形し、線と点はその場所に描き直す）。指を6px以上動かした後の指の離しは、押したと見なさない。部品は足さない（d3-zoom とそれが頼る8つで 65KB・転送 24KB になるため、アプリの中で作った）。「この辺りの国」は残す（拡大しないで押したときに要るため）。
- **地図の範囲の外は描かない（v20.1.1。指示書 sekai_chizu_hamidashi_shiji.md）**：国の形・線・点・矢印は、地図の四角（入れ子の svg）の中だけに描く。下の枠へ向かう線は、地図の上端から枠の上端までの四角の中だけ。下の3つの枠はいちばん上に描く。<g> に clipPath を付ける切り取りは、iPhone の Safari では拡大（変形）と組み合わさると効かず、地図が下の枠に重なった。動かせる範囲は、拡大した地図が地図の四角を必ず覆うところまで（地図が消える所までは動かせない）。
- **点を押す**：押した所から22px以内にある一番近い国の点を選びます。**欧州の点どうしは、全体の地図で1.5px、「欧州」に寄せても7.2px しか離れていません**（ベルギーとルクセンブルク、実測）。指では押し分けられないので、**22px以内に国が2つ以上あるときは、「この辺りの国」を短い一覧で出して選ばせます**。
- **描く期間**：直近90日（回答書 10章-4）。端末に読み込んでいない月があれば、開いたときに読み込みます。
- **描く動き**：
  - 両端の国が決まった動きだけを線にします。同じ国の中の動きは線にせず、点の横に「国内 N件」と出します。
  - 確度3以下は出しません（既定の画面と同じ）。
  - v19.15 のまとめ方（同じ動きは1本）を先に当てます。
- **線**：
  - 出す側の点から受ける側の点へ、少し曲げた線を引き、受ける側に矢印を付けます。
  - 場所の無い相手は、下の3つの枠へ線を引きます。
  - M1 では太さは固定です（M3 で5段にする）。
  - 物・人の動きは細い点線（回答書 10章-5）。累計（span がある）は破線。
  - **同じ国の組に何本もあるときは、足さずに少しずつずらして並べます。太い順に5本まで描き、6本目からは「ほか◯本」と添えます**（回答書 10章-3）。
- **線を押す**：その動きの札へ行きます。まとめた行（2つの出典）なら、2枚を並べて出します。
- **場所不明**：地図の下に「場所不明 N件」。押すと、その動きの一覧を出します。
- **枠（v20.1 で直した。指示書 sekai_chizu_naoshi_shiji.md 1・2）**：枠の上の行に枠の名前と本数、下の行に**中に入っている相手の名前**（例：「地域（1本）／NATO加盟国」「市場（2本）／為替市場・金市場」）。入りきらなければ「為替市場 ほか1」、名前が長ければ切る。**枠を押すと、その枠が絡む直近90日の札を一覧で出す**（国の点を押したときと同じ）。中が空の枠は押せない見た目にする。

### 1.8 あわせて入れる直し（回答書4章）

**鎖に出すのは、まとめた後に2件以上ある塊だけにします。** 1件になった塊（今の「米国を中心に 1件」「金市場を中心に 1件」）は、「お金の動き（単発）」へ回します。

- **気をつける点**：いまの単発は「お金」の動きだけを並べています。報復関税・金の買い入れは［物］なので、そのまま回すと既定の画面から消えます。**鎖から回ってきたものは、物・人でも単発に出します**（［物］の印つき）。
- 鎖の本数は「2件以上の塊」の数になるので、9/18 の見直しの判断に使えます。

---

## 2. M2：初期投入（v20.2）

### 2.1 形
- **新しい取り込みの仕組みは作りません。** 今の取り込みの関数（`{"events":[…]}`）をそのまま通します。url の無い札・重複・未来の日付は、今までどおり落ちます。
- 取り込む画面に「初期投入」の欄を足し、地域ごとに4つの「プロンプトをコピー」を置きます：**米州／欧州・ロシア／中東・アフリカ／アジア・太平洋**。
- 返ってくる JSON の一番上に `"seed": "americas"` のような地域の印を書かせます。**取り込みは、この印がある JSON の札に `seed` を付けます。** コピーしたボタンを端末に覚えておく作りにしないのは、初期投入のプロンプトをコピーした後に、毎朝の JSON を貼る取り違えが起きうるためです。

### 2.2 プロンプトの中身
- **集めるもの**：直近90日に、国をまたいで大きなお金が動いた出来事。1回に12件まで。
- **主体**：政府・中央銀行・財務省・政府系ファンド・大企業・集まり。**個人は、公職として動いた人だけ**（私人は入れない）。
- **国**：主体に `country`（3文字コード）を必ず付けさせる。市場・国際機関・地域は MKT・INT・REG。
- それ以外の規則（探し方・確度・wants・追えた割合・下限の規則）は、毎朝のプロンプトと同じ文を使います。
- **字数**：毎朝のプロンプトから「今日の数字」「毎回必ず見るもの」「よく使う出典」を外すので、毎朝より短くなります。~~12,000字以内~~ は根拠の無い数だったので外しました（v20.2.2、指示書 sekai_jisuu_naoshi_shiji.md）。本物のデータ（概念38件）で約14,900字。字数の決まりは「伸びる節に上限を付けて、いちばん長いときが分かる形にする」です（既知の概念は最近使った120件まで。アクターは60件まで。最近のお金の動きは30件まで。地層・出典は手で足したときだけ増えるので上限なし）。**「最近のお金の動き」と「地層の一覧」は初期投入でも外しません**（初期投入で本物の鎖ができたのはこの節のため）。

### 2.3 初期投入の札の扱い（`seed`）
- `seed` の札は、**未読にも「今日読んだ札」にも数えません**（朝の一覧を埋めないため）。
- 地図・お金の一覧・探す・鎖には出します。
- 「最近のお金の動き」（毎朝のプロンプトの末尾30件）にも入れます。鎖づくりの材料が増えるのは狙いどおりです（回答書 10章-10）。
- **何度でも流せる（指示書 sekai_m2_shiji.md で決めた）**：地図が寂しいと感じたときに材料を足せるように、同じ地域を何度でも流せる。重複は今の取り込みの関数が弾く。ボタンは流した後も押せるまま。
- どの地域をいつ流したかは、`world/index.json` に `seeds: { americas: { at: '2026-09-12', n: 2 }, … }` として残す（最後に流した日と回数）。両端末で合わせるときは、地域ごとに新しい日付と大きい回数を採る。取り込む画面に「米州 9/12（2回目）」のように出す。
- 2回目以降も、返ってくる JSON の `seed` の印で同じように札に `seed` を付ける。
- 初期投入の JSON を取り込んだときは、朝の「今日の取り込み」の最後に取り込んだ日（`pulls`）を進めない（必ず見るものを見ていないため）。取り込みの結果の1行の「必ず見るもの：…」も出さない。
- 月の索引の「未読の数」にも `seed` の札は数えない。

---

## 3. M3：ドル換算と線の太さ（v20.3）

### 3.1 金額を読む
**いまの金額を読む関数は、英語の書き方を読めません**（実測）。

| 書き方 | いまの関数 |
|---|---|
| 276億ドル・15兆3993億円・7000億元 | 読める |
| €70 billion・EUR 90 billion・$3.3 billion・£12 billion・CNY 50 billion | **読めない** |
| 3,500百万ドル・100億ドル超 | **読めない** |

国をまたぐ4本のうち3本（ウクライナ支援の2本と、米政権 → イスラエル政府）が英語の書き方です。**このままでは太さがほとんど付きません。**

- **ドル換算の専用の読み方を足します**（`worldParseMoney`）。今の関数はお金の一覧の通貨分けで使っているので、変えずに残します。
- 読むもの：
  - 通貨の記号と3文字コード（$・€・£・¥・USD・EUR・GBP・JPY・CNY・RMB・CAD・CHF・KRW・INR・RUB・SAR など）
  - 英語の桁（thousand・million・billion・trillion、k・m・bn・tn）と日本語の桁（万・億・兆・百万）
  - 「超」「以上」「約」「over」「more than」は、読めるが**「以上」の印**を付けます。太さには使い、画面では「100億ドル超（推定）」と元の書き方を添えます。
- 読めないもの（オンス・バレル・トン・人）は、換算しません。

**書き方ごとの読み方（v20.3 で確定。指示書 sekai_m3_kakutei_shiji.md）**

| # | 書き方 | 読み方 |
|---|---|---|
| 1 | 括弧の中のドル（「1,004億ペソ（57億ドル）」） | 換算せず、括弧の中のドルを使う。レートは1、出どころは「出典の書き方」。括弧の中がドル以外のとき・言葉だけの括弧（「（推定）」）は読まず、外側を読む |
| 2 | 幅（「600〜640億ドル」「80億〜110億ドル」） | 小さい方を使い、「以上」の印を付ける |
| 3 | 期間あたり（「月2.5兆円程度」「年80億〜110億ドル」） | 書かれた数のまま換算し、掛け算しない。**線は累計（span）と同じ破線**にする（1回に動いた額に見せない） |
| 4 | 上限（「最大10億ドル」） | その値を使い、「約」の印を付ける |
| 5 | どの国の通貨か決まらない名前（ペソ・ルピー・ディナール・フラン。同じ理由でリヤル・リアル・ディルハム・クローナ・クローネも） | 3文字コードか括弧のドルが無ければ換算しない（細い点線） |
| 6 | 物の動きにドルが付いているもの（「276億ドル」の物） | 換算しない。物の動きなので細い点線のまま |

- 通貨の名前は長い方から先に当てる（「台湾ドル」「香港ドル」「豪ドル」「カナダドル」を米ドルにしない）。
- 金額の後ろに続く言葉は読まない（「$3.3 billion in FMF」→ 33億ドル）。
- ドル建ての金額は換算しない（レート1、出どころは「ドル建て」）。
- 地図の下の1行の「換算できない」の数には、物・人と、通貨が決まらないもの・読めない書き方を入れる。金額が空の札は入れない。
- 米財務省のレートは、四半期の途中で直しが出ることがある（ルーブルなど）。効き始めの日（effective_date）が動きの日以前で一番新しい値を使う。

### 3.2 換算（端末がやる）
- **ECB の参照レート**（毎日・鍵なし・ブラウザから直接）で、動きの日（`eventDate`）の値を使います。その日が無ければ、7日前まで遡って一番近い日の値を使います。**円 → ドルは、ユーロを挟んで割ります**（9/10：179.09 ÷ 1.1616 ＝ 1ドル 154.2円）。
- ECB に無い通貨は、**米財務省の為替レート**（四半期ごと・約170通貨）の、動きの日以前で一番近い四半期末の値を使います。
- どちらにも無ければ換算しません（細い点線）。
- レートは端末に覚えておき（日付と通貨ごと）、同じ日のレートを何度も取りに行きません。

### 3.3 持ち方（札に保存する。回答書3章）
```
usd: { value: 3300000000, approx: false,
       rate: 1, rateDate: '2026-09-08', source: 'ECB 参照レート',
       url: 'https://www.ecb.europa.eu/stats/policy_and_exchange_rates/euro_reference_exchange_rates/html/index.en.html',
       by: '端末', at: '2026-09-12' }
```
- 元の金額（`amount`）は書き換えません。
- **換算するのは端末だけ**です。生成には書かせません。生成が書いた `usd` があっても、取り込みで捨てます。
- 米財務省のレートを使ったときの `url` は `https://fiscaldata.treasury.gov/datasets/treasury-reporting-rates-exchange/treasury-reporting-rates-of-exchange`（開けることを確かめた）。
- **過去の札も、地図を開いたときに端末が埋めます**（`usd` が無く、換算できる札だけ）。埋めたら札を保存して同期します。
- **換算したら、地図の下に1行出します**（指示書 2-A。黙って書き換えないため）：「12件を換算しました（ECB 9/10）」。換算できなかった札がある日は、その数も添えます：「12件を換算しました／6件は換算できません（オンス・バレル・人）」。ボタンは足しません。換算する札が0件の日は、この行を出しません。
- 画面では「約◯億ドル（推定・ECB 9/10）」とだけ出し、**合計には使いません**。

### 3.4 太さ（対数の5段。回答書のとおり）

| 段 | ドル換算（推定） | 太さ |
|---|---|---|
| 1 | 1億ドル未満 | 1px |
| 2 | 1〜10億ドル | 2px |
| 3 | 10〜100億ドル | 3.5px |
| 4 | 100〜1,000億ドル | 5px |
| 5 | 1,000億ドル以上 | 7px |

- 追えた割合（total・share）がある動きは、`amount`（行き先が分かった額）で決めます（回答書 10章-6）。
- 地図の隅に、5段の見本を小さく出します。

---

## 4. M4：国の網（v20.4）

- 大きな地図で国の点を押すと、その国の網の画面（`view: 'countryNet'`）が開きます。
- **点**：その国の主体のうち、直近90日のお金の動きに出たもの。**円の上に等間隔**で並べます。並び順は名前順で固定なので、毎回同じ場所に出ます。
- **矢印**：その国の中のお金の動き（出す側 → 受ける側）。太さは M3 の5段、物・人は点線です。
- **共起は描きません**（回答書 2章-6：矢印＝お金だけ）。
- **国の外への動き**：円の外側に「→ カナダ」「← 米国」「→ 市場」のような札を置き、矢印を向けます。国の札を押すと、その国の網へ移ります。
- 主体の点を押すと、主体の詳細へ行きます。矢印を押すと、その札へ行きます（2つの出典なら2枚）。
- 言葉の網の画面（`screen-graph`）の「拡大縮小と、押すと札が出る」作りを流用します。

**v20.4 で確定したこと（指示書 sekai_m4_kakutei_shiji.md）**

| # | 中身 | 確定 |
|---|---|---|
| 1 | 主体の表に無い名前（「株主」「米国の製油所」など） | 点にする。主体は塗った点、表に無い名前は白抜きの点。白抜きの点を押すと、その名前が出る札の一覧 |
| 2 | 主体が多すぎるとき | 12点まで円に並べる。13点を超えたら、動きの少ない方を「ほかN」の点1つにまとめ、押すと一覧。名前は8字で切る |
| 3 | 相手が場所不明 | 外の札に「→ 場所不明（N）」を置き、押すと一覧 |
| 4 | 相手が枠（市場・国際機関・地域） | 外の札（「→ 市場（N）」）を押すと、その国と枠の間の動きの札の一覧（地図の下の枠と同じ形） |
| 5 | 網が空になる国 | 点が1つでも網を出す。点が1つだけの国は、中央に点1つ |
| 6 | 今の「その国が絡む札の一覧」 | 網に置き換え、網の下に「この国の札を一覧で見る」を1行残す |

- **点の違いの説明を網の上に1行出す**：「塗った点：登録している主体（押すと詳しく）。白抜きの点：札に名前だけ出てきた相手（押すとその札）。」
- 点になるのは、その国の側に置かれた名前（国内の動きは両端、国の外との動きはこの国の側の端）。主体の表の名前・別名に当たるものは主体の点にまとめる。
- 外の札は、相手（国・枠・場所不明）と向き（→ 出る／← 入る）ごとに1枚。件数を添える。
- 網から開いた札の一覧・主体の詳細から戻ると、網に戻る。網の「戻る」は地図に戻る。

---

## 5. M5：世界の画面の一番上に小さな地図（v20.5）

- 世界の画面の一番上（地層の1行の上）に、高さ178px の小さな地図を置きます。
- 描くのは、**直近7日の線だけ**です。国の点は押せません（押すと大きな地図へ）。
- 地図の部品が読めなかったとき（初めて開いた日にネットが無い等）は、小さな地図の場所に1行だけ出します。「地図を読み込めませんでした（理由）。お金の流れは下の一覧で見られます」
- その下は今のまま：お金の鎖 → お金の動き（単発）→ 今日動いた主体 → 背景の札 → 探す。
- 切替（タブ）にはしません。朝の画面は変えません。

---

## 6. プロンプトの変更

| 段階 | 変更 |
|---|---|
| M1 | 毎朝の取り込みの actors に `country`（3文字コード。市場・国際機関・地域は MKT・INT・REG。分からなければ空）を足す |
| M2 | 初期投入のプロンプト（地域ごとに4本）を新しく作る。JSON の一番上に `seed` |
| M3 | 変えない（換算は端末がやる。生成には書かせない） |

字数は、M1 で「actors の説明に2行」増える見込みです。実装のときに測って報告します。

---

## 7. 確かめ方

**M1**
1. 種の主体21件に 1.2 の表どおり国が付くこと。自分で直した国を、起動しても上書きしないこと。
2. 実データの13件と同じ形の札を localhost に入れ、次のとおりに分かれること。
   - 国をまたぐ4本が線になる。
   - 国内6本は「国内 N件」に入る。
   - 場所の定まらない3本は、市場の枠か「場所不明」に入る。
3. 幅360・320で、地図が崩れないこと。点を押して国を選べること。欧州で「この辺りの国」の一覧が出ること。
4. 線を押して札へ行けること（2つの出典なら2枚）。
5. 鎖の欄に1件の塊が出ず、［物］の動きが単発に出ること。
6. 地図の部品の読み込みに失敗したとき、理由の1行が出て、ほかの画面が壊れないこと。

**M2**
1. 4本のプロンプトの字数を測る（本物のデータで。v20.2.2 で 12,000字の上限は外した）。
2. `seed` の印がある JSON だけ札に `seed` が付き、未読と「今日読んだ札」に数えられないこと。印の無い JSON（毎朝の分）には付かないこと。
3. `seeds` が両端末で合わさること。

**M3**
1. 金額の読み方：実データの書き方（€70 billion・EUR 90 billion・$3.3 billion・276億ドル・15兆3993億円・100億ドル超・3,500百万ドル）がすべて読めること。オンス・バレルは読まないこと。
2. 換算：ECB の値で、手で計算した値と一致すること。ECB に無い通貨で、米財務省の値に落ちること。どちらにも無い通貨で、換算しないこと。
3. 元の `amount` が変わっていないこと。生成が書いた `usd` を取り込みで捨てること。
4. 5段の太さと見本。

**M4・M5**
1. 国の網で、点が円に並び、矢印が引かれ、国の外への札から別の国へ移れること。
2. 小さな地図が7日の線だけを描き、押すと大きな地図へ行くこと。
3. 幅360で崩れないこと。

---

## 8. 決まったこと（指示書 sekai_chizu_jisso_shiji.md の回答）

1. 「TSMC → 先端工程と海外工場」の受ける側は **「場所不明」**。M1 では from／to に国を書かせない（v20.2.1 から「国名で始まる相手」と指示。国の定まらない相手は場所不明のまま）。
2. **EU は「地域」の枠**。
3. **国の集まり（「欧州の同盟国とカナダ」など）も「地域」の枠**。
4. M5 までの間、大きな地図への入口は **「お金の流れ」の見出しの横の「地図」**。
5. **国の別名の表は付録A**。
6. 追加 A：過去の札を換算したら、件数を地図の下に1行出す（3.3）。
7. 追加 B：国名は別名の表と丸ごと一致したときだけ当てる（1.2）。

---

## 付録A　国の別名の表（丸ごと一致で引く）

国の正式な日本語名（公開データ i18n-iso-countries の日本語名）に加えて、報道の呼び方を足します。**名前がこの表のどれかと丸ごと一致したときだけ、その国に当てます。**

| 国 | 足す呼び名 |
|---|---|
| USA | 米国・米・アメリカ・合衆国・米国政府・FRB（v20.1） |
| CHN | 中国 |
| JPN | 日本・日本国・日本銀行・日銀（v20.1） |

「財務省」は足さない（どの国にもあるため。指示書 sekai_chizu_naoshi_shiji.md 4）。
| GBR | 英国・イギリス |
| RUS | ロシア |
| IRN | イラン |
| KOR | 韓国 |
| PRK | 北朝鮮 |
| TWN | 台湾 |
| DEU | ドイツ・独 |
| FRA | フランス・仏 |
| ITA | イタリア |
| CAN | カナダ |
| MEX | メキシコ |
| BRA | ブラジル |
| IND | インド |
| SAU | サウジアラビア・サウジ |
| ARE | アラブ首長国連邦・UAE |
| ISR | イスラエル |
| TUR | トルコ |
| EGY | エジプト |
| QAT | カタール |
| AUS | オーストラリア・豪州・豪 |
| IDN | インドネシア |
| VNM | ベトナム |
| THA | タイ |
| SGP | シンガポール |
| HKG | 香港 |
| CHE | スイス |
| NLD | オランダ |
| BEL | ベルギー |
| POL | ポーランド |
| ESP | スペイン |
| NOR | ノルウェー |
| UKR | ウクライナ |
| ARG | アルゼンチン |
| ZAF | 南アフリカ |
| VEN | ベネズエラ |
| IRQ | イラク |
| PAK | パキスタン |
| PHL | フィリピン |
| MYS | マレーシア |

英語の国名（United States・China・Japan など）も、公開データの英語名で丸ごと一致させます。

## 付録B　場所の無い相手の名前の決め方（丸ごと一致・含む）

| 枠 | 丸ごと一致する名前 | 名前に含まれていたら |
|---|---|---|
| 国際機関（INT） | IMF・国際通貨基金・世界銀行・BIS・国際決済銀行・WTO・世界貿易機関・NATO・北大西洋条約機構・国連・国際連合・OPEC・OPEC＋・OECD・G7・G20 | — |
| 地域（REG） | EU・欧州連合・欧州委員会・ECB・欧州中央銀行・ユーロ圏 | 同盟国・加盟国・産油国・諸国・各国 |
| 市場（MKT） | — | 市場・買い手・購入者（「保有者」は一般名として国内に寄せる。回答書 sekai_chizu_m1_kaitou.md） |

「含む」の決め方は、国の判定より後に当てます（名前が別名の表と丸ごと一致すれば、そちらが先）。

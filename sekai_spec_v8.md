# 一万日 v17 仕様書 — 世界：予測を AI にも書かせ、答え合わせを自動にする（C）

指示書の A・B・C のうち **C**。B（`sekai_spec_v7.md`）は実装済み。次は A。
既存の言葉・引く・相談・問い・撮る・筋トレ・散歩には触らない。**API は使わない**。

---

## 0. いまの形と、足りないもの

生成は **分かれ目（fork）** だけを書き、上／下／横ばいを押すのは自分。
`prediction.result` の器はあるが、**判定する経路がどこにも無い**——予測タブには
「答え合わせは次の段階で入ります」と書いてあるだけ。

本人の言葉：

> 予測タブも自動的に埋めてほしい。〜だからこうなりそう、とかほしい。
> その時期になったらどうだったかの検証までを行えるようにしてほしい

やることは2つ。**生成にも予測させる**（C-1）。**答え合わせを一次ソースでやる**（C-2）。

そして副産物がひとつ大きい。同じ分かれ目に生成と自分の両方が答えるので、
**「AI の予測は当たるのか」が測れる**。当たらないと分かれば、生成の見立ては
読み流してよいものになる。当たるなら、自分の見立てを直す材料になる。

---

## 1. 判定は「どちらが当たったか」ではなく「何が起きたか」を聞く

ここが C の設計の芯。

素朴にやると、答え合わせのプロンプトに「生成の予測は当たったか」「自分の予測は
当たったか」を両方聞くことになる。**これはやらない。**

- 生成に予測の採点をさせると、**採点が甘くなる**方向に力がかかる
- 自分の pick を見せて判定させると、**それに引きずられる**
- 同じ事実に2つの判定が返ると、食い違ったときにどちらが正しいか分からない

だから聞くのは**「その分かれ目は実際どうなったか」1つだけ**。

```
返り値： { eventId, outcome: 'up'|'down'|'flat'|'unknown', evidence, url }
```

**当たり外れは端末が引き算する。**

```js
if (outcome === 'unknown' || !url) → 保留
else  leanResult = (p.lean === outcome) ? 'hit' : 'miss'
      result     = p.pick ? (p.pick === outcome ? 'hit' : 'miss') : ''
```

3つ効く：

1. 生成が書くものが**半分**になる（url を12本調べる作業に集中できる）
2. 2つの的中率が**厳密に同じ基準**で出る。比べる意味がある
3. **自分の pick をプロンプトに載せなくてよい**——生成が忖度する余地が消える

`lean` も同じ理由で載せない。プロンプトに載せるのは分かれ目・出来事・出典だけ。

---

## 2. C-1：生成に予測を書かせる

### 2.1 置き場所は prediction 側

`fork` は**問い**、`lean` は**答え**。別のもの。
`fork` に推定を入れると「fork は誰が見ても同じ判定になる検証可能な問い」という
縛りが緩む。`prediction` には既に `history`・`result`・`judgedAt` があり、
答え合わせが触るのはそこ。

```
prediction: {
  lean:    ''|'up'|'down'|'flat',      // C: 生成の見立て。fork がある札だけ
  because: '理由1文60字',               // C: 推定の印（琥珀）
  leanResult: ''|'hit'|'miss'|'hold',  // C: 生成の当たり外れ

  pick, text, days, writtenDate, dueDate, setAt, history,   // 自分の側（既存）
  result: ''|'hit'|'miss'|'hold',      // 自分の当たり外れ（既存）

  outcome: ''|'up'|'down'|'flat',      // C: 実際どうなったか（判定の素）
  evidence: '根拠1文',                  // C
  evidenceUrl: 'https://...',           // C: 無ければ保留
  judgedAt: '',                         // 既存
  nextCheck: '',                        // C: 保留のとき次に見る日
  holdCount: 0,                         // C: 2回で打ち切り
}
```

### 2.2 worldPredictionWritten を2つに割る

いまの `worldPredictionWritten(ev) = !!(p.pick || p.text)` は
**予測タブ・due 索引・コピー文の3か所**で使われている。lean が自動で入ると
意味が壊れるので、述語を割る。

```js
function worldPredictionMine(ev) { return !!(ev.prediction.pick || ev.prediction.text); }
function worldPredictionLean(ev) { return !!ev.prediction.lean; }
function worldPredictionAny(ev)  { return worldPredictionMine(ev) || worldPredictionLean(ev); }
```

| 使う場所 | どちら |
|---|---|
| コピー文「自分の予測」 | `Mine`（生成の見立ては別の行に出す） |
| due 索引・答え合わせの対象 | `Any` |
| 予測タブの一覧 | `Any` |
| 「予測」の印（`worldPredictionSummary`） | `Any` |

### 2.3 期限は札の日付から数える

`worldPredictionDue` は `writtenDate` が空なら `todayStr()` を入れている。
lean のときは **`writtenDate = ev.eventDate`** にする。

理由：**同じ分かれ目に、生成と自分が同じ期限で答えないと的中率が比べられない**。
自分が3日後に pick を押しても `writtenDate` は既に埋まっているので期限は動かない。
そして fork の規則が「その出来事から3ヶ月で決着」なので、時計は出来事から始まるのが正しい。

期限は **90日で固定**（生成に選ばせない）。自分が `WORLD_PREDICT_CHOICES` で
変えるのは今までどおりできるが、変えると生成と期限がずれるので、
**lean がある札で日数を変えたときは「生成の期限は 〜 のままです」を出す**。

### 2.4 fork を1回の取り込みで3件までに絞る

これが C で一番効く縛り。

実データで **fork は約半分の札に付く**（5件中3件、2件中1件）。毎日7件取り込めば
1日3〜4件、90日後に**300件が待つ**。答え合わせは12件/回なので、週2回回しても追いつかない。

```js
const WORLD_FORK_PER_BATCH = 3;
```

- プロンプトに書く：「分かれ目を書くのは、上から3件までにする」
- **取り込みでも切る**（プロンプトは破られる）。`order` の若い3件だけ `fork` を残し、
  4件目以降は `fork = null` にする。落とした数を取り込み結果に出す
- 生成には既に「重要だと思う順に並べる。上から読まれる」と指示してあるので、
  上位3件に絞るのは「重要な3件に絞る」と同じ意味になる

**分かれ目が立つ札は、その週で数件しかないのが正常。** それ以外は分かれ目なしでよい。

これで待ち行列は 90日 × 3件/回 に収まる。毎日取り込んでも上限270件だが、
**実際は fork が3件も立たない日が多い**ので、体感はその半分以下になる。
数か月回して詰まるようなら `WORLD_FORK_PER_BATCH` を 2 に下げる。

### 2.5 生成プロンプトの差分

`fork` の説明に1行足す：

```
  - 分かれ目を書くのは、上から3件までにする。4件目からは fork を null にする。
    分かれ目が立つ札は、その週に数件しかないのが正常。
```

`fork` の直後に2項目足す：

```
- lean：その分かれ目が、あなたの見立てではどちらに転ぶか。
  fork が null のときは書かない。
  "up"／"down"／"flat" のどれか。
  type が "level" なら up=示された水準に届く／down=届かない／flat=どちらとも言えない。
  type が "event" なら up=起きる／down=起きない／flat=どちらとも言えない。
  逃げないこと。flat は「本当にどちらにも転びうる」ときだけ。
- because：なぜそう見立てたかを1文・60字以内。
  これは推定です。出典に書かれた事実ではなく、あなたの読みを書く。
  何がそう思わせるのかを書く。どちらでもない文を書かない。
  良い例：「減額幅を決めるのが財政側なので、金利が上がるまで動かない」
  悪い例：「今後の動向次第で変わる可能性がある」
```

出力形式：

```
  "fork":{"text":"...","type":"level"},
  "lean":"up","because":"...",
```

`lean`・`because` は札の直下に置く。`prediction` は端末側の器なので、生成には見せない。

**プロンプトの増分は約 +500 バイト。**

---

## 3. C-2：答え合わせのプロンプト

### 3.1 対象と件数

```js
const WORLD_JUDGE_PER_BATCH = 12;
```

`worldDueEvents(todayStr())` から**期限（`nextCheck || dueDate`）の古い順に12件**。
根拠：1件あたり 分かれ目の文＋日付＋出来事1行＋出典で約150字、12件で約1800字。
**生成に url を12本調べさせるのが実際の上限**。

超えたら「あと N件」を出して、もう一度回す。

### 3.2 プロンプト全文

```
（SHOOT_PROMPT_HEAD）
あなたは、少し前に立てた「分かれ目」が実際どうなったかを調べる係です。
予測しません。採点しません。何が起きたかだけを調べて返します。

■ 必ず守ること
- web 検索を必ず使う。検索せずに書かない。
- 一次ソース（中銀・政府・国際機関・統計）で確認できた事実だけを書く。
- 分かったことと、分からなかったことを正直に分ける。
  決着していない・調べても出てこない分かれ目は "unknown" にする。
  埋めるくらいなら "unknown" のほうがよい。
- url が無いものは書かない。出典が出せない結果は "unknown" にする。
- 前に誰が何を予測したかは書いていない。予測に合わせて事実を選ばない。
- JSON のみ。ほかの文章を返さない。

■ それぞれに書くこと
- eventId：下の一覧にある id だけ。
- outcome：その分かれ目が実際どうなったか。
  "up"      … 下に書いた「up とは」に当たることが起きた
  "down"    … 起きなかった
  "flat"    … 決着したが、どちらとも言えない形で終わった
  "unknown" … まだ決着していない、または調べても分からなかった
- evidence：そう言える事実を1文・80字以内。数字か日付を含める。
  「〜と報じられた」ではなく「〜が〜した」と書く。
- url：その事実が載っているページ。outcome が "unknown" のときは空でよい。

■ 出力形式
{"results":[
  {"eventId":"w_20260610_1","outcome":"up",
   "evidence":"米エネルギー情報局の週報で10月2日に留出油在庫が9860万バレルとなり1億バレルを割った",
   "url":"https://..."},
  {"eventId":"w_20260612_2","outcome":"unknown",
   "evidence":"IMF理事会の議題に上がっておらず、承認の記録が無い","url":""}
]}

────────────────────────────────────────
■ 調べる分かれ目（12件）
[w_20260610_1]  2026-06-10
  分かれ目：米政府の見通しどおり、10月にディーゼル在庫が1億バレルを割るか
  up とは：割る　／　down とは：割らない
  もとの出来事：米政府が留出油在庫の見通しを下方修正した
  出典：EIA 週間石油在庫統計 2026-06-10  https://...
  期限：2026-09-08
  ...（12件）
```

**「up とは何か」を1件ずつ書く**のが要。`type` が level と event で意味が反転するので、
生成側に「level なら…」を毎回判断させると事故る。**端末で文言に展開してから渡す。**

### 3.3 取り込み

```js
function worldImportJudgeJson(raw)
```

1件ごとに：

| 条件 | 扱い |
|---|---|
| `eventId` が端末に無い | 無視（数える） |
| `outcome` が `unknown` | **保留**（3.4） |
| `url` が無い・http(s) でない | **保留**（判定を捏造させない） |
| `evidence` が空 | **保留** |
| それ以外 | 判定する |

判定するとき：

```js
p.outcome = outcome; p.evidence = evidence; p.evidenceUrl = url;
p.judgedAt = nowIso(); p.nextCheck = '';
p.leanResult = p.lean ? (p.lean === outcome ? 'hit' : 'miss') : '';
p.result     = p.pick ? (p.pick === outcome ? 'hit' : 'miss') : '';
```

**`pick` が無ければ `result` は空のまま。** 自分の的中率は、自分が答えた分だけで数える。
押さなかったものを「外れ」にしない。

取り込み結果：

```
12件のうち 9件を判定しました（生成 当たり6・外れ3／自分 当たり4・外れ2）。
※ 3件は保留にしました（決着していない 2件、出典URLが無い 1件）。14日後にもう一度出ます。
```

### 3.4 保留と打ち切り

```js
const WORLD_HOLD_DAYS = 14;
const WORLD_HOLD_MAX  = 2;
```

```js
p.holdCount = (p.holdCount || 0) + 1;
if (p.holdCount >= WORLD_HOLD_MAX) {
  p.result     = p.pick ? 'hold' : '';   // 打ち切り
  p.leanResult = p.lean ? 'hold' : '';
  p.judgedAt = nowIso(); p.nextCheck = '';
} else {
  p.nextCheck = addDays(todayStr(), WORLD_HOLD_DAYS);   // 14日後にもう一度
}
```

打ち切りが要るのは、**永久に戻り続けると答え合わせが詰まる**から。
2回調べて決着しないものは、**fork の書き方が悪かった**ということで、それ自体が読める情報になる。

`dueDate` は元の期限のまま残す（履歴を書き換えない）。次に見る日は `nextCheck`。

### 3.5 due 索引の直し

`updateWorldIndexMonth` の中の1か所。

```js
// いま
if (!(p.pick || p.text) || !p.dueDate || p.result) return;
(wd.index.due[p.dueDate] = wd.index.due[p.dueDate] || []).push(e.id);

// これから
if (!worldPredictionAny(e) || p.judgedAt) return;   // 判定済みは外す
const d = p.nextCheck || p.dueDate;
if (!d) return;
(wd.index.due[d] = wd.index.due[d] || []).push(e.id);
```

`p.result` ではなく **`p.judgedAt`** で外すのが要。`pick` が無い札は判定しても
`result` が空のままなので、`result` で見ていると**永久に索引に残る**。

---

## 4. 画面

### 4.1 答え合わせ（新しい画面 view:'judge'）

予測タブの先頭に入口を置く。**期限が来た日だけ出す。**

```
答え合わせ 12件  ▸
```

開くと：

```
答え合わせ                          あと 3件（次は 10/2）

[プロンプトをコピー（12件）]
┌──────────────────────────────┐
│ {"results":[...]} をここに貼る │
└──────────────────────────────┘
[結果を取り込む]

─────────────────────────────────
的中率              生成 6/9（67%）   自分 4/6（67%）

判定したもの
  10月にディーゼル在庫が1億バレルを割るか          割った
    ［推定］生成 割る       当たり
            自分 割らない   外れ
    根拠：EIA週報で10月2日に9860万バレルとなり1億バレルを割った  [開く]

  IMF理事会が年内に承認するか                      保留（1回目）
    理事会の議題に上がっておらず、承認の記録が無い
    10/24 にもう一度出ます
```

- **生成の行には琥珀の「推定」バッジ**（v12 の `--guess` と同じ）。判定の結果は事実、
  見立ては推定、と見た目で分かれる
- 根拠の url は「開く」で外部へ
- 保留は何回目かを出す（2回目で打ち切りだと分かる）

### 4.2 予測タブ

先頭に **的中率の1行**を足す。

```
的中率              生成 6/9 ・ 自分 4/6
答え合わせ待ち                     18件
```

いまの「答え合わせは次の段階で入ります」は消す。

### 4.3 札の中

fork の3択の**上**に、生成の見立てを出す。

```
10月にディーゼル在庫が1億バレルを割るか
［推定］生成の見立て：割る                    なぜ ▸

[割る] [割らない] [どちらとも]      ← 自分の3択（既存）
```

**生成の見立てを先に見せるのは是か**、という問題がある。引きずられる。
だが隠すと、「予測タブが初日から埋まる」という C の目的が札の画面に出ない。

折衷：**見立て（割る／割らない）は出すが、`because`（理由）は自分が押すまで畳む。**
押したあとは開いたまま出す。**理由を読む前に自分で決める**形になる。

---

## 5. 定数

```js
const WORLD_FORK_PER_BATCH  = 3;    // 1回の取り込みで分かれ目を書く札の数
const WORLD_JUDGE_PER_BATCH = 12;   // 1回の答え合わせに載せる数
const WORLD_HOLD_DAYS       = 14;   // 保留を次に見るまでの日数
const WORLD_HOLD_MAX        = 2;    // 何回保留になったら打ち切るか
const WORLD_BECAUSE_MAX     = 60;   // because の字数
const WORLD_EVIDENCE_MAX    = 80;   // evidence の字数
const WORLD_OUTCOMES        = ['up', 'down', 'flat'];
```

---

## 6. 守ること

- **判定は「どちらが当たったか」ではなく「何が起きたか」を聞く。** 当たり外れは端末が引き算する
- **自分の pick も生成の lean も、答え合わせのプロンプトに載せない。** 忖度させない
- **url が無い結果は取り込まない（保留にする）。** 判定を捏造させない
- **`evidence` が空・`outcome` が unknown も保留。** 埋めさせない
- **生成の見立ては推定の印**（琥珀のバッジ）。判定の結果は事実
- **`result` と `leanResult` を混ぜない。** 自分の的中率は自分が答えた分だけで数える
- **fork は1回3件まで。** 入口で絞らないと答え合わせが追いつかない
- 期限は90日固定。`writtenDate` は札の日付（生成と自分で同じ期限にする）
- 保留は2回で打ち切る。永久に戻り続けると詰まる
- 既存のデータは消さない。朝の流れは変えない

---

## 7. 実装後に見る数字

- **生成の的中率**。50%前後なら、生成の見立ては読み流してよい。
  70%を超えるなら、自分の見立てを直す材料になる
- **自分の的中率との差**。同じ分かれ目・同じ期限なので、この差だけが意味を持つ
- **保留の割合**。3割を超えるなら fork の書き方が「決着しない問い」に寄っている
- **答え合わせ待ちの件数の推移**。増え続けるなら `WORLD_FORK_PER_BATCH` を 2 に下げる
- 打ち切り（2回保留）の件数。多いなら fork の規則を締め直す
